# Teka RDC — Development Progress

## Current Phase: Payouts / commission / admin action center — all 8 PRs merged (last #652 `b6f48f5`); pre-release audit done: NEEDS FIXES BEFORE RELEASE (F1 earning-state label + D7 decision). Tracker `docs/payouts-commission-action-center.md`.
## Last completed: Mobile 0.1.7+9 → TestFlight (buyer + seller), run 33847315357; Seller / Admin UX modernization RELEASED (PR #639, main aba6d01).
## Status: decisions D1–D6 approved 2026-09-04, D7 analysed and pending the operator; F1 fix pending approval; dev DB migrated (2 manual migrations pending for prod, both auto-apply, neither on main); no production change.
## Last Updated: 2026-09-04

## Post-phase chronology — condensed index (moved out of CLAUDE.md §8 on 2026-06-03)

- **Sep 4, 2026** — **Payouts initiative — whole-initiative pre-release audit** on `develop` `b6f48f5` (PR 8 merged as #652): code trace of order → earning → commission → balance → payout; security re-audit (403/404/401 evidence); full six-app regression green; integrated runtime pass on Chrome + Android emulator with a deleted fixture (iOS build renders, not driven). Findings: **F1** `earningState()` returns `PAID` for an earning reserved in an open payout (fix before release); F2 mobile feed needs pull-to-refresh; F3 email subjects still « Retrait … »; D7 analysed (recommendation: FAILED on COD cancellation + backfill; decision pending); outbox / FINANCE / commission display / `field:"unknown"` stay follow-ups. Seller-mobile next version `0.1.8+10`. Verdict: NEEDS FIXES BEFORE RELEASE.
- **Sep 4, 2026** — **Payouts initiative PR 8 — Docs + release preparation** (`docs/payouts-initiative-release-prep`; PR 7 merged as #651 `3d859fa`): docs-only. `docs/payouts.md` (notifications table, 409 semantics, admin detail context, earnings `state`, surfaces + deep links, commission administration, runbook), `api-reference.md` (payout detail, earnings `state`, commission history + `expectedPreviousRate`, seller override endpoints, `stats.actionCenter`, seller notifications), `push-notifications.md`, `architecture.md`, `order-workflow.md`, `CLAUDE.md` Payments row; release-prep checklist (two pending auto-apply migrations, no env change, seller-mobile version bump at release). D7 + follow-ups preserved for the whole-initiative re-audit.
- **Sep 4, 2026** — **Payouts initiative PR 7 — Regression / security / performance review** (`feat/initiative-review`; PR 6 merged as #650 `c64f6e4`): audit written to the tracker (defects 1–11 closed by PR 2; every brief security/performance item verified in code; defect 13 → D7). Closed the four documented defect-12 leftovers: API-derived `state` on `GET /v1/sellers/earnings` rows (HELD / AVAILABLE / RESERVED / PAID / REVERSED), « 10 % » / « 8,25 % » commission display instead of « 0.1% » / « 0% », truthful earning labels on both clients, `showAppSnackbar` on payout request, « 5.000 FC ». No schema change. Full six-app regression green; runtime-verified on Chrome + Android emulator with a deleted fixture.
- **Sep 4, 2026** — **Payouts initiative PR 6 — Payout feed notifications + Seller Web / Mobile deep links** (`feat/payout-feed-notifications`; PR 5 merged as #649 `471b2e7`): `UserNotificationType.PAYOUT` via additive auto-apply migration; approved / paid / rejected (« Virement échoué » from PROCESSING) write a `PAYOUT` feed row first (`createIfAbsent`) then push + email fallback, only after the committed conditional transition (retry → 409, no duplicate); REQUESTED/PROCESSING silent; no Action Center entry (decision). `GET /v1/sellers/payouts/:id` owner-scoped. seller-web: bell → earnings `?tab=payouts&payout=<id>` detail card, validated post-login redirect (middleware keeps the query), vitest added (11). seller-mobile: push/feed → payout detail or Virements tab, `go` for tab roots, `from`-preserving login, earnings refresh signal, shared status vocabulary. Verified on the Android emulator + Chrome with a deleted QA fixture. API 590 unit / 142 e2e; seller-mobile 128 tests.
- **Sep 4, 2026** — **Payouts initiative PR 5 — Commission administration** (`feat/commission-admin`; PR 4 merged as #648 `dccd864`): re-audit confirmed the PR 2 model (seller override → leaf category → platform default, resolved at delivery per item, snapshotted on earning + items, never recomputed) so **no schema change**. API: `GET/PUT/DELETE /v1/admin/sellers/:sellerProfileId/commission` (effective context; set/clear audited in-transaction; 404/400/409), `GET /v1/admin/commission-settings/history`, and optimistic concurrency via `expectedPreviousRate` (409 naming the current value) on seller, category and platform rates — added after browser QA showed a stale screen could silently overwrite a colleague's change. admin-web: `lib/commission.ts` (string/integer percent ↔ Decimal(5,4), French formatting, 11 vitest), Commissions page reworked (default in force + last change, category rates, history, confirmations, error/empty states, no fabricated 10 %), seller « Commission » card (applied / specific / default, radio default vs specific, confirmations, 409 → reload). Browser-verified on the dev DB incl. historical earnings untouched after a default change. API 576 unit / 141 e2e; admin-web 31 vitest, build clean.
- **Sep 4, 2026** — **Payouts initiative PR 4 — Admin payout workflow** (`feat/admin-payout-workflow`): `GET /v1/admin/payouts/:id` returns balances, resolved actors and the `admin_audit_logs` trail (additive); stale/concurrent admin transitions → 409 (validation stays 400); reject reason 5–500 chars; admin-web `lib/payout-workflow.ts` mirrors the state machine with labels that never call an approval a payment (« Approuvé — à payer » / « Virement en cours » / « Payé »), and the payouts page gained per-state actions with confirmation dialogs (payment reference required to mark paid, reason required to reject, « Transfert échoué » from PROCESSING), double-submit lock, 409 → refresh, and a detail drawer (financial context, snapshotted destination, timeline with actors, reserved earnings + clawback flag, audit history). Browser-verified end-to-end on the dev DB (seeded 63.000 FC request → APPROVED → PROCESSING → COMPLETED, three audit rows, approved/paid seller emails; curl-proved 409 on wrong-state approve and on a second complete). API 552 unit; admin-web 20 vitest, type-check + build clean.
- **Sep 4, 2026** — **Payouts initiative PR 3 — Admin Action Center** (`feat/admin-action-center`): `ADMIN_QUEUES` where-builders shared by the stats counts and the list endpoints (reconciliation spec-pinned), additive `/v1/admin/stats.actionCenter` (payouts awaiting review / awaiting payment with amounts, sellers, products, returns, pickup, dispatch), dashboard « À traiter maintenant » tiles deep-linking to filtered queues with muted zeros, summary line, explicit error/loading states; payouts + sellers pages honour `?status=` (sellers application tabs now list from `/sellers/applications?status=`); admin-web gains vitest (8 tests). Browser-verified on the dev DB via an isolated local stack. API 551 unit; admin-web type-check/build clean.
- **Sep 4, 2026** — **Payouts initiative PR 2 — API financial foundation** (`feat/api-payout-ledger-foundation`): decisions D1–D6 approved (PROCESSING→REJECTED; PROCESSING blocks requests; seller → category → global precedence, no hardcoded fallback; rate snapshotted at delivery; per-item commission; payability requires DELIVERED with auditable reversal). Implemented: `commission-math.ts` (BigInt, half-up), per-item `computeBreakdown` + `OrderItem.commission*` snapshot, `reverseEarning` marks instead of deleting, COD completion + earning inside `markDelivered`'s transaction, forced exit from DELIVERED reverses, payout request under `SELECT … FOR UPDATE` with guarded reservation + `payouts_one_open_per_seller` partial index, conditional admin transitions with actor columns + `admin_audit_logs`, commission settings audited. Migration `2026-09-04_payout_commission_ledger_foundation.sql` (additive, auto-apply) rehearsed twice on dev. API 547 unit / 135 e2e, type-check clean.
- **Sep 4, 2026** — **Payouts / commission / action-center initiative — Phase 0 audit** (`docs/payouts-commission-action-center.md`): balance is lazy from `SellerEarning`; commission snapshots at delivery (rate + amount, no rule id); approve ≠ paid already (`COMPLETED` = manual mark-paid); destination + funds reservation snapshotted. Defects: payout-request race, admin transition TOCTOU, fire-and-forget earning creation, forced-cancel keeps payability, return deletes the ledger row, float commission math, first-item-only category rate, no actor/audit on reject/process/complete or commission edits, payout notifications skip the feed and neither client routes them. Proposed hardened state machine, `seller ?? category ?? global ?? 10%` precedence with `NULL = default`, additive migrations, PR order; six financial-meaning decisions blocked on approval.
- **Sep 4, 2026** — **Mobile 0.1.7+9 released to TestFlight (buyer + seller)** — bump #641, release PR **#643** (`main` `f113778`), *Release mobile IPA* run **`33847315357`** `app=both`: buyer 0.1.7 (1788505790) → « Teka Buyer test team », seller 0.1.7 (1788505776) → « Testers Teka RDC », both distributed and read back. Pre-release gates: buyer-mobile 238 tests / 6 known infos, seller-mobile 115 tests / 17 known infos, local `flutter build ios --flavor production` for both apps (Info.plist `0.1.7 (9)`, `Teka` / `Teka Vendeur`). Also #642: `release-mobile-{ipa,aab}.yml` now inject `POSTHOG_API_KEY` into production mobile builds (previously every prod mobile build shipped with PostHog disabled — `docs/analytics.md` open item, now closed). Android AAB / Play Store not part of this step.
- **Sep 4, 2026** — **Seller / Admin UX modernization RELEASED** (release PR **#639** `develop → main`, merge **`aba6d01`**, deploy run **`33844482470`** success, back-merged so `main == develop`): seller-mobile #634/#635/#636 (lists, action center, forms/details/notifications/earnings, native splash), seller-web #637 and admin-web #638 (grouped navigation, accessible drawers, URL-backed filters, explicit error states). Pre-merge review fixes: mobile `loadMoreError` split + `showAppSnackbar` (`40dee52`), seller-web wallet/review error states + drawer auto-close at `md` (`8a29c76`), admin avatar restored + drawer auto-close at `lg` (`d8448e7`). No migration/schema/env/dependency change; `apps/api` = one spec. Validation: type-check clean, seller-mobile 115 tests / 17 existing infos, both web builds clean, CI + CodeQL + docker-build-check green. Production: seller/admin 200 + noindex intact, API normal.
- **Sep 4, 2026** — **Admin UX modernization implemented and real-role validated** on `codex/admin-web-ux-modernization`: grouped 22 destinations by operator workflow with consistent icons; accessible mobile drawer; responsive notifications; clearer authoritative action center; URL-backed complete order/product filters; recoverable API errors; contained wide tables; shared hierarchy applied to dashboard, moderation, orders, sellers, buyers, returns, transactions, payouts and reports. Type-check/build pass. Read-only smoke QA against the development API used the real ADMIN role and verified 17 users, 3 sellers, 8 orders, 3 pending products and 121,000 FC revenue; the repository-provided temporary password was cleared immediately. No business mutation, schema/SEO/production change.
- **Sep 4, 2026** — **Seller Web UX modernization implemented and read-only validated** on `codex/seller-web-ux-modernization`: grouped icon navigation, modal mobile drawer with Escape/focus containment, responsive notifications, compact authoritative action center, URL-backed order/product filters and explicit recovery/empty states. Shared hierarchy now covers revenues, reviews, promotions and profile. Type-check/build pass; local development API QA with the seeded seller account verified auth, 294 products, pagination, notifications, wallet and empty-order states without data mutations. Admin is the next platform phase; no schema/API/SEO/production change.
- **Sep 3, 2026** — **Seller Mobile forms/details/secondary routes + native splash** (local branch `codex/seller-mobile-forms-details`, based on merged PRs #634/#635): made auth and seller-application forms keyboard/2×-text safe; hardened product edit, category/brand/attribute failures and image semantics; clarified order actions and 2-day earnings hold; added notification pagination/error recovery, Help & support, responsive earnings/payout content, and a density-safe native splash with an Android 12+ contrasting glyph and visible status bars. **112 Flutter tests pass**; analyzer has the same 17 info-only notices and no warnings/errors. Android API 34 cold launch resolves the real activity and renders the actual dashboard with synthetic fixtures. No API/schema/auth/analytics/SEO change and no production data. Real signed-in API, FCM, camera/gallery and physical-device QA remain for the final validation matrix.

> This is the authoritative chronological index of post-phase work. CLAUDE.md §8 now points here instead of carrying the list inline (the growing list kept tripping the 40k-char CLAUDE.md performance warning). Append new initiative one-liners here, not to CLAUDE.md. Full narratives for each item live in the dated sections further down this file and in `STATUS.md`.

- **Sep 3, 2026** — **Seller Mobile action center** (draft PR **#635** → `develop`, implementation **`c3c5805`** on `codex/seller-mobile-action-center`, dependent on foundation PR #634): replaced paginated dashboard counts with account-scoped existing order/product stats; four exact-status task queues, query routing, mutation/push/resume refresh, stale-response guards, and task completion that preserves the active list filter. Kept unread notifications separate. Compact neutral dashboard, explicit partial failure/loading/empty states, Material 3 navigation and responsive catalogue counts; two reproduced detail overflows fixed. **93 Flutter tests**, **3 API stats unit tests**, analyzer **17 existing infos / no warnings or errors**. Android/iOS development previews use an in-memory API; Android confirmation verified counters 25→24 and 1→2. No schema/API behavior, security guard, analytics, web/SEO or splash change; no merge/deployment. Full deliverable/QA limits and next forms/detail/splash lots: `docs/seller-admin-ux-modernization.md`.
- **Sep 3, 2026** — **Seller / Admin UX modernization started — Phase 0 + first Seller Mobile foundation lot**: draft PR **#634** → `develop`, implementation **`90a3938`** on `codex/seller-mobile-ux-foundation` (baseline `6e6d151`; remote CI pending). Audited all seller routes, representative web/admin structure, real order/product transitions and native splash assets; documented prioritized findings and staged PR boundaries in `docs/seller-admin-ux-modernization.md`. Implemented shared natural-height filters, readable status badges (≥4.5:1), scrollable empty/error states and static list placeholders; orders/products cards now wrap long metadata and amounts; search-clear cancels pending debounce; received/returned/legacy-shipped filters are reachable. **69 tests pass (42 existing + 27 new); analyzer 17 existing infos, no warnings/errors.** Actual lists and five-tab shell rendered on iPhone 17 Pro iOS 26.5 and Android API 34 using isolated fixtures; Android also checked 2×, retry, keyboard/no-results, empty/loading. Three screenshot artifacts retained. Initial native builds encountered disk exhaustion; only ignored Seller Mobile intermediates/old caches were removed. **No API/schema/auth/Buyer/Web/Admin/native-splash change, production mutation, merge or deployment.** Action center, remaining mobile screens and splash remain pending; existing home counts are not yet fixed. Next: bind existing authoritative order/product stats, test filtered routes and mutation/push/resume refresh. Web/Admin implementation stays gated on mobile stabilization.
- **Sep 3, 2026** — **Search & Sales Analytics RELEASED TO PRODUCTION** (release PR **#632**, `develop → main`, merge commit **`9e89478`**, 18 commits / 44 files / +5,812 −297; deploy run **`33756698400`** success; back-merged by fast-forward so `main == develop == 9e89478`). Workstreams **B (search analytics)**, **C (sales analytics)** and **D (CSV hardening)** are complete and live, together with the `DELIVERED ⇒ deliveredAt` prerequisite. **The migration applied itself exactly as designed** — `2026-09-05_search_query_source_intent.sql` ran in the deploy's *expand phase before the rolling swap* (`▶ applying:` … `✓ applied:` … `Auto-apply migrations done: 1 applied, 4 skipped.` then `▶ rolling api …`), was **not** also run by hand, and `_manual_migrations` went 31 → 32 with the file recorded **once** and zero duplicates. **Production verification:** both enums created with the expected values and both columns present `NOT NULL` with `UNKNOWN`/`SUBMIT` defaults; all **60** pre-existing rows read `UNKNOWN/SUBMIT` — the truthful legacy representation, never guessed as web — with row counts preserved and unrelated tables unchanged (products 495, orders 9, categories 348, brands 51, seller_earnings 6). Health `/v1/health{,/ready,/live}` 200 with `database: ok`, all three sites 200, and the swap was proven real rather than assumed: API uptime climbed monotonically 690 → 770 s across a 75 s window, so no restart or crash loop. Buyer search is unchanged (accent/case/whitespace variants agree, autocomplete and popular work, malformed telemetry still returns 200, an undeclared param still 400s). **Telemetry verified end-to-end from the real site:** a search on teka.cd persisted `BUYER_WEB/SUBMIT`, a malformed-source probe persisted `UNKNOWN/SUBMIT`, and a `REFINE` probe wrote **nothing** — two test rows only, both marked `zzrelease0903` (60 → 62). Admin analytics on real production data: **62 searches, 42 unique terms, 66.1 % zero-result** — the signal the initiative exists to surface — and sales showing 6 completed orders / 23 units / `124,795,700` centimes with `deliveredWithoutDate: 0`, by-town and by-seller each summing to 23 units and town revenue matching the summary exactly. Authorization 401 / 403 buyer / 200 admin on every route **including the CSVs**; buyer-web SEO intact (canonical, 2 JSON-LD blocks, full `og:` set, 387-URL sitemap) and seller/admin still `Disallow: /`. **Sentry was NOT checked** — no `sentry-cli` and no auth token available from this environment; Sentry is active and the deploy tagged `SENTRY_RELEASE=9e89478`, so release `9e89478` should be reviewed by someone with access. Follow-ups deliberately preserved rather than folded in: `SearchSynonym` admin CRUD (the recommended next PR), the `forceStatusChange()`/`returnedAt` gap, buyer-web's double `router.push` on Enter, the Next dev-server double-write artifact, the dev DB missing `2026-07-28_review_title.sql`, the pre-existing e2e 401 flake, and three unrelated Dependabot PRs.
- **Sep 3, 2026** — **Search & Sales Analytics INTEGRATED into `develop`** (merge order #625 `33a816b` → #626 `3ac6c7c` → #627 `9075d56` → #629 `3a1c0be` → #630 `db7628d` → #628 `9efe42a`; final `develop` = **9efe42a**, baseline was `307ad93`). Real merge commits throughout, never squashed. The chain was linear so nothing needed rebasing; **#628 was the only conflict** — one `PROGRESS.md` hunk, resolved by **preserving both initiatives' entries** (5 total, none discarded) via a merge of `develop` into its branch, re-audited before committing. Combined footprint **41 files, +5,804 / −269** across api · admin-web · buyer-web · buyer-mobile · docs, with **zero seller-web and zero seller-mobile files** — verified against the baseline diff, not assumed. **Integrated gates on the final SHA:** API **507 unit** and **135 e2e** with **0 failures across 8 consecutive e2e runs** (the documented Auth-401 / Health / Payments flake did not appear), buyer-web 74, buyer-mobile 238 + analyze 6 known infos, seller-mobile 42 + analyze 17 infos (all info-level, exit 0, and zero seller files changed — pre-existing), admin-web build clean, type-check clean across 5 projects. **Contracts re-verified on the merged tree rather than trusted from the individual PRs:** exactly ONE CSV escaping implementation with all three report services on the shared writer (the only surviving `escapeCsv` mentions are comments describing the old defect); `Africa/Lubumbashi` windows with no live `setHours` day-boundary left; sales still defined as `status = 'DELIVERED' AND deletedAt IS NULL` with `deliveredWithoutDate` intact; `@Roles('ADMIN')` on all three report controllers and 401 confirmed live on four routes including both CSVs; buyer search matching unchanged live (`telephone` 32, `telephon` 28, `casserole` 1, autocomplete 5/4/1); and a live 4-request probe proving **REFINE is still dropped, SUGGESTION kept, and a bad source resolves to UNKNOWN** (2 rows from 4 requests). **#628 × #627 compatibility confirmed:** `forceStatusChange` stamps `deliveredAt` only when NULL (never overwrites), while #627 still applies its window only when a date bound is given and still reports the legacy-NULL cohort — so the fix stops *new* NULLs without hiding old ones. Buyer Web SEO untouched (only `search-page.tsx` + a test changed; no robots/sitemap/layout/metadata), admin `robots.ts` still `disallow: '/'`. **One migration added**, `2026-09-05_search_query_source_intent.sql`, **on `auto-apply.list`** — additive and idempotent (3 `pg_type` guards, 4 `ADD COLUMN`, 5 `IF NOT EXISTS`, **0 destructive statements**, no backfill), so a future deploy applies it automatically before the rolling swap and existing rows become the truthful `UNKNOWN`/`SUBMIT`. **Not released to `main`, not deployed, migration not run in production.**
- **Sep 3, 2026** — **Admin search analytics (PR 5 of 5; merged to `develop` as #630 / `db7628d`).** The reporting half of Workstream B, reading the telemetry #629 writes. Lives as a **« Recherches » tab on the existing Rapports page** rather than a disconnected dashboard, with the panel extracted to `components/reports/search-analytics-panel.tsx`. Five admin-only read routes under `v1/admin/reports/search` (listing, summary, trending, breakdown, CSV). **The denominator is stated once and holds everywhere:** every row in `search_queries` is already a meaningful search, because the write path drops REFINE re-fetches, unrecognised intents and sub-2-char terms *before* insert — so `totalSearches` is the population for the zero-result, low-result and suggestion rates alike, and the UI says so in French. `LOW_RESULT_MAX = 3` is a judgement call, so it is returned in the payload and **spelled out in the label** ("entre 1 et 3 produits") instead of hiding behind the word "low". `UNKNOWN` renders as « Non identifiée » and is **never folded into « Site web »**. **Trending** splits the window into two equal halves and ranks by **absolute delta, not percentage** — at this volume a percentage from a small baseline is noise and from a zero baseline undefined, so a term absent from the earlier half is flagged « nouveau »; both halves are counted in one `FILTER` pass, and the UI states honestly that this answers "what is searched now that was not before", not a significant trend. **The zero-result work carries the distinction that matters:** a Diagnostic column driven by `maxResultCount` separates « Jamais de résultat » (catalog gap OR search-quality problem) from « Selon la ville » (town-coverage gap) — the dev data shows why it matters, since `riz local` and `telefone` both read "jamais de résultat" but one is a plausible product gap and the other misspells a term that returns 32 products. Filters reuse `resolveWindow` from PR 2 rather than a second date interpretation; `intent=REFINE` is rejected because it is never persisted. **No index added** — all three query shapes EXPLAINed against the real table (seq scan, 0.05–0.19 ms, planning at or above execution); town labels hydrate with ONE `city.findMany`, and the term listing is exactly two queries. CSV goes through the **shared writer from #625**, and formula protection was proven end to end by typing `=cmd|'/c calc'!A1` into the *buyer* search API and finding it neutralised in the admin export, with `Résultats` still numeric. **`SearchSynonym` investigated and deliberately not built:** it has exactly one consumer (`browse.service.ts:107`) and no admin CRUD anywhere despite the schema calling it admin-editable — deferred as the next focused follow-up, with the new zero-result table already supplying the candidate list. Verified in Chrome: cards, denominator sentence, UNKNOWN caveat, term table, pagination, trending badges, three breakdown tables (Site web 16 / Non identifiée 5 / Application mobile 2), day chart, empty state, error state with a working Réessayer, and a real CSV download. **Not verified: a genuinely narrow viewport** — the browser reported 1728 px and the resize did not take, so responsiveness is claimed only as "no horizontal page overflow at that width, wide table in an overflow-x-auto wrapper". Gates: API **453 → 500 unit**, e2e **118 → 135** (new `reports.e2e-spec.ts` covers all 16 report routes for 401), admin-web build clean, type-check clean across 5 projects. A `Payments (e2e)` failure appeared once in 8 runs — a **fourth shape** of the documented pre-existing flake, in a spec this PR does not touch. Tracker: `docs/search-sales-analytics.md`.
- **Sep 3, 2026** — **Buyer search analytics: `source` + `searchIntent` (PR 4; merged to `develop` as #629 / `3a1c0be`).** Records WHICH buyer surface a search came from and WHY it counted, without touching the search engine. **`X-Teka-Surface` was rejected on two verified grounds:** `main.ts` sets an explicit CORS `allowedHeaders` allowlist, and `surface.util.ts` defaults to `'buyer'` — so buyer-mobile, which sends no such header, would have been labelled `BUYER_WEB` with total confidence. Query parameters instead. **Both params are bounded STRINGS in the DTO, mapped to enums in the service, deliberately not `@IsEnum`:** a strict enum would turn a client typo into a **400 on the search endpoint**, i.e. telemetry breaking a buyer's search. Unknown/missing `source` → `UNKNOWN`, which means "a client predating this parameter", never "probably web". Intent accepts `SUBMIT`/`SUGGESTION`/`REFINE` but stores only the first two; `REFINE` (filter, sort, page, pull-to-refresh) is the duplicate-demand fix, missing defaults to `SUBMIT` so devices already in the field keep contributing, and an unrecognised value is not recorded at all rather than guessed. **New `normalizeSearchTerm()` collapses whitespace on top of `stripAccents`** — `stripAccents` only *trims*, so `"robe  wax"` and `"robe wax"` were two aggregation keys for one demand — kept as a SEPARATE function because `stripAccents` also feeds `expandSynonyms` and the PDP spec de-dupe, where a change would alter search MATCHING. Sub-2-char terms now dropped at write time instead of being stored and filtered on read. **No de-duplication and no identity column:** two buyers searching the same term stay two demand signals, which is the whole requirement, so no session/user id was added — a test asserts the stored row is exactly `{term, termNormalized, resultCount, cityId, source, intent, createdAt}`. **buyer-web has TWO querystring builders that disagree** — the inline one in the `[query]` effect is the real submitted search (`SUBMIT`), `buildQuery()` backs Appliquer/Effacer/sort/load-more (`REFINE`); tagging one only would have missed every first-load search or counted every filter click. **buyer-mobile's `searchIntent` is excluded from `BrowseProductsParams`' `==`/`hashCode`** because that class is a Riverpod family key whose notifier fetches in its constructor — including it would mint a new notifier and an extra request per interaction and leak the old ones. Migration `2026-09-05_search_query_source_intent.sql` (guarded enums, `ADD COLUMN IF NOT EXISTS`, O(1) defaults, **no backfill**) is on `auto-apply.list`; applied twice to dev for idempotency, never to production. **No index added** — the admin query was EXPLAINed against the real table (seq scan, 0.29 ms execution vs 2.96 ms planning). **Deployment order matters:** `forbidNonWhitelisted` makes an undeclared param a hard 400, so the API must reach production before any client sends these. Verified against the dev DB with 13 representative requests and the resulting rows inspected: `"Téléphone"`/`"TELEPHONE"` → one key `telephone` (32 results each), `"  robe   wax  "` → `robe wax`, zero-result, town scoping, legacy client → `UNKNOWN/SUBMIT`, unknown source → `UNKNOWN`, unknown intent → no row, 1-char → no row, `REFINE` → no row, undeclared param → 400 — and **every search returned 200 even with bad analytics values**. Matching measured unchanged (`telephone` 32, `telephon` 28, `caserole` 1); typo tolerance is partial (`telefone` → 0) but that is the **pre-existing** trigram threshold, and the diff removes only the old `logSearch` body, its call site and one import. **buyer-web verified in Chrome against a PRODUCTION build**, which persists exactly ONE row per search (`chemise coton` n=2, `farine manioc` n=0); the dev server writes two ~1.7s apart, a **Next dev artifact** identified by using the production build as a control. **buyer-mobile was NOT run on an emulator** — covered by `flutter test` (238) and analyze only. Also found and deliberately NOT fixed: `search-autocomplete.tsx` calls `goToSearch()` without the event in one Enter branch, so `preventDefault` no-ops and the native form submit fires a second `router.push`; a one-line fix was written, **measured to change nothing**, and reverted as out of scope. Gates: API **423 → 453 unit**, 118 e2e, buyer-web **68 → 74**, buyer-mobile **231 → 238**, seller-mobile 42, analyze 6 known infos, type-check clean. Tracker: `docs/search-sales-analytics.md`.
- **Sep 3, 2026** — **Order lifecycle: `status = DELIVERED ⇒ deliveredAt IS NOT NULL` for new writes** (prerequisite; merged to `develop` as #628 / `9efe42a`). Surfaced while building sales analytics: a delivered order could carry a NULL `deliveredAt`, which made it invisible to every date-windowed report, to payout eligibility (`deliveredAt + 2d ≤ now`) and to the delivered-today count — and un-returnable by the buyer, since `isWithinReturnWindow(null)` is `false`. **Audit of every path that can write DELIVERED found exactly two in production code** plus the seed: `markDelivered()` (correct — stamps the date, flips COD `paymentStatus`, increments `unitsSold`, creates the earning, notifies) and **`forceStatusChange()`**, which wrote only `{ status }`. The latter is reachable from the admin UI — the force-status modal lists « Livrées » among all statuses. Seller transitions (CONFIRMED / PROCESSING / READY_FOR_TEKA_PICKUP), buyer cancel, `approveReturn` and the admin `applyTransition` helper (RECEIVED_AT_TEKA / OUT_FOR_DELIVERY) never target DELIVERED; no SQL migration or script writes it either. **Three distinct causes, not one:** (1) `deliveredAt` was added by `2026-06-29_managed-order-workflow.sql` as a nullable column with **no backfill**, so every pre-existing delivery legitimately has NULL; (2) `forceStatusChange()` never stamped it, so new NULLs remained possible; (3) `prisma/seed.ts` never set it at all. **The fix deliberately did NOT give `forceStatusChange` the delivery side effects** — its purpose is administrative repair, and replaying earnings/stock/payment/notifications would double-book an order that already went through the real flow. It now fills `deliveredAt` **only when NULL**, so a `DELIVERED → X → DELIVERED` round trip keeps the original timestamp and never restarts a return window; a test asserts no earning, stock, payment or notification is triggered. Seed: order 1's lifecycle timestamps were hoisted into one array so the row's `deliveredAt` and its DELIVERED status log are the **same instant by construction** (`2026-02-16T15:30Z`, after `createdAt` `2026-02-15T09:30Z`), plus a scoped self-heal for databases seeded before the fix (`upsert`'s `update: {}` never repairs an existing row) and a post-seed invariant assertion that fails loudly on a future regression — all unreachable in production, which returns before any order is seeded. **Production was assessed READ-ONLY: 9 orders, 6 DELIVERED, 0 with NULL `deliveredAt`** — so no backfill and no production migration are needed. Legacy rows elsewhere are left alone and **no timestamp is invented**; where reconstruction is ever wanted the defensible evidence is the `OrderStatusLog` row recording the transition into DELIVERED, which both dev rows do carry. Verified by running the seed against dev: `TK-20260215-A1B2` went NULL → `2026-02-16T15:30Z` with row counts unchanged (orders 8, items 12, products 467, users 17, categories 348, earnings 2). Gates: API **313 → 320 unit**, 118 e2e, type-check clean across 5 projects. **Two pre-existing issues found, not fixed here:** the dev database is missing `2026-07-28_review_title.sql` (it IS in `auto-apply.list`), which aborts a full dev seed at Phase 6 with `reviews.title does not exist`; and `forceStatusChange` has the identical gap for `returnedAt`, deliberately left out of this narrowly-scoped PR. Detail: `docs/order-workflow.md` → "The `deliveredAt` invariant".
- **Sep 3, 2026** — **Sales analytics breakdown (PR 3; merged to `develop` as #627 / `9075d56`).** Five dimensions — product, category, seller, town, day — behind `GET /v1/admin/reports/sales/breakdown?by=…`, plus a `summary` endpoint and a French CSV, and an « Analyse des ventes » tab on the admin Rapports page. **The definition of a sale was derived from the code, not from the shape of the enum:** `status = 'DELIVERED' AND deletedAt IS NULL`, on the **`deliveredAt`** axis, because `AdminOrdersService.markDelivered()` is the single moment that stamps the date, flips a COD order's `paymentStatus` to COMPLETED (the agent collects cash at the door), increments `unitsSold` and creates the earning — nothing earlier does any of that, so CONFIRMED / PROCESSING / READY_FOR_TEKA_PICKUP / RECEIVED_AT_TEKA / OUT_FOR_DELIVERY are not sales. `RETURNED` needs **no** explicit exclusion, since `ReturnsService.approveReturn()` moves the order *off* DELIVERED, restocks and reverses the earning; a test pins that so nobody later adds a redundant `NOT IN`. Returns and cancellations are still reported as separate counters so the headline cannot be misread. **The finding that shaped the design: `deliveredAt` is not always set.** `forceStatusChange()` writes only `{ status }`, and `prisma/seed.ts` never sets `deliveredAt` at all (zero occurrences) — so **both** dev DELIVERED orders carry NULL, and a strictly windowed query returns *zero sales* (`EXPLAIN` shows `Rows Removed by Filter: 8, rows=0`). Rather than silently under-report, the window is applied **only when a date bound is supplied** and `getSummary()` exposes `deliveredWithoutDate`, which the UI renders as « ⚠ 2 commandes livrées sans date de livraison — exclue(s) de ce filtre par période. » (Follow-up: `seed.ts` should stamp it; the same gap also hides those orders from payout eligibility.) Revenue is `SUM(order_items."totalCDF")` — the charged price, discount already applied — which equals `Order.subtotalCDF` and therefore **excludes the delivery fee** deliberately, since a fee belongs to an order and cannot be attributed to a product or category; `discountCDF` reconstructs the buyer's saving from the `listUnitPriceCDF` snapshot. **The planned `@@index([status, deliveredAt])` was measured and then DECLINED:** `orders` holds 8 rows / 288 kB, Postgres seq-scans in 0.123 ms with planning time ~3× execution, so the index would never be chosen and would only add write cost — revisit around low-tens-of-thousands of orders. **PR 3 therefore carries no migration and no schema change.** All five dimensions aggregate over `order_items ⋈ orders` in one statement each (raw `Prisma.sql`, because `groupBy` cannot express the join, `COUNT(DISTINCT)`, the CASE-guarded discount sum, or town's accent-folded key distinct from its label); product is labelled from the `productTitle` **snapshot**, town from the delivery **snapshot** with the relation only as fallback, category rolled to root with the existing `rootIdOf` walk. **Validation used an independent JS oracle that performs no SQL aggregation**, run against purpose-built fixtures inside an interactive transaction that is **always rolled back** (dev DB verified unchanged afterwards: 8 orders / 12 items, 0 fixture rows) — because real dev data has no discounts, one seller, one town and an empty `day` dimension. Fixtures covered a CAT day boundary, a discounted multi-item order, a multi-seller checkout sharing one `checkoutGroupId`, an accent/case town variant, and four rows that must be excluded (CANCELLED, RETURNED, OUT_FOR_DELIVERY, soft-deleted DELIVERED) carrying 99+50+77+33 units so any leak would be obvious. All six scenarios × five dimensions matched; the 22:30Z delivery correctly bucketed to **1 July** CAT, towns folded to `Lubumbashi(8) LIKASI(6) Kolwezi(3)`, discount 600, units 17 not 276. Over HTTP: no route collision with the existing `sales` ledger (all five paths mapped distinctly, ledger unchanged), 401/403/403/200 on summary, breakdown **and** CSV, French 400s for a bad dimension/inverted range/over-limit/undeclared param, and CSV with French headers, intact accents, FC conversion and no pagination. Chrome: summary cards, the « hors ventes » line, totals reconciling to the headline, and the gap warning flipping between « incluse(s) ici » and « exclue(s) de ce filtre » — no console errors. Gates: API **389 → 419 unit**, 118 e2e, type-check clean across 5 projects. Tracker: `docs/search-sales-analytics.md`.
- **Sep 3, 2026** — **Search & Sales Analytics (Workstreams B, C, D) started; PRs 1–2 merged as #625 / `33a816b` and #626 / `3ac6c7c`.** The Seller Catalog Taxonomy initiative completed **Workstream A only** — its tracker says so outright at `docs/seller-catalog-taxonomy.md:6` — and the code agreed: no admin surface had ever read `SearchQuery` (its only consumer is the *public* `/v1/browse/search/popular`), no sales aggregation by product/category/town existed anywhere, and `getSalesReport` applied **no status filter at all**, so cancelled orders counted as sales. Search itself is mature (French FTS over an unaccented generated `search_vector`, pg_trgm `word_similarity`, admin-editable synonyms, town scoping) and is deliberately untouched. **First correction: the test baseline in the docs is wrong.** Measured on `develop`: **API 313 unit + 118 e2e**, not the 271 in CLAUDE.md nor the 287 in the taxonomy tracker. **PR 1 (#625) — shared CSV writer.** `escapeCsv` did RFC-4180 quoting only, so a cell starting with `=`, `+`, `-`, `@`, TAB or CR was emitted verbatim — and seller `businessName`, buyer/seller names and admin-typed `rejectionReason` all reach these files; a seller named `=cmd|'/c calc'!A1` landed unescaped in the finance export, with **no CSV tests anywhere in the repo**. The load-bearing decision was to split the guard **by cell kind** rather than expose one generic escape: applying a formula prefix to a *number* would prefix every negative amount with `'`, Excel would read the column as text, and `SUM()` in the finance sheet would silently break — so `csvText` guards while `csvNumber`/`csvMoneyFC` do not and assert their own output is numeric. Formula detection had to check **raw and trimmed**: trimming alone cannot replace the raw check because `\s` matches TAB and CR, which are themselves formula-starting characters (the first draft had only the trimmed check; the TAB/CR tests caught it). `csvMoneyFC` deliberately is **not** `formatFC` from `@teka/shared` — that emits `"52.957 FC"`, which every Excel locale reads as text. Columns, headers and values are unchanged, so existing downloads are byte-identical apart from neutralised payloads; one improvement falls out, a `+243…` payout phone is no longer evaluated by Excel into `243970000001`. **PR 2 — windows, bounds, pagination, N+1.** `buildDateFilter` called `endDate.setHours(23,59,59,999)`: three bugs in one line — it read the **server's** timezone, left a last-millisecond hole, and ignored that Teka operates in **Africa/Lubumbashi (CAT, UTC+2, no DST)**, so a UTC bucket puts every 22:00–00:00 local delivery on the previous day. Replaced by a half-open `[gte, lt)` window anchored to CAT, with `dateTo` still inclusive to the caller; `AdminStatsService` is **deliberately not** retrofitted because that would change dashboard numbers already on screen. `ReportQueryDto` gained `dateFrom <= dateTo`, a 366-day cap and `page`/`limit` (max 200) — validated in a **unit** spec because Nest runs **guards before pipes**, so an unauthenticated request to an `@Roles('ADMIN')` route returns 401 and never reaches validation. All four reports now return the `{ data, pagination }` envelope `AdminOrdersService` already used, while **CSV exports stay unpaginated** (same collector, `CSV_MAX_ROWS + 1`). `getSellerPerformanceReport` went from **four queries per seller** over an unbounded seller list to **four queries total**; `user.findMany` gained an explicit `orderBy` because paging an unordered `findMany` can repeat or skip rows. The admin page changed in the same PR — it derived columns from `Array.isArray(res.data)` → `Object.keys(rows[0])`, so the new envelope would have silently blanked the table. **Verified against a local API on the dev DB** (8 orders across every status, 10 sellers) with a dev-signed admin JWT: envelope and column keys on all four tabs, disjoint pages, inclusive `dateTo` (8 / 7 / exactly 1 for a single day), 400 + French message on every bound, 401/403/403/200 on **both** JSON and CSV routes, CSV ignoring `page`/`limit`, and — the strongest check — the **old per-seller N+1 logic re-run as an independent oracle producing byte-identical aggregates**. In Chrome: table renders, footer correct, paging walked 1→3 with correct disabled states under a temporary `PAGE_SIZE=3` (reverted), error state renders with a retry, no console errors. **Not verified:** nothing run against production; Sentry not checked. **Left open deliberately:** `HttpExceptionFilter` hardcodes `field: 'unknown'` on every validation error (pre-existing, affects every DTO); `AdminStatsService` still buckets in UTC; `getSalesReport` stays an order *ledger* on `createdAt`, with delivered-revenue semantics arriving in PR 3. Tracker: `docs/search-sales-analytics.md`.
- **Sep 2, 2026** — **Manual-migration tracking + historical backfill** (PRs #621 tracking, #622 backfill, #623 release → `main` merge `1727c97`, deploy `33678093321`; backfill executed as run `33679262957`). `Apply prod migration` had never recorded what it applied, so `_manual_migrations` tracked only the four auto-applied files while 26 migrations applied through the workflow since 2026-05-21 were invisible. Two changes closed it. **(1)** The workflow now writes the dispatched filename into the same `_manual_migrations` table `apply-auto.sh` uses, `ON CONFLICT (filename) DO NOTHING`, **only after the file succeeds** — and gained **`ON_ERROR_STOP=1`**, which it had been missing: psql exits 0 on SQL errors without it, so a half-applied migration used to print "✓ migration applied" and, once tracking existed, would have been *falsely recorded as successful*. That fix is what makes the record trustworthy. **(2)** `2026-09-04_backfill_manual_migrations.sql` restored the 26 missing rows. Every entry was derived from GitHub Actions rather than memory: run conclusion `success`, filename parsed out of "▶ applying …" in that run's own log, applied marker present, absent from the table beforehand, run id kept inline. `applied_at` is the **run timestamp**, never `now()` and never the date in the filename — re-verification caught **two** files whose name disagrees with when they ran (`2026-07-23_prune-non-leaf-attributes.sql` ran 2026-07-24; `2026-09-03_remediate_legacy_category_products.sql` ran 2026-09-02). **Nine of the 39 files were deliberately excluded** for lack of execution evidence — eight predate the workflow and one is documented dev-only — because absence from the table is not evidence a file was applied, and a false "applied" row is the one dangerous direction: a future auto-apply run would then skip a migration that never ran. Sequencing mattered and was enforced: the file lives inside the api image and the workflow reads it from the running container, so the release had to ship *before* the backfill could run at all. Result verified directly against the database: **4 → 31 rows** (4 + 26 + 1 self-record), all 26 present, self-record once, excluded 9 absent, no duplicates, corrected timestamps intact, and application tables byte-identical (products 495, specs 337, brand_categories 314, categories 348, brands 51). Known asymmetry, documented rather than silently assumed: the skip is one-directional — auto-apply honours the table, the manual workflow always re-runs what it is given.
- **Sep 2, 2026** — **Seller Catalog Taxonomy RELEASED to production** (release PR #618, `develop → main`, merge commit `ce76525`; deploy run `33667591201` all 5 jobs green; back-merged so `main == develop`). Carried Phases 1 (#615), 2a (#616), 2b (#617) and the seller/admin consistency fix (#619) — 16 commits, 14 files, +1,632 −23, confined to the API, seller-web and docs. **Neither manual migration ran during the deploy**: the expand phase skipped its four already-applied `auto-apply.list` entries, and the two taxonomy files are deliberately unlisted. The new code was proven live *behaviourally* rather than from CI — « Mode > Homme » attributes returned `[]` where production had returned `Type, Type de peau`. **Then both migrations were applied by hand via `Apply prod migration`, in order, each verified before the next.** (1) `2026-09-02_prune_invalid_brand_category_links.sql` (run `33668468256`, `INSERT 0 121 / DELETE 121`): live-brand links **433 → 314**, leaf links unchanged at 314, intermediate 66 → 0, soft-deleted 53 → 0, 49 brands with none deleted, and **leaf coverage identical in every category** — confirming the removed links were unreachable. Production dropdowns verified, including the deliberately kept `Lacoste → Parfums Homme`. (2) `2026-09-03_remediate_legacy_category_products.sql` (run `33668783372`): `h0d799` → « Mode > Homme > Chemises » rendering Taille/Couleur/Matière exactly once each while **all 6 rows stay stored** (3 new + 3 original Électroménager preserved); `vnkqce` → « … > Lessive » with its legacy `Type = Savon de lessive` preserved and visible and no mapping invented; `rb7t4r` untouched, `updatedAt` still 2026-07-27. **The most consequential finding of the whole initiative was a rule that was rejected rather than shipped:** scoping PDP characteristics to the product's own category would have blanked **7 live products** (18 foreign specs across 9) — a 10 kg bag of rice losing « Poids », an Android phone losing RAM — because the pre-3-level taxonomy legitimately attached attributes at subcategory level. The shipped rule preserves foreign rows and de-duplicates by name, preferring the current category, normalised with `stripAccents()`, with deterministic precedence (own-category → `sortOrder` → `attributeId`); it lives in one shared helper used by the buyer, seller and admin detail endpoints. Gates: API **313 unit + 118 e2e**, type-check clean, buyer-mobile 231, seller-mobile 42, buyer-web 68. **Not verified:** Sentry (no CLI/token available) and seller-mobile on-device (`osascript -1719`). **Left open by decision:** `rb7t4r` blocked pending a restricted-product/alcohol policy (Teka has no such policy, no age verification, no alcohol leaf); the 117 unreferenced legacy attributes; `vnkqce`'s visible legacy `Type` and `Volume`-vs-`Poids` on Lessive; brand expansion; and Search/Sales analytics. Tracker: `docs/seller-catalog-taxonomy.md`.
- **Sep 2, 2026** — **Seller Catalog Taxonomy, Phase 2a: BrandCategory cleanup** (PR #616 → `develop`, merge commit `7fc0ddb`; **not applied to production**). A read-only production audit (`prisma/scripts/audit-prod-taxonomy.ts`, findMany/groupBy/count only) quantified the same June-2026 drift on the brand side: of 435 `BrandCategory` rows, **314 point at a live leaf, 68 at an intermediate node, 53 at a soft-deleted category, 0 at a missing one**. Both clients call `GET /v1/brands?categoryId=<leaf>` with an exact link match, so the 121 non-leaf links could never be shown to anyone. Two corrections came out of enumerating rather than trusting the counts: the audit's "66 intermediate" excluded 2 links held by two soft-deleted placeholder brands (real total 68), and the "8 semantically wrong mappings" turned out to be 18 individual links of which **16 already point at an intermediate node** — so the migration names no brand at all, "not a live leaf" being the entire rule. The other 2 (`Lacoste → Beauté & Santé > Parfums Homme/Femme`) are leaf links and are **correct** — Lacoste is a genuine fragrance brand — so the earlier heuristic that flagged them was wrong and they are deliberately kept. Shipped as `manual/2026-09-02_prune_invalid_brand_category_links.sql`: archives every removed row into `brand_categories_archive_20260902` with its reason before deleting, so it is reversible; idempotent (`ON CONFLICT DO NOTHING` + a re-evaluated predicate); **deliberately not in `auto-apply.list`** because it deletes rows. Applied twice to the development database — 435 → 314, second run a no-op — with **leaf-level brand coverage unchanged per category**, confirming the removed links were unreachable. Runtime spot-checks after the prune: Chemises → Autre/Nike/Lacoste, Soins du corps → Autre/Nivea, Parfums Homme → Autre/Lacoste, and `Mode > Homme` (intermediate) → 0 brands. No `Brand` row deleted, "Autre" keeps all 145 live-leaf links, and the `brandId = NULL` no-brand path is untouched. The same audit measured production `SearchQuery` at **53 rows over ~70 days, 33 of them zero-result (62%)** — too small to justify retention/aggregation infrastructure, so that work stays on hold in favour of understanding the zero-result rate. Gates: API 287 unit, type-check clean; no application code changed.
- **Sep 2, 2026** — **Seller Catalog Taxonomy, Phase 1: leaf-category invariant** (PR #615 → `develop`, merge commit `9ecac4b`; **not released**). A men's shirt rendered « Type de peau » under *Caractéristiques du produit*. Root-caused against live production through the public browse API, with **no** database access: the product (`1a07d699…`, `h0d799`) is attached to `13000000-…-000000000501` = **« Mode > Homme »**, an *intermediate* node — 2-level breadcrumb, and all eight leaf children hold 0 products. That node still carries legacy pre-refactor rows `Type` and `Type de peau`, so `getCategoryAttributes` served them faithfully. The 3-level taxonomy attaches attributes to the **leaf** and assumed products would too, but **enforced it nowhere**. Not the July 2026 parent-chain merge bug, which is intact. The same drift is visible on « Mode > Enfants » (`Type de cheveux`) and « Électroménager > Cuisine » (`Taille`/`Couleur`/`Matière`) — and that last one owns the very attribute rows this shirt's three specifications point at, so its real values (`M`/`Bleu`/`Coton`) render in **no** seller form. **A second, worse defect surfaced while tracing it:** `update()` deleted *every* `ProductSpecification` whenever `specifications` was present, so any caller omitting those invisible rows destroyed them — a single quantity edit was enough. Shipped: `assertLeafCategory` (on `create()` always, on `update()` **only when `categoryId` actually changes**, so legacy products stay editable and a hard 400 names the category **without guessing a child** — « Homme » holds shirts, trousers and shoes alike); `getCategoryAttributes` returns `[]` for an intermediate node; `update()` clears only ids the API would serve for the target category ∪ those the payload names, preserving foreign/legacy rows; seller-web's `CategoryCombobox` offers **leaves only** (it had flattened every node into the option list while seller-mobile already restricted to leaves — the divergence that most plausibly created the bad row), while still resolving a legacy category's *label* so the field is never blank, plus an in-component warning « Ce produit utilise une ancienne catégorie… ». seller-mobile needed no change. **Browser verification caught a regression I introduced** — filtering branches out of the option list also removed them from the list `selected` resolved against, blanking the field on legacy products. Gates: **API 271 → 287 unit (16 added) + 118 e2e**, `pnpm type-check` clean. Seller Web verified end to end in Chrome against a faithful dev-DB reproduction (leaf-only dropdown, legacy warning, Fashion/Beauty/Electronics attribute sets, category-change refresh, and a quantity edit proven to preserve all three hidden specs). **Seller Mobile built, installed and launched on iPhone 17 Pro / iOS 26.5 with 42 tests green, but its interactive create/edit flows were NOT verified** — no `idb`/`cliclick` and `osascript` returns `-1719` (assistive access denied). **No production data changed, no migration written.** Production holds ≥3 ACTIVE products on intermediate nodes (« Mode > Homme », « Supermarché > Boissons », « Supermarché > Entretien Maison »); none is auto-repairable. Tracker: `docs/seller-catalog-taxonomy.md`.
- **Sep 2, 2026** — **Buyer Mobile PDP targeted layout adjustment** (complete on `develop`; not released). Removed the monetary-savings text and its padding, retaining current/original prices and the percentage badge. Moved the single existing rating/favorite/share row below stock status; its implementation is unchanged except for a widget-test key. No shared components, business logic, API, Buyer Web/SEO, or native splash changes. Added six widget cases covering all discount/stock combinations, exact spacing, favorite state/resolved ID, and native share payload/origin. **231 tests pass**, analyzer gate passes with six known info-level deprecations, both changed Dart files formatter-clean. Rendered real PDP widgets with isolated fixtures on iPhone 16 Pro and iPhone SE, including 2× text, long titles, scrolling, SafeArea, favorite toggles, review navigation, and the native share sheet; no Flutter errors and no production writes. Follow-up evidence in `docs/buyer-mobile-pdp-splash-refinement.md`.
- **Sep 1, 2026** — **Buyer Mobile PDP + native splash refinement** (complete on `develop`; not released). Removed the mobile-only breadcrumb and tightened the PDP around a shorter uncropped gallery, overlaid image counter, responsive rating/stock/seller/review layouts, scrollable COD/Teka-delivery assurances, a compact cart-only sticky action, and a content-shaped reduced-motion-aware skeleton. The existing design system, price/stock rules, resolved-UUID behavior, routes, analytics, and business flows remain intact. The native splash now uses a density-preserving padded wordmark on iOS/Android pre-12 and a white T on Modern Ruby for Android 12+; API 33+ explicitly prefers the icon so the OS cannot substitute an empty splash. Visual QA covered live PDP states on iPhone 16 Pro, iPhone SE, and Android 14, plus fresh-install cold-launch recordings on standard/small iPhones and a held real system splash on Android. Final review caught and removed generated status-bar flags. Gates: **225 Buyer Mobile tests**, changed Dart files formatter-clean, analyzer gate passing with six known info-only deprecations. No API, database, Buyer Web, seller/admin, or SEO changes. Tracker: `docs/buyer-mobile-pdp-splash-refinement.md`.
- **Sep 1, 2026** — **Buyer cart synchronization + single editable address** (#603–#608 into `develop`, #609 docs, released via #610 `develop → main`; **live in production**). Two reported buyer defects plus one found while tracing them, and **none of the three was where it appeared to be**. **(1) Stale cart after ordering (buyer-mobile).** The ordered products stayed in the cart until a manual pull-to-refresh. Not a backend bug: `checkout.service.ts` already deletes every `CartItem` inside the order transaction, so the DB was correct the moment the 201 returned. The Flutter client was never told — `cartProvider` is a **non-`autoDispose`** StateNotifier that fetches once at construction, `placeOrder()` never touched it, and `POST /v1/checkout` returns no cart (unlike every cart mutation, which returns the serialized cart). Worse, the cart is persisted to SharedPreferences with a **30-day TTL**, so the stale items survived an app relaunch, not merely a screen change. buyer-web never had the bug — it does `await clearCart()` before `router.push`. Fixed with `CartNotifier.onOrderPlaced()` (empty state → evict cache → authoritative refetch), deliberately **not** `clearCart()`, which issues a redundant `DELETE /v1/cart` and *restores the items on failure* — it would have resurrected products the buyer had just bought. `CheckoutNotifier` took a `Ref` and runs the transition before flipping to `success`, so the success screen and tab badge are correct on first paint; owning it in state rather than a widget's `ref.listen` means a disposing screen cannot skip it. **(2) No way to edit a delivery address**, and the two clients were **backwards from expectation**: buyer-mobile had a full address book (list / set-default / delete) but **no add and no edit**, its empty state telling the buyer to wait until checkout; buyer-web had **no address page at all**, only create-and-pick inside checkout, so buyers accumulated addresses forever with no way to remove one. Neither client had ever called `PATCH /v1/addresses/:id`. `POST /v1/addresses` became an **upsert** guarded by a transaction that row-locks the owner, enforced server-side so the web cannot bypass it; buyers only, since a partial unique index on `addresses("userId")` cannot be scoped by `users.role` and would have silently capped sellers too. Both clients now show one address with « Modifier », or « Ajouter mon adresse » — a deliberate **removal of shipped mobile functionality** (set-default, delete, multi-card list). **(3) Found in passing: address saves 400'd.** Both clients posted `details`/`phone` while the DTO accepts `reference`/`recipientPhone`; under `forbidNonWhitelisted` that is a hard 400, masked because both clients omit blank fields — so an address only failed to save once the buyer actually filled in the landmark or recipient phone. The same mismatch on the read side was silent data loss: the landmark never rendered on mobile, and the recipient phone rendered blank on every buyer-web checkout address and order detail. **The load-bearing decision was the order snapshot.** `Order.deliveryAddressId` was a bare FK with no snapshot, so making the address editable would have retroactively rewritten the delivery address of every past order pointing at that row. Production was measured before any code was written: **1 buyer of 1 held 2 active addresses, and 2 addresses were each referenced by more than one order**. Orders now carry their own `delivery*` columns written at checkout — the same thing `OrderItem` already did for `productTitle`/`unitPriceCDF`; the address had been the odd one out. All six read paths funnel through `resolveDeliveryAddress()`, which serves the snapshot under the unchanged `deliveryAddress` key and falls back to the relation for pre-backfill rows, so the API contract did not move. Shipped as two migrations with deliberately different paths: `2026-09-01_order_delivery_address_snapshot.sql` (additive, idempotent, **auto-applied** pre-swap) and `2026-09-01_archive_duplicate_buyer_addresses.sql` (**manual**, post-deploy, BUYER-only, soft-delete, reversible). In production the archive step soft-deleted an address that **6 of the 9 orders referenced** — they kept reading `Lubumbashi` from their snapshots while the buyer's only address became `Kolwezi`, which is precisely the case the snapshot existed for; 0 orders lost a snapshot, 0 FKs broke. **Verified live** rather than argued: legacy `details` → 400, a second POST upserted the same row id, cart `totalItems: 0` after checkout, and an order kept its address after the buyer's was edited across towns. Two defects were caught **at review, not by the suites** — a `unused_import` warning that failed CI (analyze had been read by total count rather than by severity; CI runs `--no-fatal-infos`, so infos pass and warnings fail) and buyer-web failing to re-price delivery after an edit, because the quote effect keyed on an address id that an edit does not change. Gates: **API 245→271 unit + 118 e2e · buyer-mobile 202→218 · buyer-web 63→68 · seller-mobile 42**; 45 tests added, `addresses.service.spec.ts` being the first the service ever had. **Not verified:** the on-device cart sequence — no UI automation was available (`osascript` denied assistive access, no `idb`/`cliclick`, no `integration_test/` harness), so it rests on a `ProviderContainer` test proven to fail without the wiring. **Left open:** the *development* DB carries drift where `pnpm db:push` would DROP `products.search_vector` and the trigram search indexes — deliberately untouched, production unaffected.
- **Aug 29, 2026** — **App Store 5.1.2(i) rejection + Android App Links never verifying** (branch `fix/testflight-internal-distribution-mechanism`; code complete, 2 operator steps outstanding). Two unrelated reports, two distinct root causes, **neither a logic defect**. **(1) Android deep links opened buyer-web instead of the app, while iOS worked.** Root cause: `apps/buyer-web/public/.well-known/assetlinks.json` shipped — and was *live in production* — carrying the literal placeholder `REPLACE_WITH_PLAY_APP_SIGNING_SHA256_FINGERPRINT`, an operator step from the 2026-06-28 deep-linking initiative that was never completed. Android silently fails App Links domain verification against an unparseable fingerprint and falls back to the browser; **Android App Links therefore never worked in production**. iOS was unaffected because Universal Links match on team+bundle id with no cert fingerprint — exactly why the symptom looked Android-specific. `DeepLinkParser`/`DeepLinkController`/manifest/AASA were all already correct (16 parser tests green). Fix: real **upload-key** SHA-256 (`08:AC:B5:…:21:F4`, extracted via keytool) in `assetlinks.json`; **the Play App Signing fingerprint still must be pasted in** for Play Store installs (Play Console → App integrity), which is how the reported device installed it. Documented the three signing channels and why CI `Build mobile APK` artifacts (ephemeral debug keystore, new fingerprint per run) can *never* verify. **Deliberately rejected:** adding `www.teka.cd` to the `autoVerify` intent-filter — nginx 301s `www`→apex, Android's verifier doesn't follow redirects, and on Android 12+ one failing host fails them all, so it would have broken the working apex path; `www` is moot anyway since `productWebUrl` + buyer-web canonicals emit apex. **(2) Apple rejected buyer-mobile 0.1.4 under 5.1.2(i)** for declaring Crash Data / Other Diagnostic Data / Device ID as "used to track you" with no ATT prompt. Audit found **no tracking**: zero `AdSupport`/IDFA/`ATTrackingManager` references in app source (all matches were vendored SDK checkouts under gitignored `build/`), no ad network, no attribution SDK, no data broker; Sentry is errors-only (`tracesSampleRate=0`, phones scrubbed) and PostHog is first-party (`distinctId`=our `user.id`, `role` only). So the **labels were wrong, not the app** — adding ATT would have been the wrong fix (Apple rejects prompts for tracking that doesn't happen). Shipped `ios/Runner/PrivacyInfo.xcprivacy` for **both** mobile apps (`NSPrivacyTracking=false`, empty `NSPrivacyTrackingDomains`, every type `Tracking=false`), wired into the Runner targets' Resources phase by the new idempotent `scripts/ios-add-privacy-manifest.rb` (a manifest not referenced by the project is silently not shipped); pbxproj diffs verified semantically identical via xcodeproj (9 configs, build settings byte-equal, `CODE_SIGN_STYLE` untouched, only the resource added). **Regression guards:** `apps/buyer-web/src/lib/deep-link-association.test.ts` (validates fingerprint format, rejects any placeholder, asserts AASA excludes auth/checkout and keeps the catch-all last) — **verified to fail on the reintroduced original bug**, not just to pass; plus `test/core/privacy_manifest_test.dart` in both Flutter apps. Docs: `docs/deep-linking.md` (root cause + fingerprint-per-channel table + the www trap) and `docs/mobile-release.md` (new "App privacy" section: full label matrix + rejection history). **No API/DB/migration; no web UI or SEO change.** Gates: buyer-web vitest 10 files/63 · buyer-mobile 202 · seller-mobile 42 · `pnpm type-check` clean · `flutter analyze` 0 issues in `lib/`+`test/` both apps.

- **Jul 30, 2026** — **New-only catalog · internal stock · review titles + editing · product-detail polish** (PRs #580–#588 → develop; **#586, #587, #588 still open**). Seven priorities. Trackers: `docs/new-only-stock-review-product-detail-fixes.md` (umbrella), `docs/product-condition-deprecation.md`, `docs/review-title-and-editing.md`. **(#580, found not written)** local `develop` carried unpushed Codex work that had **rewritten the very files** priorities 1/2/4/6 target, so it was raised as its own PR and reviewed rather than built on — the review found it had reintroduced the Priority 5 regression and that several guarantees from the Jul 26 initiative were protected **only by comments**; every one of those is now a test. **(#581) PDP cart icon opened a red error screen** — `commerce_header.dart` used `context.push('/cart')`, but `/cart` is a `StatefulShellBranch` route (a bottom tab), and imperatively pushing a branch route throws. Now `context.go('/cart')`. **(#582 + #585) Public New/Used condition removed** — **deprecated, not dropped**: the `condition` column, the `ProductCondition` enum and the API field all stay (every product is `NEW`; a destructive migration for a presentation change is unjustified), and JSON-LD keeps `itemCondition: NewCondition` because hiding a badge from humans is no reason to stop telling crawlers what the product is. Buyer-facing badge, the État facet and the seller-form selector are gone on web + both mobile apps. **(#583 + #585) Exact stock quantities are now internal** — buyers see `En stock` / `Stock limité` / `Rupture de stock` and never a number; `quantity` still caps the quantity stepper and drives every server-side check. Four leaks were found, including the stepper's cap message, which interpolated the remaining count (now a `const Text`). The low-stock threshold was **audited, not invented** — the pre-existing rule was ≤5, but mobile used `< 5` while buyer-web used `<= 5`, so a product with exactly 5 left disagreed across platforms; centralised as `LOW_STOCK_THRESHOLD` (`packages/shared`) + `kLowStockThreshold` (Dart mirror, since a TS constant cannot reach Flutter), tagged `TODO(stock-status-server-owned)` for the real fix of an API-derived `stockStatus`. **(#584) Review titles + editing** — new nullable `Review.title` (additive idempotent `manual/2026-07-28_review_title.sql`, in `auto-apply.list`) and `PATCH /v1/reviews/:id` that **updates in place**, never inserts. Deliberate asymmetry: title **optional on create** (mobile builds already on buyers' phones know nothing about the field; a 400 would be unfixable without an app update) and **required on edit** (an edit can only come from a title-capable client) — the compatibility window, with a documented later tightening step. `UpdateReviewDto` is **not** `PartialType(CreateReviewDto)`: `productId`/`orderId` are omitted so an edit cannot re-point a review at another product and bypass the delivered-purchase check. Eligibility is not re-evaluated (`canReview()` reports `ALREADY_REVIEWED` once a review exists, so re-running it would reject every edit); moderation status is **untouched**, because Teka has no pre-publish queue and resetting it would make editing a laundering route for `HIDDEN` content. Aggregates recalculate only when the rating actually changed. The server never invents a title — legacy rows render with no gap at all, not a placeholder. **(#586) Discounted price colour** — the current price now keeps one colour whether or not it is discounted (web `text-primary`, mobile `foreground`, each already canonical on its own PDP); the promotion is carried by the struck-through original, the `-X%` badge and the savings line. **(#587) Seller info moved to sit immediately before the ratings** it contextualises (mobile only — buyer-web's two-column grid already ordered it correctly, and moving it there would be a redesign). Shipped as a pure 66-line block move, so it had nothing to fail if the order silently reverted; now pinned by `product_detail_section_order_test.dart`, verified to fail against the pre-move file. **(#588) Docs.** **Tests:** API **243 unit + 118 e2e**, buyer-mobile **194** (with both open PRs), seller-mobile **31**, web type-check + lint clean. **Runtime:** verified on the **iOS Simulator** against live `api.teka.cd` — no sold count, no condition badge, no numeric stock, correct coarse stock states at the qty-1/2/9/10/0 boundaries, and discounted card prices matching undiscounted ones. The Android emulator is unusable (its DNS resolution is broken while the host resolves fine). `xcrun simctl` offers no tap or scroll, so gesture-dependent checks are covered by widget tests instead of claimed; **review create/edit remains unverified at runtime by standing decision — no review writes to production from a personal account.** **One migration to apply** (additive, auto-applied); no env var; steps 1–4 of the deploy order can be spread over days.
- **Jul 26, 2026** — **Mobile connectivity UX · product-detail cleanup · rating/identifier fixes** (6 stacked PRs on `fix/mobile-connectivity-toast` → develop). Nine device-reported symptoms, which root-caused into **five** independent bugs — symptoms 6–9 (rating screen error, PDP inconsistent from Order Detail, favorite failure, missing rating section) were all **one** identifier bug. Tracker: `docs/mobile-product-detail-rating-connectivity-fixes.md`. **(PR1) Intrusive connectivity bar (both apps)** — `ConnectivityBannerHost` rendered a full-width bar in a `Column` above the routed content (mounted in `MaterialApp.router.builder`), so every appearance pushed the whole page down, and on a flapping link the 3s green "Connexion rétablie" bar re-fired every ~5s. Replaced by `ConnectivityToastHost` at the same mount point, whose `build()` returns the child verbatim (zero layout contribution) and announces changes via a floating snackbar. No `scaffoldMessengerKey` needed — `MaterialApp` wraps `builder:` *inside* its `ScaffoldMessenger` — and `SnackBarBehavior.floating` inherits bottom-nav + SafeArea clearance from the root scaffold. Five states → three buckets (`disconnected`/`noInternet` → "Connexion Internet indisponible."; `connected` → "Connexion rétablie." only when closing a loop actually opened; `unstable`/`reconnecting` → silent). Anti-flap = bucket-level dedupe (a fresh snapshot lands every ~30s because `ConnectivitySnapshot ==` includes `at`) + 2s settle + 60s cooldown (2× `healthyProbeInterval`), so a 35s-period flap yields ~1 toast/80s; cooldown is a `Timer`, not `DateTime.now()`, so it is deterministic under the test clock. Cold-start invariant (`reconnecting → connected` never flashes green) is now double-guarded. **No persistent global offline chrome** — sustained offline stays with the surface that needs it (checkout's inline notice, `AppErrorState`); an infinite-duration snackbar was rejected because `showSnackBar` queues and would starve every other snackbar. New shared `core/widgets/app_snackbar.dart` (styling on the `SnackBar`, not the theme, so both apps render identically). Mirrored byte-for-byte per Rule 15 incl. the 13-spec suite — **seller-mobile's first connectivity tests**. `connectivity_service/provider/status/sentry_reporter`, `core/network/` and `features/checkout/` untouched (folding `reconnecting` into `isOfflineProvider` would flicker checkout's place-order button on every recovery probe). **(PR2) "5 vendus" on public cards** — removed from `product_card.dart` (one widget backing all nine card surfaces) + the PDP price block, mirrored on buyer-web; `unitsSold` stays in the payload/model/types because it is the popularity sort key and is incremented on delivery. **(PR3) Share did nothing** — the handler and `share_plus` were already there; it had three silent-failure paths (null URL → bare `return`; uncaught `StateError` from `FlavorConfig.instance` in `productWebUrl`; `Share.share` throwing on iPad without `sharePositionOrigin`). All three now report to Sentry + show a French snackbar, the popover is anchored via a `GlobalKey`, and the button is disabled rather than inert while loading. Which path fires on the reporter's device still needs an on-device repro. Also removed the "Pour toute question, contactez le support Teka RDC." block (mobile) and the equivalent support link (buyer-web); support access unchanged elsewhere. **(PR4) Caractéristiques showed values without labels** — `getProductDetail` returned the raw Prisma row, where the label sits at `specifications[].attribute.name`, while **all three** clients (buyer-mobile, buyer-web, admin-web) read a flat `spec.name` that never existed. Fixed at source exactly as the `seller` shape in the same method once was: flattened to `{ id, attributeId, name, value, sortOrder }` with `orderBy: { attribute: { sortOrder: 'asc' } }`, empty rows dropped server-side; same flattening applied to `admin-products.service.findProductForReview`. Defensive Dart fallback to `attribute.name` so a mobile build on an older API degrades instead of breaking. **(PR5, the four-symptom bug) "Validation failed (uuid is expected)"** — Order Detail links to the PDP by `shortCode` (`productLinkId`), and `ProductDetailScreen` forwarded that **route param** to `WishlistButton`, the reviews section, related products and the `/products/:id/reviews` push. Only `/v1/browse/products/:identifier` resolves a shortCode; reviews and wishlist are `ParseUUIDPipe`, so each 400'd — the rating screen `Future.wait`'d four calls so one 400 failed them all, and the Avis section returned `SizedBox.shrink()` when stats were null, which is why it looked "missing". buyer-web hit the same bug and fixed it in `29882b6`; this is the mobile parity fix. `ProductDetailScreen.productId` → `identifier` (uuid | shortCode | slug) and `product.id` is now the only value handed to API-bound children; Order Detail gained `productReviewId` (always the uuid) for the rating CTA while `productLinkId` stays display-only. The Avis section shows a retryable notice instead of vanishing, and `ReviewsNotifier._init` loads its four calls independently so a guest's 401 on `canReview` no longer blanks it. Backstops: `UuidParam` (French "Identifiant invalide.") on the 8 buyer-facing reviews/wishlist params, and `extractDioErrorMessage` no longer passes Nest's built-in English pipe messages through as French business copy (mirrored to seller-mobile). **No API contract change.** Side effect: recently-viewed exclusion now works from every entry point (it compares uuids too). **(PR6) Docs** — `docs/mobile-connectivity.md` § UI rewritten (its copy table listed strings that never shipped and `connectivityBanner*` l10n keys that never existed in either app; the "mirrored in seller-mobile" test claim was false and is now per-row); CLAUDE.md Rule 15 gains `showAppSnackbar` + `UuidParam` bullets. **Tests:** API **229 unit + 118 e2e**, buyer-mobile **156**, seller-mobile **31** (was 12), buyer-web/admin-web type-check clean, `flutter analyze` clean for `lib/`+`test/` in both apps. **Not device-verified** (no device attached; cloud DB unreachable from the build env) — the outstanding device pass is listed in the tracker. **No DB migration, no env var, no deployment ordering constraint.**
- **Jul 24, 2026** — **Buyer cart nav · notifications · help pages · account deletion · app-review login** (7 independent PRs → develop, #558–#563). Six device-reported buyer issues, each root-caused, web/mobile parity kept. Tracker: `docs/buyer-cart-help-notifications-account-deletion-review-login.md`. **(P1 #558) Cart item didn't open the PDP** — buyer-mobile `CartItemTile` was an inert `Container` (added an `onOpenProduct` tap on image+title/price, stepper/remove stay independent); buyer-web `CartItemRow` linked to `/recherche?q=<title>` because the cart payload lacked `slug`/`shortCode`/`city` → extended the API cart serializer + `CartItem` type and used `productHref()`; unavailable products render as non-link text; PDP 404 shows "Ce produit n'est plus disponible." **(P3 #559) Help pages** — buyer-mobile `ContentPageScreen` rendered CMS Markdown as raw text → new dependency-free `MarkdownContent` widget (headings/lists/bold + tappable `tel:`/`mailto:`/`wa.me` via url_launcher); buyer-web allows `tel:` through react-markdown's sanitizer; removed the **dead duplicate** content-pages block in `seedPhase7Data` (always overridden by `seedPlatformBaseline`) and rewrote the 8 authoritative pages: canonical **+243991427171 / contact@teka.cd** with tappable links, COD-only payment (was Mobile Money), push/email + WhatsApp-OTP wording (no SMS/Google), 2-day return window (was 7); added À propos + Comment acheter to the mobile Aide menu; idempotent prod migration `manual/2026-07-24_content_contact_details.sql` (guarded per-page swap, no admin-edit clobber). **(P2 #560) "Annonces et promotions" toggle was a no-op** — sent the retired `smsBroadcasts` key the API ignores (live keys `pushBroadcasts`/`emailBroadcasts`); fixed on all four surfaces — buyer-mobile, seller-mobile, buyer-web, seller-web (single toggle drives both channels); buyer-mobile gained OS-permission awareness (`PushService.getPermissionStatus()` + banner). **(P5 #561, core bug) Notification Center empty ("Aucune notification")** — `OrderNotificationService` fired push/email to buyers but never wrote a `UserNotification` feed row (only sellers + broadcasts persisted); added a `recordBuyerFeed()` in all 10 buyer order events (persisted **before** the prefs gate — opt-outs/blocked push gate delivery only), ORDER rows deep-link to order detail (mobile `/orders/:id`, web `/commandes/:id`), + idempotent backfill script (only the safely-derivable "commande enregistrée" per order; historical intermediate pushes were never stored). **(P4 #562) Secure account deletion** — replaced the orphaned unsafe `DELETE /v1/users/profile` with a **30-day pending-deletion** lifecycle: `User.deletionRequestedAt`/`deletionScheduledAt` (+migration), `AccountDeletionService` + controller (`/v1/users/account/deletion` GET/POST/DELETE), role-specific re-auth (buyer OTP / seller password), `SUPPRIMER` phrase, safeguards (active orders / open returns / unpaid seller balance), revoke sessions+tokens + deactivate device tokens on request, reactivate-on-login within the window, daily `@Cron` (`@nestjs/schedule`) anonymizes past-due accounts (nulls PII, releases unique phone/email, soft-deletes SellerProfile; **retains** orders/reviews/financials), + UI on all 4 surfaces (visually-separated "Zone sensible"). **(P6 #563) App-store review login** — env-gated, allowlisted bypass in `BuyerOtpService.verifyOtp` (`isReviewLogin`): fixed OTP accepted only for the exact `APP_REVIEW_BUYER_PHONE_E164` when `APP_REVIEW_LOGIN_ENABLED`; login-only, empty config never matches, OTP never logged; **ships disabled** with a placeholder phone (owner supplies the approved number as a secret). Runbook `docs/app-review-login.md`. **Tests:** API **218 unit + 116 e2e** (new specs: `order-notification` buyer-feed ×3, `account-deletion.service` ×9, buyer-otp review-login ×5), buyer-mobile **139** + seller-mobile **12** tests, web type-checks + `flutter analyze` green. Not device-verified (no physical device; cloud DB unreachable from the build env → no live integration run). **Operator actions:** apply the two prod migrations, optionally run the notification backfill, and set the review phone/flag only during a review window.
- **Jul 24, 2026** — **Seller product images + category-specific attributes** (2 stacked PRs → develop: `image-entry-camera` P1/P2 ← `api-category-attributes-leaf-only` P3). Three device-reported issues; no Prisma schema change. **(P1) Empty image placeholder wasn't tappable** — `product_detail_screen.dart` rendered it as an inert `Container`; only the "Ajouter" link opened the manager. Now the whole placeholder is a `Semantics(button)`+`InkWell` opening the same `/products/:id/images` route (gated to DRAFT/REJECTED) with an "Ajouter des images" label + add-photo icon. **(P2) Image manager was gallery-only** — added a French bottom-sheet chooser (`_chooseSourceAndUpload`: **Prendre une photo** / **Choisir dans la galerie**); `_pickAndUploadImage` takes an `ImageSource`; reuses the existing compress-to-WebP + Cloudinary upload pipeline; duplicate-tap guarded; camera/photo `PlatformException` (denied) mapped to a French hint. iOS `Info.plist` gained `NSCameraUsageDescription`+`NSPhotoLibraryUsageDescription` (were missing → camera would crash on iOS); Android needs no manifest permission (image_picker delegates to the system camera). seller-web stays gallery-upload (no native camera) — business rules unchanged. **(P3) A monitor form showed cooker attributes** (Type: Cuisinière à gaz…, Nombre de feux, mixed with Taille écran/Résolution). **Root cause (reproduced against a seeded DB): `BrowseService.getCategoryAttributes` queried `productAttribute WHERE categoryId IN [self, parent, grandparent]` — a parent-chain merge.** Attributes attach per product type (the leaf), but the admin UI lets you add attributes to any category, so cooker attributes created on the **Informatique** parent leaked into every descendant product type. Fixed 3 ways: **(a)** API `getCategoryAttributes` → `where: { categoryId }` (leaf-only; single caller, no consumer relied on inheritance); **(b)** `categories.service.createAttribute` rejects (400) adding attributes to a non-leaf category (has children) — stops contamination at source; **(c)** idempotent prune migration `manual/2026-07-23_prune-non-leaf-attributes.sql` deleting **unreferenced** non-leaf `product_attributes` (backward-compatible — legacy attributes a product actually used are preserved; leaf-only fetch just stops offering them). Tests: `browse.service.spec` (leaf-only where + NotFound), `categories.service.spec` (non-leaf reject / leaf allow); API suite **209 green**; Flutter `product_image_manager_test` (chooser opens with both options) + analyze clean. **Verified on-device (Android emulator, dev flavor, local seeded DB):** placeholder tap → manager; chooser → **Prendre une photo** → device camera → capture → WebP → **Cloudinary upload** → thumbnail (1/8); a new **Moniteurs** product shows only Taille écran/Résolution/Garantie despite injected parent contamination. **Operator:** apply the prune SQL post-deploy (API fix alone resolves the symptom; SQL is hygiene). Tracker: `docs/seller-product-images-category-attributes-fixes.md`.
- **Jul 21, 2026** — **Seller-mobile submit / first-load / image-editing / notifications fixes** (4 stacked PRs `#550 submit-error` ← `#551 first-load-race` ← `#552 inline-image-edit` ← `#553 notifications` → develop). Four defects from a real device build + seller-web parity, each fixed at source, minimal + backward-compatible, **no DB/API-contract change**. **(P1) "Soumettre pour révision" failed with a generic banner on a 0-image draft** — the API correctly rejects a 0-image submission with a clear French reason (`products.service.ts:735`), but seller-mobile's `catch` discarded it. Now `_submitForReview` surfaces `friendlyErrorMessage(e)` + a **pre-flight guard**: when `images.isEmpty`, skip the round-trip and show an actionable SnackBar with an "Ajouter" shortcut to the image manager. seller-web's product-list submit no longer swallows the error (`ApiError.message` → existing banner; detail page already surfaced it). +3 `ProductsService.submitForReview` specs (0-image reject / non-draft reject / happy → PENDING_REVIEW). **(P2) First-open Commandes/Produits showed a full-screen error until "Réessayer"** — cold-start race: seller list `StateNotifier`s (`SellerOrdersNotifier`, `ProductsListNotifier`, `NotificationsNotifier`, `EarningsNotifier`, `PromotionsListNotifier`, `SellerReviewsNotifier`) fetched in their constructors before auth tokens restored → request with no bearer → 401 → error cached. The fix already existed in `dashboardStatsProvider` but was never ported: each provider now **starts in a loading state**, **stops fetching from the constructor**, and drives the first/re-load from the factory via `ref.listen(authProvider.select((s)=>s.status), …, fireImmediately: true)` (loads only on the transition into `authenticated`; warm path via `fireImmediately`, cold path via the transition guard → skeleton, never a cached 401). `home_screen` drops its now-redundant manual reload-on-auth workaround (would double-load). +provider test (no constructor fetch, loading start, single load). **(P3) Edit form had no image controls** (a working add+delete `ProductImagesScreen` existed but was only reachable from the DRAFT detail "Ajouter" button; seller-web manages inline) — extracted a shared **`ProductImageManager`** (add+delete grid reusing `ImageUploadTile` + `products_repository.uploadImage/deleteImage`; ≤8; compress-to-WebP; shrink-wrapped for ListView/Column; upload/delete errors now via `friendlyErrorMessage`); `ProductImagesScreen` reduced to a thin wrapper; `ProductFormScreen` embeds it **inline on the edit path** ("Images" section). **No reorder/cover** — neither web nor mobile has a reorder endpoint (`products.controller.ts` = `POST/DELETE :id/images` only), so parity = add+delete. Cloudinary delete already swallows+logs failures (`cloudinary.service.ts:128`) → DB row always removed, orphan logged for reconciliation, no user-blocking, no silent failure (verified, no change). +widget test. **(P4) "Centre de notifications" reported empty** — diagnosis: the seller notification stack is already fully wired (backend writes `UserNotification` rows for order + product-moderation + broadcast events; `/v1/seller/notifications` returns them; device tokens register on login), so the empty feed was **the P2 race** (notifications provider, now auth-gated). Two real code-proven gaps fixed: (a) a plain admin broadcast sends `{screen:'notifications'}` (`broadcasts.service.ts:383`) but the seller `NotificationRouter` had no case → tap did nothing → added `case 'notifications' → '/notifications'` (parity w/ buyer-mobile) + a router test; (b) the settings screen never reflected OS permission (a toggle could read "on" while the phone denied it) → added `PushService.getPermissionStatus()` (reads `getNotificationSettings()` without prompting) + a truthful banner (denied → "réactivez dans Réglages"; notDetermined → "Activer les notifications" request CTA). Admin `ALL_SELLERS` broadcast audience already exists in admin-web (no gap). A one-tap open-settings deep-link is deferred (needs a native package). **Remaining: on-device runtime pass** (needs a device/emulator) — steps in the tracker. Tests: seller-mobile suite **11** green + `flutter analyze` clean on our source; API `products.service.spec` green; seller-web `type-check` clean. Tracker: `docs/seller-product-loading-images-notifications-fixes.md`.
- **Jul 4, 2026** — **Buyer-mobile TestFlight fixes — nav / cart / search / checkout / town / rating** (branch `fix/buyer-mobile-cart-search-checkout-rating` → PR into develop). Six defects from a real iPhone build + buyer-web parity, each fixed at source, minimal + backward-compatible, no DB migration. **(1) Nav stack lost after login from a PDP** (no back button) — `ensureAuthenticated` used `context.go` (replace) and the verify screen `go(returnTo)` (replace again), destroying the browsing stack. Now `ensureAuthenticated` is `Future<bool>` that **pushes** login + awaits; OTP verify **pops** connexion+itself so the PDP reappears with its back button and the original action **resumes in place**. New `pushAuthFlowProvider` deterministically distinguishes push (inline action → pop) vs redirect (protected route → `go(returnTo)`) since `canPop()` is true in both; router post-auth redirect skipped in push mode. **(2) Jumia-style PDP bar** `[accueil | − | qté | +]` via new `cartItemProvider(productId)` — stock-capped +/− (French SnackBar at the ceiling), qty 0 removes + restores the add button, already-in-cart aware, synced with cart/checkout. **(3) Search suggestions** — `getSearchSuggestions` discarded the endpoint's `products`+`brands`; now returns `SearchSuggestions{categories,brands}` and the search screen shows **Catégories + Marques** chips above the live product grid (brand tap re-runs the search, parity with web). **(4, P0 blocker) False "Erreur de validation" on confirm** — mobile built a non-UUID idempotency key the API rejects; now a real v4 UUID (`uuid` promoted to a direct dep). Shared `dio_error_messages` now prefers the field-specific `error.errors[0].message` over the generic wrapper (mirrored to seller-mobile, Rule 15). **(5) Town-mismatch warning** — `resolveDeliveryFee` returns `fromTown`; `quote()` adds per-seller `fromTown`+`townMismatch` + top-level `townMismatch`/`buyerTown` (additive; case/space-insensitive compare). Both web + mobile render a **non-blocking** amber card ("… ville différente … frais de transport supplémentaires …"); confirm stays enabled. **(6) Rating after delivery** — `canReview()` now returns the eligible `orderId` (mobile `CanReviewModel` already parsed it; web `ReviewModal` now sends it → fixes the web 400); "Noter le produit" entry added per delivered item on both order-detail pages (mobile → `/products/:id/reviews`, web → PDP `#avis`). Eligibility/moderation rules unchanged. Tests: API **202 unit / 29 suites** green (new `reviews.service.spec.ts` + checkout town-mismatch cases); buyer-mobile `flutter analyze lib test` clean + 3 test files (checkout townMismatch, dio field-message, cartItemProvider); buyer-web `tsc` clean. Tracker: `docs/buyer-mobile-cart-search-checkout-rating-fixes.md`.
- **Jul 4, 2026** — **iOS TestFlight CI/CD — operator bootstrap + end-to-end verification** (on `develop`; `develop → main` release PR). Merged #524, then bootstrapped the Fastlane `match` store for **both** apps in the new private `teka-ios-certs` repo (buyer `com.tootiye.teka` + seller `com.tootiye.tekaseller`, **one shared team `YK6Z393A4D`** → one Apple Distribution cert `HC5DQBC8S2` + a profile per bundle id; non-interactive via the App Store Connect API key). Corrected the seller team `APYDR8XX6S → YK6Z393A4D` in the Fastfile map, `ExportOptions-seller.plist`, seller pbxproj (9 configs), and docs (`fc14abf`). Verified all GitHub secret names/scopes (moved signing secrets to Repository scope; ASC `.p8` secrets are base64; `ios-testflight` env holds only the reviewer rule). Ran "Release mobile IPA" (app=both) repeatedly to prove the pipeline, fixing **four issues found only by running it for real:** (1) `persist-credentials:false` on the build checkout so the persisted `GITHUB_TOKEN` github.com auth header doesn't collide with the match-repo PAT header (two `Authorization` headers → 403); (2) explicit whitespace-stripped git auth (`http.<host>.extraheader`) for the private certs clone + verify-from-neutral-dir with the real git error surfaced; (3) **absolute** IPA path to `fastlane upload_testflight` (fastlane runs the lane from its own project dir, so a relative `ipa/*.ipa` wasn't found); (4) auto-incremented epoch `CFBundleVersion` via `flutter build ipa --build-number=$(date -u +%s)` (TestFlight rejects a duplicate build number — the earlier manual Xcode upload was build `2`). **Verified end-to-end (run 28698644247, conclusion success):** both `build` jobs sign → build → dSYM→Sentry → `.ipa` artifact automatically; the `ios-testflight` gate was approved; both `testflight` jobs uploaded to App Store Connect. STATUS marked done. All four fixes are CI-only (`c942eed`, `f71cbc5`, `ddea7e9`, `a035098`); no API/DB/web/runtime change.
- **Jul 3, 2026** — **iOS dSYM → Sentry + TestFlight CI/CD** (branch `feat/ios-testflight-cicd` → PR into develop; **gated on operator one-time setup** before the first release run). Wires the two missing iOS release gaps by **mirroring the Android two-workflow split** — no app regen, no API/DB/web change, Android CI untouched. **(A) dSYM → Sentry:** new `.github/workflows/build-mobile-ipa.yml` (macOS, app×flavor matrix, `flutter build ios --no-codesign` + the same "Upload Sentry debug symbols" `sentry_dart_plugin` step the Android workflows use). The committed `sentry.properties` already governs iOS and every Release config emits `dwarf-with-dsym`, so iOS crash reports now symbolicate; runs for **all 3 flavors** with zero signing infra. **(B) Fastlane (greenfield, repo-root, parametrized by `TEKA_APP`):** `Gemfile`/`Gemfile.lock` (fastlane 2.236.1, darwin+ruby platforms), `fastlane/{Fastfile,Appfile,Matchfile,ExportOptions-buyer.plist,ExportOptions-seller.plist}`. Lanes: `setup_signing` = `setup_ci` + `match(appstore, readonly)` + `update_code_signing_settings` flipping **only `Release-production` to Manual in the CI checkout** (never committed → local dev keeps `Automatic`); `upload_testflight` = `app_store_connect_api_key` + `upload_to_testflight`. `ExportOptions-*.plist` set `uploadSymbols=false` so Apple doesn't re-collect dSYMs (sidesteps the precompiled `Sentry.framework` dSYM failure the user hit — symbolication is Sentry's job). **(C) release:** new `.github/workflows/release-mobile-ipa.yml` (mirror of `release-mobile-aab.yml`, `workflow_dispatch` app=buyer|seller|both, production only). `build` job (auto, macOS): fail-fast secret guard → match signing → `flutter build ipa … --export-options-plist` (preserves flavor `--dart-define`s + Sentry DSN/release — `gym` can't inject those) → dSYM → `.ipa` artifact. `testflight` job bound to the protected **`ios-testflight` GitHub Environment** (reviewer approval) → download artifact → `fastlane ios upload_testflight`. `MATCH_GIT_URL` derives from `github.repository_owner`. **Decisions (user):** signing = Fastlane `match` (new private `teka-ios-certs` repo; both apps on the same Apple team `YK6Z393A4D` → one Distribution cert + a profile per bundle id, no collision); TestFlight = production only (dev/staging = build + dSYM artifact); approval = manual trigger + environment gate. Docs: `docs/mobile-release.md` iOS section rewritten (pipeline + operator setup + secrets table) + `docs/sentry.md` (iOS dSYM section; the four-workflow `SENTRY_AUTH_TOKEN` usage). `.gitignore` += `*.mobileprovision`/`*.p12`/`*.cer` + fastlane artifacts. Static-verified: Ruby `-c` on all fastlane files, `plutil -lint` on both plists, `ruby -ryaml` parse on both workflows, `bundle lock` resolved. **Operator TODO (not code):** create `teka-ios-certs` + `fastlane match appstore` per app; per-team ASC API keys; `ios-testflight` environment + reviewers; secrets `MATCH_PASSWORD`/`MATCH_GIT_BASIC_AUTH`/`{BUYER,SELLER}_ASC_API_{KEY_ID,ISSUER_ID,KEY_P8_B64}`. Full runbook: `docs/mobile-release.md` → "iOS — TestFlight / App Store (automated CI)".
- **Jun 30, 2026** — **Order Details & Admin Dashboard Improvements** (5 phases → develop). Built on the managed workflow; reused existing services, **no new endpoints / no migration**. **P1 (API):** buyer `findBuyerOrderById` items += `product{id,slug,shortCode,status,city{slug}}` (PDP link target + availability; snapshots stay authoritative); admin `findProductForReview` seller select += user `status` + `sellerProfile{avgRating,totalReviews}` (the detail endpoint, NOT the unrelated `adminSelect`); admin `getDashboardStats` += `orderOps{awaitingConfirmation,readyForPickup,receivedAtTeka,outForDelivery,deliveredToday,pendingReturns}` via a status groupBy + deliveredToday + REQUESTED-return count (+2 stats tests). **P2 (admin):** new **`/dashboard/sellers/[id]`** seller-detail page (boutique + contact + rating, reusing `GET /v1/admin/users/:id`); **fixed two latent shape-drift bugs** — the admin **order** detail seller card and admin **product** detail seller section both read `businessName`/`user` but the API returns `firstName/lastName/email` + nested `sellerProfile`, so they rendered blank → realigned + enriched (email/phone/status/rating), clickable boutique → seller detail; "Vendu par {seller}" per order line. **P3 (admin):** dashboard "Opérations commandes" — 6 managed-bucket cards from `stats.orderOps`, each deep-linking to the filtered order/return list; admin orders + returns lists now honor a `?status=` URL param on mount (window-based, matching the products list). **P4 (buyer):** order products link to the PDP — buyer-web via city-first `productHref(item.product)`, buyer-mobile via `/products/{shortCode|id}` (OrderItemModel += `productShortCode`/`productStatus` + `productLinkId`); inactive products show "· Indisponible", aren't linked, order line preserved; seller-mobile reviewed + intentionally unchanged. **P5:** gates + docs. One Order = one seller (multi-seller cart → multiple Orders), so "seller per line" surfaces the order's single seller. Gates: api 196 unit + 116 e2e; 3 web type-check+build; both mobile analyze (0 lib errors) + buyer-mobile order tests; live-verified all new API contracts (buyer item.product, admin product seller status+rating, dashboard orderOps, seller-detail). SEO preserved (buyer order pages are client islands; PDP links use the canonical city-first URL). Tracker: `tasks/order-admin-improvements-progress.md`; model: `docs/order-workflow.md`.
- **Jun 29, 2026** — **Managed Order Workflow** (7 phases → develop; on `develop`, ready for `develop→main`). Upgraded the order flow from **seller-driven** to **Teka-managed** (Teka collects from the seller → delivers → collects COD cash → 2-day return window → seller payout minus commission) **without** app regen or global refactor. Decisions (confirmed with user): add 2 statuses (don't rewrite the enum); transfer delivery+cash control to admin; 2-day payout hold via **lazy** eligibility (no cron). **P0** schema (`OrderStatus` +`READY_FOR_TEKA_PICKUP`/`RECEIVED_AT_TEKA`, `Order.deliveredAt`/`returnedAt`, `ReturnRequest`+`ReturnStatus`; idempotent prod SQL `2026-06-29_managed-order-workflow.sql`) + canonical state machine `order-workflow.constants.ts`; **admin** owns `markReceivedAtTeka`/`markOutForDelivery`/`markDelivered` (deliveredAt + COD COMPLETED + unitsSold + earning); seller gains `markReadyForPickup`; **buyer cancel widened to {PENDING,CONFIRMED,PROCESSING} + restock-on-cancel** (was missing on every path) (+13 tests). **P1** seller surfaces (web+mobile): ship/deliver → "Prête pour collecte"; dashboard status summary (`GET /v1/sellers/orders/stats`); product **town** column; removed seller ship/deliver routes; fixed latent COD label bug. **P2** returns module (`ReturnsService` create/list/approve/reject; approve → RETURNED+returnedAt+restock+`reverseEarning`+REFUND tx) + `AdminReturnsController` + admin order-detail managed buttons + `/dashboard/returns` (+8 tests). **P3** buyer returns (`POST /v1/orders/:id/return`, 2-day window) + managed status labels (web+mobile). **P4** seller new-order **email** (new template) + **in-app `UserNotification`** (type ORDER) + push; return notifications; PostHog (`return_requested`, `order_received_at_teka`, `order_out_for_delivery`); `UserNotificationType`+=ORDER. **P5** **lazy return-window payout hold**: earnings eligible only `deliveredAt+2d` & not RETURNED, computed on read (`getBalances`/`getEligibleEarnings`); `createEarning` no longer credits `walletBalanceCDF` (retained but non-authoritative); payout reserves only eligible; seller earnings UI shows "en attente (fenêtre de retour)" — **runtime-verified on real Postgres** (3d-old→available, now→held, RETURNED→excluded). **P6** all-surface gates + docs. Gates: **api 194 unit + 116 e2e**, 3 web type-check+build, both mobile analyze (0 lib errors). Tracker: `tasks/managed-order-workflow-progress.md`; model: `docs/order-workflow.md`. **Operator:** apply the prod migration (Apply-migration action) before/with deploy.
- **Jun 29, 2026** — **Order Management Workflow — Review & Implementation** (9 phases, develop→main). Reported bug: confirmed buyer orders not appearing on buyer `/commandes`, seller dashboard, buyer-mobile. **Root cause (NOT creation — DB-verified the orders exist): order-list response-shape mismatch.** List endpoints emit `{ success, data: { data: Order[], pagination } }` (services return `{data,pagination}` → `ResponseInterceptor` wraps once); correct read is `res.data.data` + `res.data.pagination`. buyer-web read `res.data.meta` (threw), seller-web read `res.data.orders` (empty), buyer-mobile cast the wrapper Map `as List` (threw); admin-web worked (defensive); **seller-mobile was the only correct reference**. **Phase 2** aligned the 4 clients (no API change → mobile-safe; +pure `parseOrdersEnvelope` +4 tests) and **shipped early** (release 2a24026) — EMULATOR-VERIFIED Mes commandes + detail (TK-20260628-BBA3 = 123.500 FC). **Phases 3-9 (audit + gap-fill):** the COD lifecycle (paymentStatus separate from order status, no fake-paid, → COMPLETED on delivery), commission (on subtotal excl. delivery, 10% default cascade category→global→default, Math.round, net→wallet, snapshotted, idempotent) and snapshots were **already correct** (+commission unit tests); buyer-web order-detail not-found copy fix; **seller revenue breakdown** added — seller/admin `findOrderById` return `financials {grossCDF,commissionCDF,netCDF,commissionRate,isFinal}` (persisted earning once delivered, else projected) via a new shared `EarningsService.computeBreakdown`; seller-web "Votre rémunération", admin-web "Répartition financière", seller-mobile "Votre rémunération (estimation)"; buyer-mobile detail "Mode de paiement: Paiement à la livraison"; emails (Resend order-confirmed/shipped/delivered/cancelled), push (FCM `OrderNotificationService`) + PostHog (`order_created`/status/`payment_completed`) all already wired. Gates: api 171 unit + 116 e2e, 3 web type-check+build, both mobile analyze. Tracker: `tasks/order-management-progress.md`; model: `docs/order-workflow.md`.
- **Jun 28, 2026** — **Mobile Image Preview + Dot-Separator Price Formatting** (6 phased commits → develop). (1) buyer-mobile full-screen product-image viewer: replaced the AppBar back arrow with a clear top-right **close (X) button** in a SafeArea over the black viewer + a page-counter pill; swipe (PageView) + zoom (InteractiveViewer) preserved (`features/catalog/.../image_gallery.dart`). (2) **Dot thousand separators** for FC (52.957 FC) + USD `1.250,75 $US` via a **shared formatter**, display-only (no value/calc changes): web canonical `packages/shared/.../currencies.ts` (`formatFC`/`formatUSD`/`groupThousands`) consumed by buyer-web (`lib/format.ts`), admin-web + seller-web (16 inline formatters delegated), and the API notification/payout formatters; mobile `core/utils/price_formatter.dart` in both Flutter apps mirrors it. Kept `*CDF` field names + ISO `'CDF'` + JSON-LD `priceCurrency`. Tests: api 165 unit + 116 e2e, buyer-mobile 120 (+price_formatter test), all web type-check/build, both mobile analyze. Live-verified: buyer-web /promotions dots; buyer-mobile emulator dots on cards/PDP + image-preview open/close. Tracker: `tasks/image-preview-and-dot-formatting-progress.md`; model: `docs/delivery-fees-and-currency.md`. **Deferred follow-up:** buyer-mobile HOME header debug overflow (`home_screen.dart`) — separate fix.
- **Jun 28, 2026** — **Delivery-Fee Correctness + Currency CDF→FC** (6 phased commits → develop; code-complete, unpushed). **Free-delivery root cause**: server calc always returned ≥ a 5000 default, so checkout "Gratuit" could only come from buyer-web's `/v1/checkout/quote` call failing and being swallowed (`catch{}→free`). **Scaling root cause**: the admin delivery-zone form lacked the ×100/÷100 major↔minor conversion the seller product form has → fees stored 100× too small. Decisions: **D1 block on no-zone** (no silent default), **D2 city-based matching**. **API**: `estimateFee` trim+case-insensitive + `{found}` (no default charge); `checkout()` throws friendly FR block when no zone; `quote()` returns per-seller + overall `deliveryAvailable`; city-resolved `fromTown/toTown`; payout/validation messages CDF→FC. **admin-web**: zone form ×100/÷100 + FC. **buyer-web**: checkout blocks + message (no swallowed free), `formatCDF`→"FC". **shared/admin/seller-web**: 14 inline formatters + labels → FC (Intl can't render "FC", so output changed; ISO `'CDF'` + JSON-LD kept). **buyer-mobile**: 1-line `price_formatter` symbol→FC + checkout `deliveryAvailable` block. **seller-mobile**: ~14 widgets/forms → FC. Kept all `*CDF` field names. Tests: api 165 unit + 116 e2e, buyer-mobile 116 (+new deliveryAvailable test), all web build, both mobile analyze; live FC verified on buyer-web /promotions. Tracker: `tasks/delivery-fee-and-fc-progress.md`; model: `docs/delivery-fees-and-currency.md`. Follow-ups (user): prod zone-data audit (100×-off / route coverage) · confirm original cause on prod as a real BUYER · click-test login-gated admin/seller UIs.
- **Jun 24, 2026** — **Product Lifecycle Management** (phased PRs #473–#476 + docs → develop). Marketplace-grade lifecycle, **extending** the existing one (DRAFT/PENDING/ACTIVE/REJECTED/ARCHIVED + soft-delete + IDOR-safe ownership already existed). **Phase 0 audit** (3 sweeps); decisions: **+`SUSPENDED`** enum (out-of-stock stays derived from quantity≤0; DELETED stays `deletedAt`), **ACTIVE content edit → re-review**, **skip SKU + add `ProductStatusLog`**. **#473 (API):** migration `2026-06-24_product_lifecycle.sql` (+SUSPENDED + `product_status_logs`); seller withdraw/restore/duplicate + content-edit-re-review (value-compared) + search; admin suspend/restore/archive/soft-delete/history + multi-field search; every transition logs `ProductStatusLog` + PostHog lifecycle events; SUSPENDED notifies seller. **#474 (admin-web):** search + suspend/restore/archive/soft-delete + collapsible status-history. **#475 (seller-web):** search + withdraw/restore/duplicate + ACTIVE content-unlock with re-review banner. **#476 (seller-mobile):** parity — search + SUSPENDED + withdraw/restore/duplicate. Buyer unchanged (SUSPENDED auto-excluded by `status=ACTIVE`; out-of-stock UX already shipped). 163 unit + 116 e2e + mobile tests; all apps build/analyze clean. Prod migration gated (Apply-migration action). Tracker: `tasks/product-lifecycle-progress.md`; model: `docs/architecture.md` → "Product Lifecycle".
- **Jun 24, 2026** — **Advanced Search, Autocomplete & Filtering** (phased PRs #461–#464 + docs → develop). Marketplace-grade search built by **enriching** the existing French FTS engine (not greenfield). **Phase 1 audit** found a strong base (FTS + `f_unaccent` + GIN, trigram typo tolerance, suggestions endpoint, dynamic facets, `search_performed`); decisions: DB `SearchSynonym` (query-expansion) + `SearchQuery` log (+ keep PostHog), ranking signal blend (no ML), recent searches client-local. **#461 (P2–3 backend):** `SearchSynonym` + `SearchQuery` tables + idempotent migration; **synonym query-expansion** (cached 60s, sanitized, injection-safe — gsm→portable/smartphone/telephone, frigo→réfrigérateur, tv, pc/laptop; 8 seed groups); **ranking blend** (relevance → in-stock → unitsSold → avgRating → recency); fire-and-forget search logging (guarded); `@Throttle(40/10s)` on autocomplete; suggestions += **brands** + synonym-expanded products; `GET /v1/browse/search/popular`. **#462 (P4 filters):** mobile filter parity — price range + promotion + **sort-bug fix** (`'popular'`→`'popularity'`, was a 400). **#463 (P5 mobile):** recent searches (shared_preferences) + popular chips in empty/zero-result states + active-filter badge + `zero_results`. **#464 (P6 web):** autocomplete brands + recent (localStorage) + popular; search-page zero-result recovery chips; `zero_results`. **P7–8:** perf validated (GIN/trgm indexes present; planner-optimal plans) + docs (architecture.md Search section). SEO preserved (`/recherche` noindex; category pages indexable). Verified: "gsm" 0→5 via synonyms, "telephonne"→Smartphones, "sam"→Samsung; 152 unit + 116 e2e + 115 mobile tests; all apps build/analyze clean. **Prod:** search migration ships at release via the Apply-migration action (gated). Tracker: `tasks/search-progress.md`; model: `docs/architecture.md` → "Search engine".
- **Jun 24, 2026** — **Catalog Architecture Refactor — 3-level taxonomy** (phased PRs #455–#458 + docs → develop). Added **Product Type** as a 3rd level (Category → Subcategory → Product Type) by **reusing the self-referential `Category` tree** — product types are `Category` nodes (`16000000-` range), **no new table** — so the breadcrumb (3-level), browse child-expansion (self+2), and attribute/brand systems were already depth-agnostic; the only API depth fix was a **recursive facet count roll-up**. New **7-category ordered tree → 35 subs → ~145 product types → ~360 per-type attributes → 49-brand library** (always "Autre"); **Construction & Bricolage + Automobile & Moto removed** (audit: 0 real products dev+prod). **Condition was already `NEW`/`USED`.** **#455 (A+B)** `taxonomy-data.ts` rewrite + `seed.ts` (3-level consumption, brand-name-collision fix, real-product remap by old name, taxonomy-driven demo regen) + recursive roll-up. **#456 (C)** admin: `PATCH /v1/admin/categories/reorder` + `findTree` maps `subcategories→children` (3 levels) + orphan-node soft-delete (161 cleaned) + tree move-up/down UI. **#457 (D)** seller selectors (web combobox full-path; mobile recursive search/tree/display) pick the product-type leaf. **#458 (E)** buyer drill-down chips (web + mobile) + recursive sitemap (product-type pages indexed). **F** docs (`architecture.md`, CLAUDE.md §4) + `reorderCategories` tests. Existing products **remapped, not deleted**; prod migration is a separate gated step. Verified on dev: seed exit 0, facet counts roll up (Supermarché 58…), 21/21 real products remapped, 149 unit + 116 e2e + mobile tests pass, all apps build. Tracker: `tasks/catalog-refactor-progress.md`; model: `docs/architecture.md` → "Marketplace taxonomy + brands".
- **Jun 23, 2026** — **Universal Deep Linking & App Links** (buyer-mobile; phased PRs #446–#449 + docs → develop). A `https://teka.cd/...` link opens buyer-mobile on the right screen when installed, else the website; **additive, zero web URL/SEO change**. **#446** web association files (`buyer-web/public/.well-known/assetlinks.json` [placeholder Play-signing SHA-256] + `apple-app-site-association` for `YK6Z393A4D.com.tootiye.teka` + `next.config.ts` content-type header; nginx passes `.well-known` through, no Cloudflare). **#447** Flutter core: pure `DeepLinkParser` (mirrors buyer-web `lib/urls.ts` shortCode resolver; host-allowlist; private/unknown→browser) + `DeepLinkController` (`app_links` cold/warm + town context) + `/search?q=` + `WEB_BASE_URL` per flavor; 16 unit tests. **#448** Android App Links (manifest `VIEW`/`BROWSABLE` `autoVerify` + per-flavor `appLinkHost` placeholder prod=teka.cd + `teka://`; verified in built APK via aapt). **#449** iOS Universal Links (`applinks:teka.cd` entitlement, untracked `ios/`, build-verified) + `NotificationRouter` `url` unify + product Share (`share_plus`, canonical URL). Phase 7: `deep_link_opened`/`deep_link_failed`/`product_shared` analytics + Sentry breadcrumb (no PII) + `docs/deep-linking.md`. The API resolver `GET /v1/browse/{products,categories}/:identifier` (shortCode/slug/uuid) needed no change. Verified: 115+ mobile tests, dev APK + iOS Runner.app builds, buyer-web build + live `.well-known` serving (200 + `application/json`). Operator: real Play-signing SHA-256 in assetlinks.json; iOS Associated Domains capability at sign time. Tracker: `tasks/deep-linking-progress.md`; model: `docs/deep-linking.md`.
- **Jun 23, 2026** — **Broadcast notifications (Admin → Buyers)** (5 phased PRs #438–#441 + docs → develop). Production notification platform on the generic user-scoped `UserNotification` feed + FCM (`PushService`). **A** (#438) backend: `UserNotificationType` +BROADCAST/PRODUCT_PROMO; `NotificationBroadcast` +productId/recipientIds; idempotent migration `2026-06-23_broadcast_notifications.sql` (dev-applied); audience = recipientIds (specific buyers) OR segment; **fan-out on write** (createMany one `UserNotification` per recipient; opt-outs gate push/email delivery only, never the feed); product/notifications push deep-link data; new unified `/v1/notifications` controller (current user; seller alias untouched); send `@Throttle`; 7 new tests (149 unit + 116 e2e). **B** (#439) admin-web: extended broadcasts page → **Centre de notifications** (audience toggle + buyer autocomplete + product typeahead + live preview + created-by/recipients audit). **C** (#440) buyer-web: header bell (client island, 60s poll, deep-link) + `/notifications` page (`noindex`) — SEO-safe. **D** (#441) buyer-mobile: model+repo+provider, wired the stub Notification Center, AppBar unread badge, `NotificationRouter` screen:'notifications' mapping (FCM receipt/tap already done). **E** docs + `notification_opened` analytics. Decisions: fan-out on write; all/specific buyers + linked product; extend (not replace) the admin page; iOS push deferred. Tracker: `tasks/broadcast-notifications-progress.md`; model: `docs/architecture.md` → "Notifications & broadcasts".
- **Jun 23, 2026** — **Per-product discount system + product-card UX** (6 phased PRs #431–#435 + docs → develop). Industry-standard seller-set discount price (no admin approval, always-on; distinct from the untouched admin Promotion/flash-deal model). `Product.discountPriceCDF/USD` (nullable BigInt) + `OrderItem.listUnitPriceCDF/USD` snapshot; effective price = `discountPriceCDF ?? priceCDF`, % derived on display, validation `0<discount<price`. **A** (#431) backend: schema + idempotent migration `2026-06-23_product_discount_price.sql` (dev-applied), DTO validation + `validateDiscount`, relaxed edit-after-publish (ACTIVE → price/discount/stock only), cart/checkout/order effective-price snapshot, browse `onPromotion` filter (Prisma + FTS), demo-discount seed (44/173 dev), 142 unit + 116 e2e. **B** (#432) buyer-web: card redesign (−X% + strikethrough; **removed condition badge + seller name** from cards), PDP "Vous économisez", cart/checkout savings, SEO-safe JSON-LD/metadata effective price, `/promotions` route + home section + filter toggle. **C** (#433) seller-web: discount input + live −X% preview + edit-after-publish for ACTIVE. **D** (#434) buyer-mobile: models/card/PDP/cart/wishlist + promotions screen+section+filter. **E** (#435) seller-mobile: discount input (DRAFT/REJECTED; ACTIVE-edit deferred). **F** docs + analytics enrichment. Tracker: `tasks/discount-system-progress.md`; model: `docs/architecture.md` → "Per-product discounts".
- **Jun 22, 2026** — **Buyer-mobile UX/UI redesign + navigation overhaul** (in flight; 2 PRs). Audit found a flat router with no shell/bottom-nav, an overloaded red AppBar (6 targets), red doing background-duty everywhere, and a Favorites back-button inconsistency. **Step 1 — l10n removal (PR #422, merged to develop):** the platform is French-only, so the gen-l10n/`AppLocalizations` machinery was removed from buyer-mobile — ~272 `l10n.*` calls across 27 files inlined as French literals; `lib/l10n/` + `locale_provider.dart` + `l10n.yaml` deleted; `generate: true` + the delegate dropped (framework `GlobalMaterialLocalizations` stays `fr`); CI's gen-l10n step now guards on `l10n.yaml`; CLAUDE.md Rule 1 rewritten. (seller-mobile l10n + web next-intl are separate later follow-ups — user decision.) **Step 2 — navigation + redesign (PR pending, `feat/buyer-mobile-bottom-nav`):** `StatefulShellRoute.indexedStack` bottom nav (Accueil · Categories · Favoris · Panier · Compte, cart badge, one navigator per tab); **Search is a Home search bar, not a tab**; **Orders moved under Compte**; new Categories tab grid + Notifications screen; simplified **white** AppBar (brand + town pill + notification bell) replacing the red 6-icon header; **Favorites back-button bug fixed by construction** (top-level tab — was caused by `context.go(returnTo)` replacing the stack to `[Wishlist]` root vs `push` keeping a parent); guest→protected-tab→login gains a ✕ close (never stranded); red rebalanced to CTAs/accents. Preserved APIs/business-logic/analytics (PostHog `$screen` via per-branch `PosthogObserver`)/auth/town-architecture/guest gates. analyze 0 errors; 93 tests; device-verified on emulator (prod flavor, Kolwezi). Tracker: `docs/buyer-mobile-redesign.md`. Deferred Step 3: product-card polish + shared empty/error/loading widgets + push error-mapping into shopping providers.

- **Jun 21, 2026** — **Town Architecture Refactor** (release #408, shipped + verified on prod). Scalable, Amazon-style town selection across all 4 apps + API, 4 phased PRs (#404–#407). API: `City.accentColor`/`heroImageUrl` (data-driven towns — future towns config-only, no hardcoded slug switches), `User.preferredCityId` + `PATCH /v1/users/me/preferred-city` (cross-device persistence; `/me` carries the resolved `preferredCity`), seller-profile update accepts `cityId`. buyer-web: Amazon-style header selector ("Livrer à / {ville}"), **SEO-safe first-visit modal** (client overlay over SSR content, cookie-gated — deliberately not an SSR gate), persistence to **cookie + localStorage + profile**, homepage "Achetez dans votre ville" section removed → crawlable `/{ville}` links moved to the footer, data-driven `lib/city-accent.ts`. buyer-mobile: profile hydration (`ref.listen(authProvider)` → `hydrateFromProfile`) + PATCH sync + data-driven `TekaColors.cityAccent`. seller-web + seller-mobile: profile **city picker** (existing `SellerProfile.cityId`; `location` kept as free-text address detail — sellers don't browse by town). Two idempotent prod migrations applied via the Action (concurrency-queued behind the deploy): `2026-06-21_town_architecture.sql` then `2026-06-21_town_identity_backfill.sql`. Decisions: D1 SEO-safe auto-modal, D2 `User.preferredCityId`, D3 promos/banners/categories stay global (products+search already town-scoped), D4 buyer apps select town / sellers get a profile picker. Tracker: `docs/town-architecture-refactor.md`.
- **Jun 21, 2026** — **Category image tiles** (release #410, shipped + verified on prod). Emoji category cards → real product-photo icons (circle + label below) on buyer-web + buyer-mobile. Bundled static assets keyed by category slug (`public/categories/<slug>.png` web, `assets/categories/<slug>.png` mobile, 256px), emoji fallback for subcategories / future categories (chosen over a DB/Cloudinary `imageUrl` field — the 7 top categories are a fixed taxonomy and bundling is faster + offline-friendly on DRC 2G/3G). web: `lib/category-images.ts` + `components/category/category-icon.tsx` used by home-page, city-landing-page, all-categories page. mobile: `category_images.dart` + `category_circle.dart` (circle+label strip replaces the home pill strip), `CategoryModel.slug`, pubspec assets entry. No API/DB/migration. Prod-verified: 7 icons serve 200 image/png on teka.cd. Add a future icon = drop `<slug>.png` in both asset dirs + one map line.
- **Jun 22, 2026** — **City landing hero parity** (release #412, shipped + verified on prod). `/{ville}` city pages rendered a flat red-gradient hero instead of the homepage's premium image hero (two divergent implementations). Extracted a shared `StoreHero` (`apps/buyer-web/src/components/home/store-hero.tsx`) — single source of truth (town image + accent badge + H1 + subtitle + CTA + trust strip). Homepage uses it as the `BannerCarousel` fallback (unchanged); city page renders it **directly** so the city `<h1>` stays in SSR HTML (BannerCarousel's loading skeleton would push it client-only — SEO regression avoided). Copy is template-driven (`Products.cityLandingTitle/Subtitle` + `city.name`); image/accent from the data-driven `City.heroImageUrl`/`accentColor` → a new town auto-gets the hero with zero code + zero per-city data (decision: template, no new DB columns). buyer-mobile: new `CityHero` widget on home (bundled `assets/hero/<slug>.jpg` for Lubumbashi/Kolwezi + accent-gradient fallback). No API/DB/migration. Prod-verified: `/lubumbashi` + `/kolwezi` ship the city H1 + subtitle + hero image in raw SSR HTML.
- **Jun 22, 2026** — **Town switcher fix + town-selection UX** (release #416, shipped + verified on prod). The header "Livrer à {city}" selector did nothing on non-home pages (PDP/category/search) — it only opened after navigating back to `/`. Root cause: `<CitySelectorModal />` was mounted in `home-page.tsx` only, while the global header just flips the `showSelector` store flag → no modal mounted elsewhere to render it. Fix: mounted the modal once in `app/layout.tsx` (global; renders null until opened → no SSR footprint), removed the homepage mount; redesigned the modal (compact, searchable for 50+ towns, province grouping, town-accent selected state, full a11y, mobile bottom-sheet); added smart centralized town-switch routing (`lib/town-switch.ts` `resolveTownSwitchUrl` — category → same category in new town, else `/{newSlug}` — + `lib/use-select-town.ts` `useSelectTown`; modal + `CityPrompt` both go through it, no per-page logic). buyer-mobile: searchable city screen (50+-town scalability parity; no bug there). No API/DB/migration. Decisions: smart centralized routing; web fix + mobile search. Prod-verified: SSR non-regression (0 `role="dialog"` in raw HTML on all page types), new modal strings live. Pre-existing follow-up (out of scope): cold deep-links to category/PDP don't adopt the URL town into the header. Tracker: `docs/town-switcher-ux.md`.
- **Jun 22, 2026** — **buyer-mobile login regression hotfix** (#420 → release #421). P1's guest-browsing change removed the explicit `context.go()` from the OTP + claim verify screens and relied on the router redirect; `refreshListenable` doesn't reliably navigate off a **pushed** auth route, so after a valid WhatsApp code the user was stuck on the OTP screen (login *succeeded* — tokens stored, push token registered — but the screen never moved). Fix: restored explicit navigation to the saved return-to route (or home) after a successful verify in `otp_verify_screen.dart` + `claim_verify_screen.dart`; the post-await nav runs before the async redirect re-eval so it wins cleanly. **Device-verified on the prod-flavor app:** OTP verify → Home, with the authenticated wishlist/cart badges populating. No API/DB.
- **Jun 22, 2026** — **Mobile guest browsing + enterprise error handling** (P1 #417 + P2 #418, shipped; no API/DB/migration; reaches devices on the next Play Store build). **P1 (buyer-mobile guest browsing):** root cause = `core/router/app_router.dart` forced every guest to `/auth/connexion` (browse APIs already public). Selective router gate — city-first for everyone; only `/cart`/`/checkout`/`/orders`/`/wishlist`/`/profile` require auth, with `returnToRouteProvider` for seamless return; `core/auth/auth_guard.dart` `ensureAuthenticated()` on inline protected actions (add-to-cart, favorite); guest-safe home (wishlist/cart badges, product-card heart, city profile-sync skip auth-only calls → 0×401 for guests). Device-verified on the prod-flavor app. **P2 (error handling, buyer + seller):** root cause = auth providers/screens set `error: e.toString()` (raw `DioException` shown); the centralized `core/network/dio_error_messages.dart` mapper was bypassed and seller-mobile had none. Upgraded the mapper (both apps, byte-identical): friendly FR per category (no-internet/timeout/5xx/unknown), prefer the API's French message for business 4xx, never `.toString()`; added `friendlyErrorMessage()`/`errorCategory()`. Replaced all ~23 raw-error sites; `friendlyErrorMessage` auto-captures only UNEXPECTED errors (5xx/non-Dio) to Sentry with endpoint/method/status/type + userId (id+role) + active-city scope; PostHog auth events (`auth_otp_requested`/`auth_login_success`(method)/`auth_login_failure`(error_category)); hardened `PosthogAnalytics._enabled` to never throw. Decisions: guest cart = gate-on-action (no local cart/merge); error text = prefer-API + friendly generics. Tracker: `docs/mobile-guest-and-errors.md`.
- **Mar 2026** — Redis removed entirely; 5 Docker containers.
- **Mar 2026** — City marketplace upgrade: `City` + `Commune` models, 8 cities (2 active), city-based product filtering, dynamic per-category attribute forms (web + mobile), commune-based addresses.
- **Apr 2026** — Auth refactor: introduced email + password as the email/password path; SMS provider abstraction (Orange DRC default, Africa's Talking fallback, Mock); seller migration flow scaffolded.
- **Apr 25, 2026** — Google OAuth removed entirely (endpoint deleted, deps removed).
- **Apr 25, 2026** — Category SEO slugs (`/categorie/<slug>` canonical, `/categories/<id>` returns 308 redirect). Sample catalog: "Teka RDC Officiel" platform seller + 152 sample products seeded idempotently.
- **May 2026** — Platform reduced to French only; all multi-locale UI plumbing collapsed and DB translatable fields flattened to plain TEXT.
- **May 14, 2026** — `[locale]` URL routing removed entirely from all 3 Next.js apps. Routes flattened (`app/[locale]/x` → `app/x`). `next-intl` middleware integration + `createNavigation` Link/useRouter wrappers + `i18n/routing.ts` + `i18n/navigation.ts` all deleted. `next-intl` kept solely as a French-only string loader for `messages/fr.json`. Component-level `useTranslations()` calls untouched. Pages that read `useSearchParams()` (login flows: reset-password, setup-password, payment-pending) wrapped in `<Suspense>` to satisfy Next.js 15 prerender rules. `next.config.ts` redirects strip both `/en/*` and `/fr/*` legacy locale prefixes to the un-prefixed path.
- **May 12, 2026** — **Phone-OTP auth removed everywhere.** All roles (buyer, seller, admin) authenticate with email + password. `OtpService` + `OtpModule` + `otps` / `otp_rate_limits` tables deleted. Seller migration redesigned to drop the OTP step. `User.phone` is now `String? @unique` (email-only buyers have `null`). New endpoints: `POST /v1/auth/register/buyer`, `POST /v1/auth/buyer/{migrate-check, migrate-link-email, setup-password}`. Removed (404): `/v1/auth/otp/*`, `/v1/auth/register`, `/v1/auth/login`. `SmsService` is notification-only now (orders, payments, broadcasts).
- **May 15, 2026** — **Buyer auth reverted to WhatsApp OTP (via Gupshup).** Sellers + admins keep email + password. `Otp` + `OtpRateLimit` tables restored (sha256-hashed codes, never plaintext). New `WhatsappModule` + `WhatsappService` + `Gupshup/Mock` providers (separate from `SmsService` — notification SMS stays untouched). New `BuyerOtpService` + `BuyerClaimService`. Endpoints added: `POST /v1/auth/buyer/otp/{request, verify, resend}`, `POST /v1/auth/buyer/claim/{request, verify}`. Endpoints removed (return 404): `POST /v1/auth/register/buyer`, `POST /v1/auth/buyer/{migrate-check, migrate-link-email, setup-password}`. Hard cutover, no feature flag — rollback is `git revert`. Email-only legacy buyers (3-day window 2026-05-12 → 2026-05-15) use the new `/reclamer-compte` claim flow to attach a phone. `BuyerMigration` repurposed for the claim flow (new columns: `tempPhone`, `claimEmailSent`). New env vars at root: `WHATSAPP_PROVIDER`, `GUPSHUP_API_KEY/APP_NAME/SOURCE_NUMBER/BASE_URL/OTP_TEMPLATE_ID`. Production keeps `WHATSAPP_PROVIDER=mock` until the Gupshup template UUID is approved (typically 24–48h after submission).
- **May 25–26, 2026** — **Orange + Africa's Talking SMS + Flexpay Mobile Money removal** (10-PR initiative, A1 → D3). Triggered by the 2026-05-24 outage in which an operator stripped `ORANGE_*`/`FLEXPAY_*` env vars from `.env.production` before the API was made tolerant — Joi validation crash-looped the api container for ~2h. Sequencing was code-first, env-strip-second: A1/A2/A3 added Push (FCM) primary + Email (Resend) fallback for order events and per-channel push+email for admin broadcasts; B1/B2 deleted the Mobile Money checkout UI + entire Flexpay provider/webhook/factory and tightened the checkout DTO to `paymentMethod: 'COD'` only; B3 wired the admin broadcasts UI to the channels payload; C1 dropped the 14 stale env keys from Joi validation (operator then stripped `.env.production` safely); C2 deleted `apps/api/src/sms/` entirely (`SmsService`, `SmsModule`, Orange DRC + AT + mock providers, the SMS branch in `BroadcastsService`, the `smsBroadcasts` opt-out), relaxed the broadcast message cap 160 → 500 chars, and removed the SMS checkbox from admin-web; D1 converted sample seed orders/transactions from FLEXPAY/MOBILE_MONEY to COD; D2 swept docs + `.env.example` (this entry); D3 marked initiative complete in `STATUS.md` and archived the plan file. Net effect: ~1450 lines deleted, ~250 added across 50+ files; 16 PRs merged + 8 release PRs. The `PaymentMethod.MOBILE_MONEY` and `TransactionProvider.FLEXPAY` enum values stay on the Prisma schema for historical-row display only; new rows are always COD. Re-introducing SMS or automated payments would mean adding a new provider abstraction from scratch — none currently exists.
- **May 27, 2026** — **Mobile connectivity management** (7-PR initiative). Both Flutter apps gained a centralized 5-state connectivity machine + 4-layer Dio interceptor chain (`OfflineAware → Auth → Retry → Log`), a global connectivity banner, lifecycle pause/resume, SharedPreferences cart persistence, hard-block checkout offline, and rate-limited Sentry capture. Enshrined as **Rule 15**; authoritative reference `docs/mobile-connectivity.md`. The two trees are kept byte-for-byte identical for the connectivity layer.
- **Jun 2026** — **PostHog analytics on buyer-web** (#257). Client-side events, session replay, and feature flags. Setup + conventions in `docs/analytics.md`. No API involvement.
- **Jun 2026** — **Microsoft Clarity on buyer-web + seller-web** (#260). Heatmaps + session recordings, client-side only. Setup in `docs/clarity.md`. Also: `uuid` pinned to `^11.1.1` via pnpm override for GHSA-w5hq-g745-h8pq (#258).
- **Jun 3–4, 2026** — **PostHog platform rollout** (8-PR initiative, PR-1→8; #262–#268 + docs). Extended PostHog from buyer-web-only to **all 6 surfaces**: api (`posthog-node` `@Global AnalyticsModule`/`PostHogService` — server-owned auth/order/payment/admin events, PR-1→3), seller-web + admin-web (cloned the buyer-web provider; PR-5/6), buyer-web custom UI event taxonomy (PR-4), and both Flutter apps (`posthog_flutter`, screen+lifecycle+identity, lockstep; PR-7). One US-Cloud project; `distinctId = user.id` everywhere; identity carries **id+role only** (never phone/email); each event has ONE authoritative owner (server=transactional, client=UI-intent — no duplication); `api_error`/`notification_sent` deliberately excluded. `POSTHOG_API_KEY` wired into the api container via deploy.yml/compose; web keys are build-arg secrets; mobile keys are per-flavor. Decisions: single prod project + dev keys empty (R2 fixed). Authoritative reference rewritten platform-wide in `docs/analytics.md`. CLAUDE.md §2 tech-stack row updated. (Deferred: custom buyer-mobile ecommerce events; server-side flag bootstrap; replay↔Sentry linking.)
- **Jun 7, 2026** — **Initiative #1 (Real Catalog & Merchant Supply) — Phase 1: Seller self-onboarding** (4 sub-PRs #304–#307). Audit first found the live catalog is **100% seeded sample** (Teka Officiel seller `10000000-…-999999`, products `31000000-…`, picsum images; 0 real merchants) and a **dead-end defect**: `register/email` assigns role SELLER but `/sellers/apply` required role BUYER → a fresh merchant got 403 on apply and couldn't list products either. Phase 1 unblocks real merchant onboarding end-to-end. **P1a** (#304): widened the apply guard to `@Roles('BUYER','SELLER')` (product creation stays gated on `applicationStatus==='APPROVED'`, so role-SELLER-without-profile is inert) + service spec. **P1b** (#305): seller-web `/inscription` (registration) + `/devenir-vendeur` (business-application form → `POST /v1/sellers/apply`; city select from `/v1/cities`; phone via `normalizeDrcPhone`) with PENDING/REJECTED states; `dashboard/layout` approval gate redirects unapproved sellers to the application flow. **P1c** (#306): seller-mobile mirror — new `seller_application` feature (repo + screen), `/devenir-vendeur` route + router approval gate, ported `normalizeDrcPhone` byte-for-byte from buyer-mobile (Rule 13 SSOT; seller-mobile had none), and login/register now refresh via `checkAuthStatus()` so auth state carries `sellerProfile` (login response omits it). **P1d** (#307): `reviewSellerApplication` now notifies the seller by **email** (two French Resend templates: approved = welcome+next-steps+dashboard CTA; rejected = reason+resubmit CTA) **+ push** (FCM via `SellerNotificationService`), fire-and-forget. Decisions locked: KYC = ID/RCCM photo + manual review (Phase 2); sample retirement = `isDemo` flag + rank-real-above-demo + SEO-safe per-category phase-out (Phase 3). Out of scope: external RCCM/sanctions integrations, vision-API moderation, variant SKUs, 3rd-party inventory sync. Verified-false agent claim (excluded): "no stock decrement" — checkout decrements atomically (`checkout.service.ts:195`). Tests green throughout (api 51 unit + 92 e2e; seller-web build/lint; seller-mobile analyze/test). **Phase 1 shipped to prod (release #308) + verified.** **Next: Phase 2 (KYC docs) — review-gated.**
- **Jun 7, 2026** — **Initiative #1 Phase 1 — QA pass** (4 fixes #309–#312, released after #308). Post-Phase-1 review surfaced four issues, each fixed as a separate PR. **QA-4** (#309): the admin Vendeurs list rendered `User.phone` — null for email-registered sellers, since the application phone lives on `SellerProfile.phone` — so it showed "—"; now selects + renders `SellerProfile.phone` and the search covers boutique name + seller phone (the placeholder promised both but searched neither). **QA-2** (#310): the verification email links to `${SELLER_WEB_URL}/verify-email?token=` but seller-web had no such route → Next 404; added the `/verify-email` page (Suspense/token pattern) that auto-calls `GET /v1/auth/email/verify` and shows verifying/success/error states. **QA-3** (#311): admins had no signal for pending applications; added `pendingSellerApplicationsCount` to the admin stats → a dashboard "Demandes vendeurs en attente" alert card (shown only when >0) + a count badge on the Vendeurs sidebar item. **QA-1** (#312): added required **Commune** to the application — `SellerProfile.communeId` (nullable column so existing rows stay valid; required in DTO/forms), idempotent prod migration `2026-06-07_seller_commune.sql`; `apply()` validates the commune and **derives `cityId` from it** (city + commune can't disagree); dynamic Commune dropdown filtered by Ville (reset on city change, prefilled on resubmit) on **both** seller-web + seller-mobile; commune names returned in `getApplication` + admin `findUserById`. Decision: commune nullable-column + required-in-form (no legacy backfill). Tests green (api 55 unit + 92 e2e; seller-web build; seller-mobile analyze/test). Shipped to prod (release #313) + verified; commune migration applied.
- **Jun 7, 2026** — **Initiative #1 Phase 2 — Seller KYC** (4 sub-PRs #314–#317). ID/RCCM document photo + **manual** admin review. Locked decisions: **private storage + signed admin URLs**, **single required photo**, no external/automated verification. **P2a** (#314, API): `SellerProfile.idDocumentCloudinaryId` + `idDocumentUploadedAt` (nullable → existing rows valid; required at DTO/form layer; idempotent prod migration `2026-06-07_seller_kyc_document.sql`); `CloudinaryService.uploadPrivateImage` (Cloudinary `type:authenticated` — not publicly accessible) + `getSignedImageUrl` (signed, expiring); `POST /v1/sellers/documents` (multipart, 5MB, image MIME) → returns the private `public_id`; `ApplySellerDto.idDocumentCloudinaryId` **required + folder-constrained** (`^teka-rdc/seller-documents/`, so a client can't point at an arbitrary asset); `apply()` persists it + stamps `idDocumentUploadedAt`; `GET /v1/admin/sellers/applications/:id/document` → admin-only short-lived **signed URL** (404 when absent). **P2b** (#315, seller-web): `/devenir-vendeur` document upload control (compress → `/v1/sellers/documents`), upload/✓/error states, blocks submit until attached, sends `idDocumentCloudinaryId`, prefills prior doc on REJECTED. **P2c** (#316, seller-mobile): mirror — `uploadDocument(File)` repo method + `image_picker` control + same required-before-submit/prefill behavior. **P2d** (#317, admin-web): "Voir la pièce" action in the Vendeurs list → fetches the signed URL → preview modal (+ open-in-new-tab); shown alongside approve/reject. Security: KYC docs are authenticated/private, the raw URL 401s without a signature, admin viewing is a short-lived signed URL generated on demand and never persisted, DTO rejects public_ids outside the private folder. Tests green (api 61 unit + 92 e2e; seller-web + admin-web builds; seller-mobile analyze/test). Shipped to prod (release #318) + verified; migrations applied to prod + dev.
- **Jun 10, 2026** — **Mobile release readiness (Android / Play Store launch prep)** (#365). Makes buyer-mobile + seller-mobile shippable to Google Play — they were stuck signing release builds with the debug keystore. Scaffolds everything except the keystore generation (operator-owned signing secret). `apps/{buyer,seller}-mobile/android/app/build.gradle.kts` gain a release `signingConfig` that loads `android/key.properties` (gitignored) and signs with the upload key, falling back to debug when absent (local dev / internal APK builds unaffected). `scripts/sync-android-signing.sh` decodes the keystore + writes key.properties from env/secrets (mirrors sync-firebase-secrets.sh). New `.github/workflows/release-mobile-aab.yml` ("Release mobile AAB") signs + builds the production release App Bundle, fails fast if signing secrets are missing, asserts the bundle isn't debug-signed, uploads the .aab (per app — buyer/seller have separate keystores). `docs/mobile-release.md` documents the keytool commands, the 8 GitHub Secrets (BUYER_/SELLER_ UPLOAD_KEYSTORE_B64 + KEYSTORE_PASSWORD + KEY_PASSWORD + KEY_ALIAS), version-bump + build steps, and a full Play Store submission checklist. No Dart/web/api changes (gradle + CI + docs only) — no prod deploy needed; the gradle changes take effect when the AAB/APK workflows run. Operator TODO: generate keystores, set secrets, finalize prod google-services.json, run the workflow, upload to Play. Out of scope: iOS (blocked on the deferred iOS scaffold), automated Play Console upload.
- **Jun 10, 2026** — **Initiative #1 (Real Catalog & Merchant Supply) — P3c: demo-catalog retirement** (4 sub-PRs #359–#362; SHIPPED + VERIFIED on prod (release #363; prod seed created the setting rows)). The deferred final piece of Initiative #1: as real merchants populate categories, the seeded demo catalog (152 "Teka RDC Officiel" products, isDemo=true) is hidden + 301-redirected so the storefront converges on real inventory. **Per-category automatic, ships DORMANT.** Decisions: master switch RETIRE_DEMO_CATALOG (default false → zero change to today's demo-only prod); when on, a category auto-retires its demo once it has ≥ DEMO_RETIRE_THRESHOLD real ACTIVE products (store never empties; gradual as merchants onboard); retired demo PDPs 301 → category; hide = exclude via filter (keep isDemo rows, reversible). **P3c-1** (#359, API): getRetiredCategoryIds() (default-safe setting reads + groupBy real counts ≥ threshold) + (isDemo=false OR categoryId NOT IN retired) filter on browseProducts (Prisma + FTS), searchSuggestions, getRelatedProducts; isRetired on getProductDetail; 2 seed settings (idempotent, update:{}); 2 unit tests. Sitemap inherits the filter. **P3c-2** (#360, buyer-web): the PDP server component permanentRedirect()s a retired demo product to /{ville}/categorie/{slug}. **P3c-3** (#361, admin-web): a retirement control panel on the catalog-coverage page (toggle + threshold) + per-category "Démo retiré / En attente du seuil" status. **P3c-4** (#362, docs): retirement model in architecture.md. No migration. Tests: api 81 unit + 108 e2e; buyer/admin-web tsc+build. Release runs the prod seed to create the two setting rows. **Closes Initiative #1's remaining code work — only operational (recruit merchants, then flip the switch) remains.**
- **Jun 9, 2026** — **Initiative #2 (Discovery & Conversion) — Phase D (conversion polish): SHIPPED + VERIFIED on prod (#352–#357; release #358; no migration).** Three buyer conversion features, web + mobile, no migration (pure UI + client state). Decisions: build all 3 in order scarcity→quick-add→recently-viewed; scarcity = exact count; quick-add = always-visible button (qty 1); recently-viewed on home + PDP, client-local only (persisted view-tracking out of scope). **D-1 scarcity** (#352 buyer-web, #353 buyer-mobile): a "Plus que X en stock" warning badge on listing cards + a PDP low-stock line when 0 < qty ≤ 5; buyer-web gained parity with mobile, and mobile's `productLowStock` ARB was parameterized from the generic "Stock limité" to the exact count. **D-2 quick-add** (#354 buyer-web, #355 buyer-mobile): an always-visible add-to-cart control on the listing card (full-width button on web, circular icon button on the mobile card), adds qty 1, hidden/disabled when out of stock, toast/snackbar feedback; reuses the existing cart addItem so the `add_to_cart` analytics event + guest/auth paths are unchanged; ripples across home/category/search/wishlist/related via the shared card. The card click still navigates to the PDP (button is outside the link / wins its own tap). **D-3 recently-viewed** (#356 buyer-web, #357 buyer-mobile): a client-local history (localStorage on web, SharedPreferences on mobile; key `teka_recently_viewed`, most-recent-first, deduped, capped 12) captured on PDP view next to the existing `product_viewed` event, surfaced as a "Vus récemment" strip on the home page + PDP (PDP excludes the current product), reusing the product grid/card; entries are snapshots (tapping opens the live PDP). buyer-web `detailToBrowseProduct` + buyer-mobile `BrowseProductModel.fromDetail`/`toJson` map a PDP detail to the lightweight card shape. Tests: web tsc+build; mobile analyze + 76 tests. **Phase D was the last deferred phase of Initiative #2; A–D now all shipped/queued.**
- **Jun 8, 2026** — **Initiative #3 (Seller Payouts Operationalization) — SHIPPED + VERIFIED on prod (A–E, #343–#350; release #351; migration applied).** Made seller payouts operational end-to-end on the COD platform. **Settlement model resolved (operator):** Teka couriers collect the buyer's COD cash on the platform's behalf → the platform holds it and owes the seller net (gross − commission) → the existing Earning/wallet direction is correct; payouts settle **manually** (no automated provider). The seller request UI had been deliberately disabled until this was settled. **A1** (#343): payout lifecycle completion — admin `POST /v1/admin/payouts/:id/process` (APPROVED→PROCESSING) + `/complete` (APPROVED|PROCESSING→COMPLETED; requires `externalReference`, sets `processedAt`); state-machine guards; first 9 payout unit tests. No `Transaction{PAYOUT}` row (`Transaction.orderId` is required) — the Payout row + externalReference are the ledger. **A2** (#344): seller notifications on approve/paid/reject via push-primary + **email fallback** (money events; sellers always have email) — 3 FR templates + `SellerNotificationService` methods + `pushOrEmailToSeller`. **B1** (#345): reusable destination `SellerProfile.payoutMethod`/`payoutPhone` (idempotent migration `2026-06-08_seller_payout_destination.sql`, applied to dev) + GET/PATCH `/v1/sellers/payout-method`; `requestPayout` body fields now optional, defaulting from the saved destination. **C1/C2** (#346/#347): re-enabled the request flow on seller-web + seller-mobile (saved-destination prefill, min-balance + single-pending guards, lifecycle display incl. transfer reference for COMPLETED / reason for REJECTED). **D1** (#348): admin-web "Marquer en traitement"/"Marquer payé" (reference input) actions + reject-on-APPROVED + reference display; also fixed pre-existing field mismatches (`payout.method`/`phone`/`seller` → `payoutMethod`/`payoutPhone`/`sellerProfile`) that rendered the admin console blank. **D2** (#349): payouts reconciliation CSV (`GET /v1/admin/reports/payouts[/csv]`, streaming + UTF-8 BOM, date+sellerId filters) + a "Virements" tab on the admin reports page. **E** (#350): authz e2e (9 — every money endpoint 401s without auth) + `docs/payouts.md` (authoritative reference + ops runbook) + api-reference/architecture/CLAUDE refreshes. Tests across the initiative: api 79 unit + 108 e2e; seller-web/admin-web tsc+build; seller-mobile analyze+test. **Out of scope (decided):** automated mobile-money disbursement, multi-currency payouts, per-order rider cash-reconciliation ledger.
- **Jun 8, 2026** — **Initiative #2 (Discovery & Conversion) — seller-web attribute-label fix + initiative PAUSED.** Follow-up #341: the seller product form (`dynamic-attributes-form.tsx`) labelled each attribute input with the raw `name`, which is a JSON-encoded `{"fr":…,"en":…}` string (legacy jsonb column) — sellers saw `{"fr":"Marque",…}` instead of "Marque". `getLocalizedName` now extracts the French label (tolerating plain/malformed values), mirroring buyer-web `attrLabel` (#338) + buyer-mobile `_frAttributeLabel` (#339). Merged to develop, pending the next release. **Initiative #2 paused** by the user after Phase C: Phases A (best-seller ranking + "X vendus"), B (Postgres FTS + pg_trgm search/autocomplete + accent/typo follow-up), and C (P-C1 related products + P-C2 attribute facet filters) all shipped to prod and verified; Phase D (conversion polish — scarcity badges, recently-viewed, quick-add) remains deferred. **Notable prod finding:** the sample/demo catalog has empty `specifications` (no attribute values), so facet filtering returns nothing against demo data — real merchant products (specs set via the now-fixed seller form) will be filterable; demo catalog slated for retirement (Initiative #1 P3c). Out of scope (decided): external search engine (Meilisearch/Typesense), ML recommendations, persisted view-tracking.
- **Jun 8, 2026** — **Initiative #2 (Discovery & Conversion) — Phase C / P-C2: attribute (facet) filters** (3 sub-PRs #337–#339; merged to develop, pending release). Decisions: SELECT/MULTISELECT only; plain options (no per-option counts); AND-across-attributes / OR-within-attribute. **P-C2a** (#337, API): new optional `attributes` query param on `GET /v1/browse/products` — a URL-encoded JSON map of `attributeId → [values]`. Resolution is one raw pass over `product_specifications` using Postgres array overlap (`string_to_array(value, ',') && ARRAY[…]::text[]`) so MULTISELECT's comma-joined storage matches token-exact (avoids `"8Go"` ⊂ `"128Go"`); `GROUP BY productId HAVING COUNT(DISTINCT attributeId)=N` enforces the AND. The resolved id-set is injected into BOTH the Prisma (non-search) and raw-FTS (search) branches so they filter identically; an empty set short-circuits to an empty page. Param is parsed + bounded leniently (≤10 attrs, ≤30 values, malformed → ignored). No migration (`ProductAttribute`/`ProductSpecification` already exist + power the seller form). Live-DB SQL verified (single facet resolves; unsatisfiable AND → 0). **P-C2b** (#338, buyer-web): the category `ProductFilters` (desktop sidebar + mobile sheet) renders a checkbox group per SELECT/MULTISELECT attribute, fetched from `/v1/browse/categories/:id/attributes` on category change; selections feed the `attributes` param applied with the existing condition/price/sort on “Appliquer”, reset on category switch + “Effacer”. New `attrLabel()` extracts the French label from the legacy `{fr,en}` jsonb-name (returned as a JSON-encoded string), tolerating plain/malformed values. **P-C2c** (#339, buyer-mobile): the category filter bottom sheet loads the category’s attributes (new repo `getCategoryAttributes` + `FacetAttribute` model) and renders multi-select `FilterChip` groups; facets ride `FilterOptions.attributes`, get JSON-encoded in `category_screen`, and flow via a new **String** `attributesJson` param on `BrowseProductsParams` (kept a String so the Riverpod family key stays value-equatable — a Map would break ==/hashCode); reset clears facets; sheet body made scrollable; `_frAttributeLabel` mirrors web. Search page (`/recherche`) intentionally gets no facets (attributes are category-scoped). Tests green (api 65 unit + 99 e2e; web build; mobile analyze + 76 tests). **Parked:** (1) the *seller* product form renders these same attribute names raw (`{"fr":…}`) — pre-existing bug, follow-up; (2) Phase D conversion polish deferred.
- **Jun 8, 2026** — **Initiative #2 (Discovery & Conversion) — Phase C / P-C1: related products** (3 sub-PRs #333–#335; P-C2 attribute filters deferred). The PDP had no cross-sell. Decision: similarity = same category + price proximity (±40%). **P-C1a** (#333, API): `GET /v1/browse/products/:id/related` — same category, price within ±40% of the source, excluding the current product, ranked real-above-demo → best-seller (`unitsSold`) → recency; tops up with same-category (any price) when the price band is sparse; returns the standard BrowseProduct shape; limit default 8, cap 12; `@Public`; plain Prisma. **P-C1b** (#334, buyer-web): `RelatedProducts` component → "Produits similaires" via the shared `ProductGrid` (wishlist hydration + product card reused) below the PDP reviews; hidden when empty. **P-C1c** (#335, buyer-mobile): repo `getRelatedProducts` + a `_RelatedSection` horizontal product-card row below the PDP reviews (new `relatedTitle` ARB). Tests green (api 65 unit + 96 e2e; web build; mobile analyze/test 74). No migration (pure query/UI). **Deferred: P-C2 attribute filters** (ProductAttribute/ProductSpecification exist + power seller forms, but browse has no attribute filter) + **D conversion polish**.
- **Jun 7, 2026** — **Initiative #2 (Discovery & Conversion) — Phase B: search relevance + autocomplete** (4 sub-PRs #326–#329; C/D deferred). Search was relevance-free `ILIKE` (no ranking, no typo tolerance, no index). Decision: **Postgres FTS + pg_trgm** (raw SQL; no new infra). **P-B1** (#326): migration `2026-06-07_product_search_fts.sql` — a STORED generated `tsvector` over title+description (`french` config) + GIN index; `pg_trgm` extension + trigram GIN index on title. `browse.service` search path ranks via `$queryRaw` (real-above-demo → `ts_rank` → `similarity(title)` → recency; Prisma can't ORDER BY ts_rank), then hydrates by ranked id with the shared select (response shape unchanged); same filters in the raw WHERE; cursor encodes an offset. e2e search tests updated (hydrate-by-ranked-id; replaced the obsolete where.OR test). Real-DB verified on dev (migration + full ranked+filtered query + typo case). **P-B2a** (#327): `GET /v1/browse/search/suggestions?q=&cityId=` → top 5 relevant products (same FTS+trigram ranking, city-scoped) + up to 5 matching categories; empty <2 chars. **P-B2b** (#328): buyer-web `SearchAutocomplete` — debounced dropdown (products + categories), keyboard nav (↑/↓/Enter/Esc), deep-links via productHref/categoryHref; replaced both header search forms. **P-B2c** (#329): buyer-mobile — `getSearchSuggestions` + tappable "Catégories" chip row above the live FTS results grid (products already relevance-ranked from P-B1). Tests green (api 65 unit + 94 e2e; web build; mobile analyze/test 74). FTS migration applied to dev; prod via the Action at release. **Deferred:** C attribute filters + related products; D conversion polish (scarcity badges, recently-viewed). Out of scope: external search engine, ML recs, persisted view-tracking. **Follow-up (#331):** prod verification found search was accent-sensitive ("telephone"≠"Téléphones") + typo-weak ("Tecnoo"≠"Tecno Spark"). Fixed with migration `2026-06-07_product_search_unaccent.sql` (`unaccent` ext + IMMUTABLE `f_unaccent()` wrapper; `search_vector` rebuilt over unaccented text; trigram index on `f_unaccent(title)`) + `browse.service` now matches/ranks on `f_unaccent` and uses `word_similarity` (`<%`) for one-word typos; category suggestions match via raw `f_unaccent ILIKE`.
- **Jun 14, 2026** — **Marketplace Taxonomy, Dynamic Attributes, Brands & Catalog Reset** (8-phase initiative, PRs #368–#383; **SHIPPED TO PROD via release #384**). Replaced the catalog with a **strict 2-level taxonomy** (7 categories → 80 subcategories, `apps/api/prisma/taxonomy-data.ts`), a first-class **Brand library**, per-subcategory **dynamic attributes** (incl. BOOLEAN), and pre-launch **catalog-reset tooling**. **P1** (#368): schema foundation — `Brand` + `BrandCategory` models, `Product.brandId` (`ON DELETE SET NULL`), `BOOLEAN` AttributeType, orphan-FK hardening (cart/reviews/wishlist productId RESTRICT→CASCADE; order_items left RESTRICT); idempotent migration `2026-06-14_taxonomy_brand_foundation.sql`. **P2a** (#369): `CatalogResetService` + `pnpm db:reset-catalog` CLI (dry-run/`--confirm`, skips order-referenced products, purges Cloudinary after DB commit) + admin single-product hard-delete. **P2b-1** (#370): strict taxonomy + 160 attribute templates + 51-brand library seeded as the only active tree (older taxonomies deactivated + slug-nulled; collision-safe slugs). **P2b-2** (#371): demo catalog (152 products) remapped onto strict subcats + brands + specs. **P3** (#372/#373): brand CRUD/merge/activate API + admin-web `/dashboard/brands` UI. **P4** (#374/#375): attribute admin — BOOLEAN + chip option editor + reorder endpoint/UI. **P5** (#376/#377/#378): seller product create/edit — `brandId` on product API + brand dropdown & BOOLEAN field on seller-web + seller-mobile. **P6** (#379/#380/#381): buyer brand filter (`brandIds` on browse, Prisma + FTS) + BOOLEAN attribute facet on buyer-web + buyer-mobile. **P7** (#382): product JSON-LD emits the real brand. **Decisions:** D1 first-class `Product.brandId`; D2 keep JSON attribute options; D3 re-seed demo catalog. Seeded ids are non-RFC4122 → DB-lookup validation, not `@IsUUID`. **P8** (#383): docs + prod runbook. **Prod rollout — SHIPPED 2026-06-14 via release #384:** `develop→main` merge → deploy (api + 3 web) → applied migration `2026-06-14_taxonomy_brand_foundation.sql` (Apply-prod-migration Action) → ran prod seed (Run-prod-seed Action: deactivated 116 prior cats, seeded 7 cats/80 subs + 160 attrs + 51 brands/121 links + 152 demo products). Catalog reset **skipped** (reconciling seed handled the demo catalog; nothing irreversible run). **Verified live on prod:** 7 active top cats + 80 subs, 51 brands, Smartphones→12 brands, brand filter (Prisma + FTS), BOOLEAN attr "Sous garantie", PDP JSON-LD `"brand":{"@type":"Brand","name":"Tecno"}`, sitemap strict slugs, admin/seller/buyer pages 200. Tracker: `tasks/marketplace-taxonomy-progress.md`; architecture: `docs/architecture.md` → "Marketplace taxonomy + brands".
- **Jun 18, 2026** — **Admin product notifications + seller session/auth isolation** (2 PRs: #385 `feat/seller-session-isolation`; `feat/admin-product-notifications` stacked). Two prod defects from live testing. **Workstream A (session/auth, PR #385):** (1) sellers were logged out after creating a product — `AuthService.refreshTokens()` treated a just-rotated (already-revoked) refresh token replayed during a benign race (15-min access token + rotate-on-refresh + shared `.teka.cd` cookie) as a stolen-token replay and called `revokeAllUserTokens()`. Fix: a **15s rotation grace window** (token revoked <15s ago = benign → re-issue without revoke-all; beyond grace still trips the replay defense; uses existing `revokedAt`, no migration). (2) buyer/seller/admin shared one cookie namespace on `.teka.cd` — now **per-surface cookies** `teka_{admin,seller,buyer}_{access,refresh,session}`, resolved via an `X-Teka-Surface` header each web api-client sends (`auth/surface.util.ts`; surface-aware JWT extractor + cookie set/clear/refresh; CORS allowlist); **logout scoped** to the current session `jti`. Also: `apiFetch` made FormData-aware + all authenticated raw fetches (image-uploader, avatar ×3, KYC doc, admin CSV) routed through it/given the header; `fetchUser` only nulls on a genuine 401; Sentry/log breadcrumbs at replay/race/logout (no PII). Mobile (bearer auth) untouched. **⚠ cookie rename forces a one-time re-login** for active web users on deploy. **Workstream B (admin product notifications):** `submitForReview()` emitted nothing — admins had to poll the moderation queue. New `AdminNotification` model + enum (idempotent migration `2026-06-18_admin_notifications.sql`, applied to dev) + `AdminNotificationService` (Prisma-only, fire-and-forget) emit `PRODUCT_SUBMITTED`; `pendingProductsCount` in admin stats; `GET /v1/admin/notifications(+/unread-count)` + `PATCH .../:id/read(+/read-all)`. admin-web: Produits sidebar badge + dashboard alert (deep-link to `?status=PENDING_REVIEW`) + header `NotificationBell`. Tests: `auth.service.spec.ts` (7), `admin-notifications` e2e (4), stats `pendingProductsCount`; green api 115 unit + 112 e2e, buyer-web 51, admin-web build. Reference: `docs/session-management.md`; architecture admin-notification flow.
- **Jun 21, 2026** — **Buyer-web visual redesign — Phase 1** (branch `feat/buyer-web-redesign`; tracker `docs/buyer-web-redesign.md`). Modernized teka.cd buyer-web. Audit headline: the design is fully token-driven (zero hardcoded hex in components) so the new palette is a low-risk token swap, and the SEO/analytics layer is cleanly separated from the presentational layer. **Decisions (user-validated at a palette+hero checkpoint):** **Modern Ruby** red (canonical `--color-primary-600` `#BF0000`→`#C8102E` — cleaner/brighter, AA-safe white text, used as accent not flood); **town accents** copper `#B87333` (Lubumbashi) / cobalt `#1A56DB` (Kolwezi), subtle (city badge/chips only, brand stays unified) via `lib/city-accent.ts`. **Changes (presentational only):** P1.0 palette + accent tokens (`globals.css`); P1.1 hero — replaced the all-red gradient fallback with a light, conversion-focused hero (town-accent location badge + value prop + CTA + trust strip: fast local delivery / cash-on-delivery / verified sellers); P1.2 categories — larger rounded-2xl cards, emoji-in-tinted-circle, 2/3/4/6 grid; P1.3 product cards — bold dark CDF + secondary USD price (red reserved for the add-to-cart CTA, not flooded), only badge "Occasion" (USED); P1.4/P1.5 header city selector (desktop+mobile) + CityPrompt chips wear the town accent; P1.6 SectionHeader accent bar + bigger quick-add touch target. **Preserved:** all `track()` call sites (`add_to_cart` via cart-store), `thumbnailUrl`/`productHref`/wishlist wiring, server components / metadata / JSON-LD / sitemap / robots / `urls.ts` / analytics / Sentry — **untouched** (verified by diff). No API/DB change. Browse API has no rating/discount fields → those card elements not added (would require an API change). **Dev fix:** local DB has legacy picsum.photos seed images → `next.config.ts` now allows that host in **development only** (prod images are all Cloudinary) so buyer-web dev doesn't crash on next/image — pre-existing issue, not the redesign. buyer-web type-check + production build green. **Deferred:** Phase 2 (buyer-mobile alignment), Phase 3 (seller/admin brand consistency).
- **Jun 18, 2026** — **Seller Product Management UX** (2 PRs: #392 `feat/searchable-category-selector`; `feat/seller-notifications` stacked). Two usability gaps from live testing. **F1 — searchable category selector (#392):** the strict 7→80+ taxonomy made the product Category `<select>` unwieldy. Both surfaces already load the full tree client-side, so pure client-side filtering — **no API/DB change**. seller-web: new `CategoryCombobox` (replaces the native select in product create + edit) — search-as-you-type, **accent-+case-insensitive** (`tele`→Téléphones, `chauss`→Chaussures), filters category AND subcategory names (subs show parent context), keyboard nav (↑/↓/Enter/Esc), grouped+indented when empty; no combobox lib (repo idiom). seller-mobile: search `TextField` added to the existing category bottom sheet (flat accent-insensitive matches while typing, tree when empty). **F2 — seller product approval/rejection notifications (stacked):** sellers had only ephemeral push. New **per-user `UserNotification`** model + enum (`PRODUCT_APPROVED`/`PRODUCT_REJECTED`; idempotent migration `2026-06-18_user_notifications.sql`, applied to dev) — distinct from the global admin inbox, every read/write scoped to `userId` (`markRead` filters `{id,userId}` → no IDOR). `UserNotificationService` + `GET /v1/seller/notifications`(+`/unread-count`) + `PATCH .../:id/read`(+`/read-all`), `@CurrentUser`-scoped. `SellerNotificationService.notifyProductApproved/Rejected` now **persist an in-app row + push-primary + email-fallback** (new product-approved/rejected FR Resend templates) instead of push-only; **approve and reject both** notified (reject email carries the full reason). seller-web notification bell (sidebar header, mirrors admin); seller-mobile new `notifications` feature (model+repo+Riverpod `StateNotifier`, `/notifications` screen with pull-to-refresh + unread dot + mark-read + deep-link to product, home app-bar bell + unread Badge). **Decisions:** new per-user model (not generalizing the just-shipped admin feed — they're different surfaces; reusable for buyers later); approve+reject; in-app + push + email-fallback (payout pattern). Tests: `user-notification.service.spec` (6 — scoping/security), `seller-notification.service.spec` (4 — persist + push/email fallback), `seller-notifications` e2e (4 authz); green api 129 unit + 116 e2e, seller-web type-check+build, seller-mobile analyze + tests. Mobile mirroring N/A (buyers don't create products / get approval notifications). Architecture: `docs/architecture.md` → notification stacks.
- **Jun 7, 2026** — **Initiative #2 (Discovery & Conversion) — Phase A: best-seller ranking + social proof** (3 sub-PRs #322–#324; B/C/D deferred). `sortBy=popularity` was a stub (fell back to `createdAt desc`, `// TODO`). **A1** (#322, API): denormalized `Product.unitsSold Int @default(0)` + index (mirrors avgRating/totalReviews); idempotent migration `2026-06-07_product_units_sold.sql` (column + index + backfill `SUM(OrderItem.quantity)` over DELIVERED orders); `deliverOrder` increments each line item's unitsSold atomically inside the existing delivery transaction (no RETURNED code path exists → no decrement, consistent with backfill); `popularity` sort → `[{isDemo:'asc'},{unitsSold:'desc'},{createdAt:'desc'}]` (real-above-demo → best-sellers → recency tiebreaker); `unitsSold` exposed in browse list (detail already returns it). **A2** (#323, buyer-web): "{count} vendu(s)" (ICU plural) under the price on product card + PDP, only when `unitsSold>0`. **A3** (#324, buyer-mobile): mirror — parse unitsSold on both models + render on card + detail (new app_fr.arb `productUnitsSold`). Decision: all-time denormalized counter (vs on-the-fly aggregate / time-windowed trending). Tests green (api 65 unit + 92 e2e; buyer-web build; buyer-mobile analyze/test 74). units-sold migration applied to dev; prod via the Action at release. **Deferred:** B search relevance (Postgres FTS + pg_trgm) + autocomplete; C attribute filters + related products; D conversion polish.
- **Jun 7, 2026** — **Initiative #1 Phase 3 — Sample-catalog coexistence** (2 sub-PRs #319–#320; P3c deferred). The live catalog is 100% the seeded "Teka RDC Officiel" demo seller (0 real merchants). Locked decisions: build P3a+P3b now, defer P3c; retirement (later) = hide + 301-to-category. **P3a** (#319): `Product.isDemo Boolean @default(false)` + index (idempotent migration `2026-06-07_product_is_demo.sql` — column + backfill the demo seller's rows by `sellerId 10000000-…-999999`); seed marks sample products `isDemo:true`; `browse.service` ranks real above demo on every sort (`orderBy [{isDemo:'asc'}, <chosen sort>]` — browse/search/homepage all hit `/v1/browse/products`). Flag on `Product` (denormalized) since that's what ranking + coverage query. **P3b** (#320): `GET /v1/admin/stats/catalog-coverage` (per-MAIN-category ACTIVE product counts split by isDemo, rolled up from leaf subcats to root; empty roots surface at zero) + admin-web `/dashboard/catalog-coverage` page (Catégorie / Produits réels / Démo / Statut "Couvert") + sidebar item — so ops see which categories have real merchant supply. **P3c — SEO-safe per-category retirement (hide demo from browse/sitemap + 301 the product URLs to the category, avoiding 404s on ~152 indexed URLs) — DEFERRED** until real merchants populate categories (only operable/testable with real coverage; P3a ranking already prevents demo from burying real products meanwhile). Decision: ranking is real-above-demo as the **primary** order key (not just a tiebreaker) so real inventory is never buried during coexistence. Tests green (api 64 unit + 92 e2e; admin-web build; browse.service.spec + admin-stats.service.spec). isDemo migration applied to dev; prod via the Action at release.
- **Jun 7, 2026** — **Delivery-fee preview quick win** (single PR `feat/delivery-fee-preview`). The delivery fee was already computed correctly server-side at checkout, but the **preview** was broken: buyer-web called `/v1/delivery-zones/estimate` with no `fromTown` (→ 400 → fee shown as `0`), and buyer-mobile showed a hardcoded `--` with the order total **omitting the fee entirely**. Fix: new `POST /v1/checkout/quote` endpoint backed by a shared `CheckoutService.resolveDeliveryFee(sellerId, toTown, tx?)` extracted from `checkout()` — so the previewed fee equals the charged fee **by construction** (same code path, same `DeliveryZonesService.estimateFee(fromTown, toTown)`, `fromTown` from the seller's city/location, fallback `Lubumbashi`). buyer-web `/paiement` now POSTs the quote on address-select and maps `sellerQuotes[].deliveryFeeCDF`; buyer-mobile gained a `CheckoutQuote` model + `getQuote()` repo method + `deliveryFeeCDF`/`isLoadingQuote` on `CheckoutState` (fetched on auto-select/select/create, stale-response-guarded) and now renders the real fee + `subtotal + fee` total. Tests: 4 API unit specs (empty-cart/missing-address throw, per-seller breakdown summed + `estimateFee` arg parity, `fromTown` fallback) + 3 mobile model/parity specs. **Non-goals held:** no City/Commune delivery-zone redesign, no logistics ops, no pricing-model change. **Zone-records fix (#302 → release #303, applied to prod):** the prod `delivery_zones` table was **empty** because `seedDeliveryZones` only ran in the dev-only Phase 4 block — *after* the `if (isProd) return` in seed.ts — so all orders fell back to the **5,000 CDF default**. Fix shipped both layers: (a) extracted module-level `seedDeliveryZones()` called **before** the prod return so it runs in dev + prod (idempotent upsert, now updates feeCDF/feeUSD on conflict); (b) idempotent migration `apps/api/prisma/migrations/manual/2026-06-07_seed_delivery_zones.sql` (`INSERT … ON CONFLICT (fromTown,toTown) DO UPDATE`, exact seed values + deterministic IDs) applied to prod via the "Apply prod migration" Action (`INSERT 0 12`). **Verified live:** all 4 launch routes return seeded fees with `isDefault:false` — Lub→Lub 3,000 · Kol→Kol 3,000 · Lub↔Kol 15,000. Checkout quote + final order pricing consume the same `estimateFee` path, so both now use seeded values, not the default.
- **Jun 7, 2026** — **Mobile Parity Sweep** (5-phase initiative, #293–#297). Audited buyer-mobile + seller-mobile vs recent web/API changes; found the apps **already largely at parity** (auth, cart, checkout, orders, profile, Sentry, connectivity, seller-mobile all solid) and closed only the genuine gaps. **P1** (#293): category + search now pass `cityId` (home already did) → city-scoped listings; product/city models parse the new `slug`/`shortCode`/`city*` fields. **P2** (#294): wishlist header **count badge** via `GET /v1/wishlist/count` + optimistic ±, and inactive/deleted-product add now surfaces the real API reason. **P3** (#295): buyer-mobile fires the full **client ecommerce PostHog event set** (`product_viewed`, `category_viewed`, `search_performed`, `add_to_cart`, `remove_from_cart`, `checkout_started`; query scrubbed) — closed the documented deferral; `docs/analytics.md` updated. **P4** (#296): a buyer OTP that resolves to a **SELLER** account is now blocked with a "use the seller app" dialog (+ session cleared) — mobile can't deep-link the seller APK. **P5** (#297): removed seller-mobile's dead `/auth/migrate` button; buyer OTP uses the server `cooldownSeconds`. **Decisions:** D1 keep mobile auth-required + blocking city gate (web guest flows N/A on native). **Deferred (backlog):** seller-mobile dashboard stats breakdown, PDP related-products. Closeout also slimmed **CLAUDE.md** 38.6k→~29.5k (moved feature spec + phase table to `docs/product-spec.md`). Tracker: `tasks/mobile-parity-sweep-progress.md`.
- **Jun 6, 2026** — **SEO / city-first URL refactor** (7-phase initiative, #283–#289; **shipped to prod release #290**; follow-up stale-token `/connexion` bounce fix #291→#292). buyer-web moved to a **city-first** URL scheme: `/{ville}`, `/{ville}/{slug}-{shortCode}` (product), `/{ville}/categorie/{slug}` (city-scoped category). The product DB `slug` became **clean/cosmetic, city-free, non-unique**; a new `Product.shortCode` (6-char base36, `@unique`) is the resolver, and `City.slug` is the `{ville}` segment (idempotent prod migration `2026-06-06_city_first_urls.sql` adds the columns + md5/slug backfill). Browse resolver = UUID→shortCode→slug. Storefront routes renamed to **French** (`/cart→/panier`, `/checkout→/paiement`, `/orders→/commandes`, `/wishlist→/favoris`) with 301/308 redirects (incl. legacy flat product URLs → canonical via the `/[ville]` dispatcher). Homepage **de-blocked** (no forced city modal — fully crawlable). SEO surface (canonical = city URL, sitemap city-paths, JSON-LD Accueil→Ville→Catégorie→Produit, robots) all city-aware. Single URL builder `apps/buyer-web/src/lib/urls.ts`. **Mobile + analytics untouched by design** (ID-based go_router, no `teka.cd` intent-filters; analytics impact is dashboard-only). Decisions: D1 city-scoped categories, D2 clean-slug+shortCode, D3 buyer-web-only renames. Authoritative reference: `docs/url-and-seo-strategy.md`. Tracker: `tasks/seo-city-url-refactor-progress.md`.
- **Jun 5, 2026** — **Wishlist (Favorites) completion & hardening** (6-PR initiative; #272–#274 web/API merged + PR-4/5/6 mobile/docs). The feature already existed end-to-end but was incomplete; audited all 3 surfaces then completed to ecommerce best-practice. **API:** `GET /v1/wishlist/count` (active-filtered, for the header badge); add now rejects non-ACTIVE/deleted products (was selectable); `/check` UUID-filtered; new service unit spec (11) + 2 e2e auth contracts. **buyer-web:** `wishlist-store` (ids Set + batch `/check` hydrate → no per-card N+1; optimistic toggle); heart on ALL listing surfaces (ProductCard home/category/search + flash-deals, sibling overlay); header count badge (`WishlistBadge`); `/wishlist` add-to-cart (keeps item) + stock status + error/retry; **guest heart → `/connexion?redirect=` → auto-completes the pending add after login** (safe-redirect guarded). **buyer-mobile:** heart overlay on product cards + `loadWishlistIds` hydration; seller name on the wishlist card; add-to-cart on the wishlist card. **Analytics:** kept `wishlist_added`/`wishlist_removed`, added `wishlist_viewed` + `wishlist_item_moved_to_cart` on both web + mobile (no PII). Decisions: guest auto-continue; keep shipped event names; add-to-cart keeps the item. Authz/IDOR + dedup verified. `docs/api-reference.md` + `docs/analytics.md` updated.

### Initiative — Mobile connectivity management (2026-05-27, 7 PRs: #243 → #255)

**Brief**: Both Flutter apps (`buyer-mobile` + `seller-mobile`) needed network-state awareness for DRC's 2G/3G reality — frequent drops, captive portals at cafés, shared cell-towers that flake under load. Letting Dio time out after 30s meant users tapped "Payer" three times and rage-quit. This initiative built the layer: state-aware, retry-where-safe, fail-fast-where-not, surface-state-visibly.

**Authoritative reference**: `docs/mobile-connectivity.md`. CLAUDE.md Rule 15 enshrines the hard rules.

**Locked decisions (2026-05-27)**:
- Reachability probe is `${ApiConstants.baseUrl}/v1/health` — no `internet_connection_checker_plus` dep. The probe target equals what the API considers "alive."
- Cart persists to SharedPreferences. Cart **mutations** when offline are blocked (no queue-and-replay) — replay would race price + stock during the offline window.
- Order placement when offline is hard-blocked at the checkout review step (button + permanent French banner). No queue-and-replay anywhere.
- The two Flutter trees are kept byte-for-byte identical for the connectivity layer; changes ship in lockstep.
- Cache wiring is opt-in. Today only the cart is wired; product/category/profile/seller-order keys reserved.

**PR sequence (each feature-PR has a paired develop→main release-PR; smoke after every deploy)**:

- **PR1 — Core foundation** (#243 → #244). `ConnectivityStatus` enum + `ConnectivitySnapshot`. `ConnectivityService` with injectable `interfaceStream` (defaults to `connectivity_plus`) + injectable `probe` (defaults to Dio GET `/v1/health`, 3s timeout). 30s healthy cadence; 5s `noInternet` cadence; **2-slow-probes-to-unstable / 1-fast-out** hysteresis. Riverpod providers (`connectivityServiceProvider`, `connectivitySnapshotProvider`, `connectivityStatusProvider`, `isOnlineProvider`, `isOfflineProvider`). `connectivity_plus ^6.0.0` added. 10 unit tests. CI surprise: `flutter analyze --fatal-warnings` flags warning-level issues that local analyze logs as info — must remove unused fields not just deprecate them.
- **PR2 — Dio integration** (#245 → #246). `RetryInterceptor`: full-jitter backoff `[0..500ms, 0..1500ms, 0..4000ms]` capped 5s, GET/HEAD only or `Options(extra: {retryable: true})` opt-in. Retries on `connectionTimeout` / `receiveTimeout` / `sendTimeout` / `connectionError` + HTTP 502/503/504. `OfflineAwareInterceptor`: synthesizes `DioException(type: connectionError, error: 'offline', message: 'Pas de connexion internet — la requête ne peut pas être envoyée.')` on non-safe verbs when disconnected. `Options(extra: {allowOffline: true})` opt-out. **Chain order**: `OfflineAware → Auth → Retry → Log` (load-bearing — retries must flow through auth attach). `AuthInterceptor` now distinguishes connectivity-caused refresh failure (`connectionTimeout` / `receiveTimeout` / `sendTimeout` / `connectionError` / `502/503/504`) from real 401s — tokens preserved on the former. No more "logged out on the bus" footgun. 19 unit tests using hand-rolled `HttpClientAdapter` mocks.
- **PR3 — Global UI banner** (#247 → #248). `ConnectivityBannerHost` mounted via `MaterialApp.router.builder`. Red `disconnected`, orange `noInternet` / `unstable` / `reconnecting`, green `restored` (3s only). Key state: `_previousOfflineStatus` only tracks `disconnected` + `noInternet` — `reconnecting → connected` on app startup must NOT flash a green banner. `AnimatedSize` 200ms + `AnimatedSwitcher` 200ms. 5 new `app_fr.arb` keys + regen of `app_localizations*.dart`. Widget tests need a `_settleEmission` helper (pumps 2 frames + 350ms) to clear AnimatedSwitcher cross-fade. 8 widget tests.
- **PR4 — Lifecycle integration** (#249 → #250). `ConnectivityLifecycleObserver`: `ConsumerStatefulWidget + WidgetsBindingObserver`. `paused / inactive / hidden → service.pause()` (stops probe timer; saves battery). `resumed → service.resume()` (immediate probe + restart timer). Deliberately does **not** invalidate authProvider on resume — PR2's auth interceptor already handles "tokens still valid, just couldn't reach the server." 8 widget tests using `_SpyConnectivityService` with overridden pause/resume.
- **PR5 — Offline behavior** (#251 → #252). `TypedCache`: SharedPreferences-backed envelope `{v:1, savedAt: ISO, ttlMs, value}`. Methods: `write<T>`, `read<T>`, `evict`, `evictPrefix`. Returns `CacheEntry<T>` with `isFresh` + `age` getters. Stale-while-reconnecting: always returns value if present. `main.dart` preloads `SharedPreferences` then `ProviderScope(overrides: [sharedPreferencesProvider.overrideWithValue(prefs)])` — previously threw `UnimplementedError`. `CartNotifier` hydrates synchronously from cache (`_hydrateFromCache()` on construct) + write-through (`_persistToCache()` on success). 30-day TTL. Silent on offline `DioException` — connectivity banner already tells the story. Checkout review step watches `isOfflineProvider`: place-order button disabled + permanent banner "Connexion requise pour passer commande" with `Icons.wifi_off_outlined`. 9 unit tests using `SharedPreferences.setMockInitialValues`.
- **PR6 — Sentry monitoring** (#253 → #254). `ConnectivitySentryReporter`: subscribes to connectivity stream, sets `connectivity_state` tag + structured `connectivity` context on every snapshot via `Sentry.configureScope`. Breadcrumb (category `connectivity`) on every transition. Rate-limited (1/min/category) captures on `connected → noInternet` (real degradation, not clean drop) + `sustained_noInternet` (≥5 consecutive). `RetryInterceptor` got `_maybeCaptureBudgetExhaustion()` (also 1/min) for full retry-budget exhaustion. Read-once-instantiate Riverpod pattern matching `pushControllerProvider`. **Privacy**: only connectivity status + latency + short error tag + HTTP method + URL path. No query strings, no payloads, no tokens, no phones. The existing `core/config/sentry_scrub.dart` phone-scrubber is untouched. Switched from deprecated `setExtra` to recommended `setContexts` to silence info-level lint warnings.
- **PR7 — Docs + cleanup** (#255 → #256). New **`docs/mobile-connectivity.md`** — single landing page: ASCII state diagram, retry policy table, offline behavior matrix, observability signals, "adding a new feature" checklist, deferred work list, file map. **CLAUDE.md Rule 15** — hard rules: never bypass the Dio chain, never `retryable: true` on non-idempotent calls, never call SMS/OTP/payment vendors directly from app code, mirror buyer-mobile ↔ seller-mobile in the same PR, surface errors with the shared helper, never log creds/tokens/phones, state mutations are hard-blocked offline. **`docs/architecture.md`** gains a "Mobile network-resilience layer" subsection in Data Flow. **Cleanup**: 6 byte-identical copies of `_extractErrorMessage(DioException)` in buyer-mobile feature providers (catalog / checkout / cart / orders / wishlist / reviews) deduped into a single shared top-level `extractDioErrorMessage()` at `apps/buyer-mobile/lib/core/network/dio_error_messages.dart`. Net cleanup: −108 lines. Stale code comments in both `offline_aware_interceptor.dart` files updated to reference the new helper. STATUS.md closeout (active initiative → None) + plan archived locally to `~/.claude/plans/archive/mobile-connectivity-management.shipped.md`.

**Files added** (mirrored byte-for-byte in seller-mobile):
```
apps/buyer-mobile/lib/core/connectivity/
├── connectivity_status.dart                 enum + ConnectivitySnapshot
├── connectivity_service.dart                state machine + probe loop
├── connectivity_provider.dart               Riverpod providers
├── connectivity_lifecycle_observer.dart     WidgetsBindingObserver bridge
├── connectivity_sentry_reporter.dart        observability
└── widgets/
    └── connectivity_banner.dart             global banner widget

apps/buyer-mobile/lib/core/network/
├── offline_aware_interceptor.dart           fail-fast for non-safe calls when disconnected
├── retry_interceptor.dart                   full-jitter exponential backoff
└── dio_error_messages.dart                  French error-string helper (PR7 dedup)

apps/buyer-mobile/lib/core/cache/
├── typed_cache.dart                         SharedPreferences-backed cache
└── cache_keys.dart                          reserved key constants
```

`auth_interceptor.dart` + `api_client.dart` were modified, not added.

**Verification**:
- Tests: 54/54 buyer-mobile specs across `connectivity_service_test.dart` (10), `retry_interceptor_test.dart` (9), `offline_aware_interceptor_test.dart` (10), `connectivity_banner_test.dart` (8), `connectivity_lifecycle_observer_test.dart` (8), `typed_cache_test.dart` (9).
- `flutter analyze --no-fatal-warnings`: 18 baseline issues buyer-mobile / 14 baseline seller-mobile, no new issues introduced by any PR.
- CI: 7/7 checks green on every PR (Lint & Type Check, Analyze (actions), Analyze (javascript-typescript), API Tests, Flutter Analysis ×2, CodeQL).
- Post-release smoke (2026-05-27 20:11 UTC): API health 200, db ok, uptime 413s; all 6 removed endpoints still 404; all public read endpoints respond <1s; all 3 web surfaces 200.
- **Emulator walkthrough** (2026-05-27, `emulator-5554` Android): production-flavor APK installed, exercised every visible signal — orange "Connexion instable" on slow probe; red "Pas de connexion internet" + inline French error after killing wifi/data (verifying `OfflineAwareInterceptor` synthesis + `extractDioErrorMessage` rendering); green "Connexion rétablie" caught in its 3s window; clean resume from HOME→relaunch (PR4 pause/resume). PR5 cart persistence + PR6 Sentry capture not exercised (would need real login + a configured Sentry DSN — both out of scope for a CLI walkthrough).

**Net effect across the initiative**: ~1300 lines added, ~150 removed across 50+ files. 14 PRs total (7 feature + 7 release).

**Decisions captured for future maintainers** (also in STATUS.md + `docs/mobile-connectivity.md`):
- The two Flutter trees are kept byte-for-byte identical for the connectivity layer. PR diffs that touch one and not the other should be rejected at review.
- No queue-and-replay anywhere (cart, checkout, anything stateful). The offline window can be arbitrarily long; replay would race price + stock.
- Cache wiring is opt-in. `productsList` / `categoriesTree` / `userProfile` / `sellerOrdersList` keys are reserved in `cache_keys.dart` but not yet used — future opt-in pass.
- Re-introducing SMS still requires a written ADR (Rule 11 unchanged). Re-introducing automated payments would need a new provider abstraction from scratch.

**Ship cycle**: 7 feature PRs (#243, #245, #247, #249, #251, #253, #255) each followed by a release PR develop→main (#244, #246, #248, #250, #252, #254, #256). Every deploy ran clean; prod was healthy after each.

---

### Initiative — seller-mobile multilingual cleanup (2026-05-20, PR #123 → #124)

**Brief**: Mirror of PR #117 (buyer-mobile cleanup) for the seller-mobile app. After the jsonb→text DB migration (PR #112), the API serves plain French for every translatable field; the defensive `{ 'fr': value }` Map wrapping in 4 seller-mobile models was preserving a shape the API no longer returns.

Also drops bilingual fields from the create-promotion form (same fix that shipped to the product form in PR #120).

**Models simplified (4)**:
- `features/products/data/models/attribute_model.dart` — AttributeModel.name: Map → String. Drop `getLocalizedName`.
- `features/products/data/models/product_model.dart` — CategoryModel.name; SellerProductModel.title + description; ProductSpecificationModel.attributeName simplified. Drop `getLocalizedName`, `getLocalizedTitle`, `getLocalizedDescription`, `parseTranslatable`.
- `features/promotions/data/models/promotion_model.dart` — PromotionProduct.title; PromotionModel.title + description. Drop `getLocalizedTitle` x2, `getLocalizedDescription`, `parseTranslatable`.
- `features/orders/data/models/order_model.dart` — OrderItemModel.productTitle: Map → String. Drop `getLocalizedTitle`, `parseTranslatable`. Inline nested-product fallback as `.toString()`.

**Call sites updated (8 files, ~15 sites)**: home_screen, products_list_screen, product_detail_screen (4 calls), category_selector (4 calls), dynamic_attribute_field, create_promotion_screen (bilingual form removal + Map-building drop), promotion_card, order_detail_screen (`item.title` → `item.productTitle`), seller_reviews_screen, product_form_screen.

Unused `locale` / `l10n` locals removed from 5 sites.

**Verification**:
- `flutter analyze` (seller-mobile): 32 pre-existing `deprecated_member_use` + `use_build_context_synchronously` infos. Zero errors, zero warnings from this refactor.
- CI: lint, type-check, API e2e, both Flutter analyses, all docker-build-check stages — all green
- Net diff: -208 / +47 lines across 14 files

**Ship cycle**: PR #123 → develop ✓ → PR #124 → main ✓ → deploy `26175543769` succeeded → back-merge main → develop ✓. Web prod unaffected (no web files changed); next mobile APK build picks up the simplifications.

### Initiative — Seller-platform cleanup (2026-05-20, PR #120 → #121)

### Initiative — Seller-platform cleanup (2026-05-20, PR #120 → #121)

**Brief**: Full review of seller-web + seller-mobile against the May 2026 platform changes (messaging removal, COD-only buyer payments, monolingual French, jsonb→text). Audit-first; targeted fixes. Profile management deferred as its own initiative.

**Audit method**: Two parallel Explore subagents (one per surface), then cross-platform drift synthesis. Found 5 confirmed bugs and 1 dead-types pile.

**Bugs fixed (all silent in prod until now)**:

1. **seller-web `/dashboard` stat cards always 0** — read `res.data.meta.total` but API returns `data.pagination.total`. All 4 cards (Total / Active / Pending / Drafts) silently rendered 0 even when sellers had products. Plus `loadReviewStats()` read `res.data.products` instead of `res.data.data` (rating calc always returned 0/0). Plus dead `/v1/messages/unread-count` API call + dead `/dashboard/messages` link (both gone since the 2026-05-17 messaging removal).

2. **seller-web `/dashboard/earnings` lists silently empty** — `/sellers/earnings` and `/sellers/payouts` endpoints flatten their envelope to `{ success, data: [...], meta: {...} }` (see payouts.controller.ts:88,118) instead of the canonical nested shape. The page was reading `res.data.earnings`/`payouts` and `res.data.meta` — both wrong. Tabs silently showed "Aucun gain" / "Aucun virement" for everyone.

3. **seller-mobile `orders_repository.dart:46`** — `data['meta']` → `data['pagination']`. Same bug pattern as the products fix in PR #108.

4. **seller-mobile `product_form_screen.dart`** — form still had `_titleEnController` + `_descriptionEnController` and submitted `{ fr, en }` Map to a DTO that expects plain `string`. Any submit would fail validation. Fix: drop EN fields + their dispose() + the modal sections + the Map building in the submit handler.

5. **MM payout request UI hidden (web + mobile)** — per user decision. Wallet balance + earnings + past-payouts list remain visible. Backend (POST `/v1/sellers/payouts` + historical methods) untouched — trivial UI-only re-enable. New `payoutTemporarilyUnavailable` translation key on both surfaces. seller-mobile's `request_payout_screen.dart` collapsed from 276 lines to a 50-line "info" screen (deep-link safe).

**Staleness removed**: `Conversation`, `Message`, `MobileMoneyProvider` types from `seller-web/lib/types.ts`. Inline `PAYOUT_METHOD_LABELS` lookup replaces the `MobileMoneyProvider` enum-based one for displaying historical payouts.

**Verification**:
- `pnpm --filter {api,seller-web} type-check`: clean
- `pnpm --filter api test:e2e`: 90/90 passing
- `flutter analyze` (seller-mobile): 14 pre-existing `deprecated_member_use` + `use_build_context_synchronously` infos; zero errors/warnings from this refactor
- CI: lint, type-check, API e2e, both Flutter analyses, all docker-build-check stages — all green
- Prod smoke: `seller.teka.cd/dashboard` → 307 (auth redirect, route working); `/v1/messages/unread-count` → 401 (endpoint deleted, auth wall behind it)

**Out of scope (flagged for future PRs)**:
- **Profile management** still missing on web + mobile (separate initiative, multi-PR scope)
- seller-mobile models still use the `Map<String, dynamic>` + `getLocalizedTitle/Description(locale)` pattern (product, category, attribute, promotion). buyer-mobile got the equivalent cleanup in PR #117; seller-mobile is a separate follow-up. The defensive `parseTranslatable` makes them tolerate plain-string API responses so they keep working.

**Ship cycle**: PR #120 → develop ✓ → PR #121 → main ✓ → deploy `26173659447` succeeded → back-merge main → develop ✓.

### Initiative — Flutter buyer-mobile multilingual cleanup (2026-05-20, PR #117 → #118)

### Initiative — Flutter buyer-mobile multilingual cleanup (2026-05-20, PR #117 → #118)

**Brief**: Post-migration cleanup. After the jsonb→text DB migration (PR #111 → #112), the API serves plain French strings for every translatable field. The defensive `{ 'fr': value }` Map wrapping in the Flutter buyer-mobile models was protecting against a shape the API no longer returns — pure dead weight.

**Models simplified (10 — audit identified 2, found 8 more during the work)**:
- `catalog/product_model.dart` — BrowseProductModel.title, ProductDetailModel.title/description, ProductCategory.name, BreadcrumbItem.name
- `catalog/category_model.dart` — CategoryModel.name
- `home/banner_model.dart` — title, subtitle
- `home/flash_deal_model.dart` — FlashDealProduct.title + FlashDealModel.title
- `content/content_page_model.dart` — title, content (both classes)
- `cart/cart_model.dart` — CartItemProduct.title
- `orders/order_model.dart` — OrderItemModel.productTitle
- `wishlist/wishlist_model.dart` — WishlistProductModel.title
- `city/city_model.dart` — CityModel.name (audit missed)
- `city/commune_model.dart` — CommuneModel.name (audit missed)

Each dropped: `Map<String, ...>` typed field → `String?`/`String`; `is Map ? Map.from(...) : { 'fr': value }` parsing branch → `json[k]?.toString()`; `localized*(locale)` / `getLocalizedName(locale)` helper; private `_parseTranslationMap` where present.

**Call sites updated (12 files)**: product_card, product_detail_screen (3 calls), category_chip, checkout_screen (3 calls — including the form-submit `data.town`/`neighborhood` which were `name['fr']`), banner_carousel, flash_deal_card, content_page_screen (3 calls), order_detail_screen (`item.localizedTitle` → `item.productTitle`), wishlist_screen, home_screen, cart_screen, city_selection_screen.

Unused `locale` local variables were removed (cart_screen, product_detail_screen, content_page_screen, category_chip, flash_deal_card, home_screen, checkout_screen inner widget). `CartItemTile` no longer takes a `locale` constructor argument.

**Verification**:
- `flutter analyze` (buyer-mobile): zero errors, zero warnings from this refactor (18 pre-existing `deprecated_member_use` infos remain — `withOpacity`/`groupValue` etc, unrelated)
- CI: lint, type-check, API e2e 90/90, both Flutter analyses, all 4 docker-build-check stages — all green
- Net diff: -251 / +57 lines across 23 files

**Ship cycle**: PR #117 → develop ✓ → PR #118 → main ✓ → deploy `26139068828` succeeded → back-merge main → develop ✓. Web prod unaffected (no web files changed); next mobile APK build picks up the changes — distribution is manual via Play Store / direct download, not part of the web deploy pipeline.

### Initiative — Admin user-management split (2026-05-19, PR #114 → #115)

### Initiative — Admin user-management split (2026-05-19, PR #114 → #115)

**Brief**: From the larger admin audit, replace the muddled `Utilisateurs` (mixed BUYER+SELLER+ADMIN) and broken `Vendeurs` (was an application queue, called 404'd endpoints, never actually worked) with 3 clean role-scoped pages.

**New surfaces**:
- `/dashboard/buyers` — BUYER list with status filter (Actifs / Suspendus / Bannis), search by name/email/phone.
- `/dashboard/sellers` (rewritten) — SELLER list joining `sellerProfile`. Dual status columns: KYC `applicationStatus` (PENDING / APPROVED / REJECTED) + account `status` (ACTIVE / SUSPENDED / BANNED). Filter tabs: Tous / En attente / Approuvés / Rejetés / Suspendus. Inline Approve / Reject buttons (with reason modal) on PENDING rows, calling the correct `PATCH /v1/admin/sellers/applications/:applicationId` endpoint.
- `/dashboard/admins` — ADMIN read-only list with last-login column.

**API change**: `admin-users.service.findAllUsers` now selects `sellerProfile { id, businessName, applicationStatus, rejectionReason }`. Null for non-SELLER users. Backward compatible.

**Bonus bug fix uncovered during the work**: the old `/dashboard/sellers` page was calling `/v1/admin/sellers?status=` (404 — controller only exposes `/v1/admin/sellers/applications`) and the Approve/Reject buttons called phantom `/v1/admin/sellers/:id/approve` POST endpoints. The seller-approval workflow had been completely broken; the rewrite is the first version that actually works.

**Routes**:
- New: `/dashboard/{buyers,sellers,admins}` (sellers URL repurposed)
- Removed: `/dashboard/users` (file deleted)
- Redirect: `next.config.ts` adds 308 from `/dashboard/users → /dashboard/buyers`
- Sidebar: replaces 2 entries with 3 (Acheteurs / Vendeurs / Administrateurs)
- `messages/fr.json`: new `Buyers` and `Admins` namespaces; rewritten `Sellers` namespace

**Verification**:
- `pnpm --filter {api,admin-web} type-check`: clean
- `pnpm --filter api test:e2e`: 90/90 passing
- Local Docker smoke against dev DB (1 buyer, 2 sellers, 1 admin):
  - /buyers: Jean Mulamba (Actif) visible
  - /sellers: both sellers with correct dual-status; Marie's pending application approved end-to-end (PATCH succeeded, row updated to "Approuvé", buttons disappeared, page refreshed)
  - /admins: Admin Teka (Actif, last-login today)
  - /users → /buyers 308 redirect verified
- Prod smoke after deploy: all 3 new routes return 307 (auth redirect to login — route exists, middleware protects correctly); /users returns 308 (redirect rule active).

**Ship cycle**: PR #114 → develop ✓ → PR #115 → main ✓ → deploy `26123825619` succeeded → back-merge main → develop ✓.

### Initiative — jsonb→text migration for monolingual French (2026-05-19, PR #111 → #112)

### Initiative — jsonb→text migration for monolingual French (2026-05-19, PR #111 → #112)

**Brief**: After PR #108/#109 fixed the empty product lists, dev admin UI was rendering product titles + category names as raw `{"en":"...","fr":"..."}` JSON. User asked to fully strip multilingual support from the data layer.

**Root cause** (investigation went deeper than the original brief): the May 2026 monolingual refactor updated the Prisma schema to declare 12 translatable columns as `String`, but **no ALTER TABLE was run on the dev DB**. The Postgres columns were still `jsonb` holding `{en, fr}` objects. Prisma's client silently stringifies JSONB on read when the schema declares String — which is why every translatable field rendered as raw JSON.

Verified via `information_schema` on dev DB — 12 columns affected:
- `products.{title, description}`
- `categories.{name, description}`
- `cities.name`, `communes.name`
- `banners.{title, subtitle}`
- `promotions.{title, description}`
- `content_pages.{title, content}`

**Fix** — one-shot migration script at `apps/api/prisma/scripts/migrate-jsonb-to-text.ts`:

```sql
ALTER TABLE <t>
ALTER COLUMN <c> TYPE text
USING COALESCE(<c>->>'fr', <c>->>'en', <c>::text)
```

- Atomic per-column (data conversion + type change in one statement)
- Idempotent (skips columns already `text`)
- COALESCE preserves data for any malformed rows (.fr → .en → raw JSONB text fallback)

**Verification — dev DB**: all 12 columns migrated `jsonb → text`. Sample data: `products.title = "Tecno Spark 10 Pro - 128Go"` (plain, not JSON). Browser smoke on admin /Produits + seller /Mes Produits — every title, category, city renders as plain French. No JSON visible.

**Verification — prod DB**: ran the same script; **all 12 columns reported "already text, skipping"**. Prod had been migrated at some earlier point (likely an earlier `prisma db push` that ran the ALTER when the schema was first changed, OR a manual migration). The user-visible JSON-rendering issue was dev-only — prod has been correct all along. Script committed as a safety net + documentation against future drift.

**Ship cycle**: PR #111 → develop ✓ → PR #112 → main ✓ → deploy `26119949796` succeeded → back-merge main→develop ✓ → prod migration ran idempotently (all skipped). User confirmed products visible on prod admin Produits.

**Out of scope (deferred)**:
- Flutter mobile model defensive parsing — `buyer-mobile/lib/features/catalog/data/models/{product,category}_model.dart` wrap incoming `title`/`name` strings in `{ 'fr': value }` maps. Tolerates plain strings, so functionally fine; simplification to plain `String` is a separate cleanup PR.

### Initiative — Product list empty bugs (2026-05-19, PR #108 → #109)

### Initiative — Product list empty bugs (2026-05-19, PR #108 → #109)

**Brief**: Two reported bugs — admin "Produits" menu shows empty list despite products in DB; seller "Mes produits" shows empty list. Larger brief also requested user-management refactor + profile management systems (deferred — out of scope for this PR per user sign-off).

**Diagnosis** (read-only Explore subagent + direct code reading + dev DB queries):

**Bug 1 — admin /produits empty**
- Root cause: `apps/api/src/admin/admin-products.service.ts:23` hardcoded `where: { status: ProductStatus.PENDING_REVIEW }`. Page was effectively a moderation queue mislabeled "Produits".
- Dev DB had 24 products (16 ACTIVE, 3 PENDING_REVIEW, 3 DRAFT, 1 REJECTED, 1 ARCHIVED) — only 3 visible to admin even though all 24 exist.

**Bug 2 — seller /mes-produits empty**
- Root cause: pure response-shape mismatch.
- API at `products.service.ts:findSellerProducts` returns `{ data: [...], pagination: {...} }` (the canonical shape used everywhere else in the codebase).
- seller-web at `dashboard/products/page.tsx:64-65` read `res.data.products` + `res.data.meta` — both keys are undefined → `setProducts(undefined || [])` always empty.
- seller-mobile at `products_repository.dart:48-50` correctly read `data['data']` for items but `data['meta']` for pagination (pagination defaulted to zeros on mobile but items rendered).

**Fix** (per user sign-off — all 3 questions): Full list + status filter; fix frontends to match API; ship just these two bugs.

API:
- `admin-products.service.ts`: `findPendingProducts(page, limit)` → `findProducts(page, limit, status?)`. When `status` omitted, returns all statuses. Status whitelisted against `ProductStatus` enum (invalid value falls back to all — no SQL injection).
- `admin-products.controller.ts` @Get() handler exposes `?status=` query param.

admin-web:
- Status filter tabs: Tous / En attente / Actifs / Rejetés / Archivés / Brouillons
- Status badge renders all 6 ProductStatus values (was only APPROVED/REJECTED/pending)
- Approve/Reject buttons remain gated to PENDING_REVIEW rows (existing service logic unchanged)
- Response-shape parity: read `pagination` not `meta`
- 6 new FR translation keys

seller-web:
- `seller-web/.../products/page.tsx`: read `res.data.data` + `res.data.pagination`. Update `ProductsResponse` type.

seller-mobile:
- `products_repository.dart`: read `data['pagination']`.

**Verification** (local Docker, dev DB):
- `pnpm --filter {api,admin-web,seller-web} type-check`: clean
- `pnpm --filter api test:e2e`: 90/90 passing
- `flutter analyze` (seller-mobile): no new warnings
- Browser smoke (admin@teka.cd ADMIN + marie@shop.cd SELLER, with temporary COOKIE_DOMAIN= override for localhost):
  - Admin /Produits: 24 listed; "En attente" → 3; "Actifs" → 16; "Rejetés" → 1; "Brouillons" → 3; "Archivés" → 1
  - Approve/Reject buttons gate to PENDING_REVIEW only
  - Seller (Marie) /Mes Produits: all 11 of her products listed across all statuses with correct prices and actions

**Ship cycle**: PR #108 → develop ✓ → PR #109 → main ✓ → deploy `26117131092` succeeded → back-merge main→develop ✓. Prod smoke: admin.teka.cd/login + seller.teka.cd/login → 200; api.teka.cd /admin/products + /sellers/products → 401 (auth working).

**Out of scope (flagged for follow-up)**:
- Product titles render as `{"en":"...","fr":"..."}` JSON strings (seed-data shape from before the May 2026 monolingual refactor). Pre-existing — affects buyer-web SEO too. Separate cleanup PR.
- Larger architecture asks from the brief (user-management refactor into Buyers/Sellers/Admins split; profile management systems for buyer+seller+admin). Each is multi-PR scope; needs separate plan.

### Initiative — Admin dashboard audit + sync (2026-05-18, PR #105 → #106)

### Initiative — Admin dashboard audit + sync (2026-05-18, PR #105 → #106)

**Brief**: Full review/sync/validation of admin dashboard after the May 12-18 platform refactors (buyer WhatsApp OTP, phone-OTP removal, monolingual FR-only, Google OAuth removal, seller SMS-migration cleanup). Browser-test locally with Docker. Modify only what's necessary.

**Audit method**: Read-only Explore subagent mapped all admin-web pages, endpoints, auth flow, and grepped for stale references to removed surfaces.

**Confirmed issues (3 — all type-definition drift)**:
1. `User.phone` is `String?` since 2026-05-12 but admin-web typed it as `string` in 6 places (auth-store + 5 dashboard pages). Email-only buyer cohort (2026-05-12 → 2026-05-15) has null phone until they complete /reclamer-compte; admin previously rendered a blank cell.
2. `User.locale` was dropped during the May monolingual collapse but the type still declared it on all 3 web apps' auth stores (admin-web, seller-web, buyer-web).
3. Cross-platform parity: also fixed `locale` field in seller-web + buyer-web auth stores (buyer-web `phone` was already correct).

**Changes**:
- `apps/admin-web/src/lib/auth-store.ts`: phone → nullable, drop locale
- `apps/admin-web/src/app/dashboard/{users,sellers,orders,orders/[id],products/[id]}/page.tsx`: phone interfaces → nullable, render `?? '—'` fallbacks
- `apps/{buyer,seller}-web/src/lib/auth-store.ts`: drop stale locale field
- 8 files, +14/-17. No runtime behavior change beyond rendering `—` instead of blank cells.

**Audit findings — CLEAN (no fix needed)**:
- Auth middleware accepts access OR refresh token (parity with seller-web after PR #95→#97)
- Login: email-only, no phone tab, no Google OAuth, no `/migrate` link
- Dashboard role gate logs out non-ADMIN users (PR #100 → #101 still working)
- `apiFetch` has hasSessionHint + coalesced refresh-on-401 (parity)
- Zero references to removed surfaces: Conversation/Message, seller migration endpoints, Google OAuth, `[locale]` routing, Mobile Money UI
- Cloudinary lifecycle handled API-side; admin delete doesn't need extra
- Stats queries don't reference dropped models

**Verification**:
- `pnpm --filter {admin,seller,buyer}-web type-check`: clean
- `pnpm --filter admin-web build`: clean
- Local Docker rebuild + smoke on `/admin/login`: email-only form renders, no console errors, middleware correctly redirects `/dashboard/*` → `/login?redirect=...` for unauthenticated users
- CI green on both #105 and #106 (lint, type-check, API e2e 90/90, both Flutter analyses, all 4 docker-build-check stages)
- Local dev DB at `postgresql-congofoot.alwaysdata.net` was unreachable during smoke — blocked post-login browser walk-through; CI build pipeline + the trivial-safety of the diff (pure type-level + render guards) substituted for that

**Ship cycle**: PR #105 → develop ✓ → PR #106 → main ✓ → deploy `26055805939` succeeded (1m13s) → back-merge main→develop ✓. Prod smoke: `https://admin.teka.cd/login` returns 200.

### Initiative — Seller auth cleanup + platform-seller email migration (2026-05-18, branch feat/seller-auth-cleanup-migration)

### Initiative — Seller auth cleanup + platform-seller email migration (2026-05-18, branch feat/seller-auth-cleanup-migration)

**Brief**: Sellers must authenticate ONLY via email + password. Remove all remaining legacy SMS-based seller authentication code. Migrate the "Teka RDC Officiel" platform seller account to `ipanga@outlook.fr` without disturbing historical data (orders, product ownership, audit trails). Must NOT touch buyer WhatsApp OTP flow.

**Scope of removal (API)**:
- `apps/api/src/auth/auth.controller.ts` — dropped `sellerMigrateCheck`, `sellerMigrateLinkEmail`, `sellerSetupPassword` handlers + DTO imports.
- `apps/api/src/auth/auth.service.ts` — dropped `migrateSellerCheck()`, `migrateSellerLinkEmail()`, `setupSellerPassword()`, private `sendSellerSetupLink()`.
- `apps/api/src/auth/dto/seller-{migrate-check,migrate-link-email,password-setup}.dto.ts` — deleted (3 files).
- `SellerMigration` Prisma model **kept** (historical rows / audit). `AuthProvider` enum keeps `PHONE_OTP` (buyer WhatsApp OTP path still uses it).

**Scope of removal (seller-web)**:
- `apps/seller-web/src/app/migrate/` + `apps/seller-web/src/app/setup-password/` — deleted.
- `apps/seller-web/src/app/login/page.tsx` — removed the `/migrate` link (forgot-password stays).

**Scope of removal (seller-mobile)**:
- `apps/seller-mobile/lib/features/auth/presentation/screens/{migrate,setup_password}_screen.dart` — deleted (2 files).
- `apps/seller-mobile/lib/features/auth/data/auth_repository.dart` — dropped `migrateSellerCheck`, `migrateSellerLinkEmail`, `setupSellerPassword`, `requestOtp`.
- `apps/seller-mobile/lib/features/auth/presentation/providers/auth_provider.dart` — dropped matching provider methods.
- `apps/seller-mobile/lib/core/router/app_router.dart` — dropped `/auth/migrate` + `/auth/setup-password` routes.

**Platform-seller migration (declarative, via seed.ts)**:
- `apps/api/prisma/seed.ts:seedTekaOfficielSeller()` — constants `TEKA_OFFICIEL_SELLER_EMAIL='ipanga@outlook.fr'` + `TEKA_OFFICIEL_DEV_PASSWORD='TekaDev2026!'`. Dynamic `bcrypt` import + `bcrypt.hash(password, 10)` runs once per seed. Upsert now writes `email`, `authProvider='EMAIL_PASSWORD'`, `emailVerified=true`, `passwordHash`, `passwordSetAt=new Date()` on BOTH create AND update branches (previous `update: {}` would not have migrated an existing row).
- Phone field (`+243800000000`) preserved — required for delivery contact + retains uniqueness with historical rows. UserId `10000000-0000-0000-0000-000000999999` unchanged → SellerProfile, Product ownership, and Order references all untouched.

**Verification**:
- `pnpm --filter api type-check` — clean
- `pnpm --filter seller-web type-check` — clean
- `pnpm --filter api test:e2e` — 90/90 passing (no test referenced the dropped endpoints)
- Flutter analyze (seller-mobile) — clean

**Prod data migration** (seed does not run on prod): provide the user with one-off SQL after merge to:
1. Update the existing `users` row (`id = '10000000-0000-0000-0000-000000999999'`) to set `email='ipanga@outlook.fr'`, `authProvider='EMAIL_PASSWORD'`, `emailVerified=true`, `passwordHash=<bcrypt('TekaDev2026!', 10)>`, `passwordSetAt=NOW()`.
2. Trigger `/v1/auth/password-reset/request` for ipanga@outlook.fr to rotate to a real password via the standard reset flow.

**Out of scope (intentional)**:
- `SellerMigration` Prisma model retained (historical reference).
- `email.service.ts:sendSellerSetupEmail` method retained (no callers, but harmless dead method — leave for next janitorial sweep).
- `PHONE_OTP` enum value retained (buyer WhatsApp OTP creates these records).

### Hotfix — admin/seller /login bypass via cross-subdomain cookies (2026-05-17, PR #100 → #101)

**Symptom**: Clicking the "Connexion / Login" button on `admin.teka.cd` or `seller.teka.cd` opened the dashboard directly instead of showing the login form. Buyers who'd logged into teka.cd were silently landing on broken admin/seller dashboards.

**Root cause** (regression from interactions between today's earlier work):
- PR #95 (earlier today) added refresh-token fallback to seller-web + admin-web middleware (so 15-min access token expiry doesn't kick people out)
- The COOKIE_DOMAIN=.teka.cd infra fix (also earlier today) made cookies cross-subdomain-visible
- Combined: buyer cookies on .teka.cd satisfied the middleware's `hasSession` check on seller.teka.cd / admin.teka.cd → triggered the `if (isAuthOnly && hasSession) → /dashboard` redirect
- Dashboard layouts didn't check role, so wrong-role users briefly saw the dashboard chrome before APIs started 403'ing

**Fix** (4 files, defense in depth):
- `apps/{seller,admin}-web/src/middleware.ts` — removed the authOnly `/login → /dashboard` redirect. The login form renders unconditionally; `/dashboard` protection (redirect to /login when no cookie) unchanged.
- `apps/{seller,admin}-web/src/app/dashboard/layout.tsx` — wait for `/me`, then verify `user.role === 'SELLER'` (or `'ADMIN'`). Wrong role → `logout()` (clears wrong-role cookies) → `router.replace('/login')`. Spinner shown until check passes; never flashes dashboard for a wrong-role user.

**Ship cycle**: PR #100 → PR #101 → main → deploy run 25997178223 succeeded. Back-merged main → develop.

**Prod smoke verified**:
- `admin.teka.cd/login`: login form renders even with `teka_session=1` hint cookie present ✓
- `seller.teka.cd/login`: login form renders ✓

**Out of scope** (called out for posterity):
- Adding a per-role cookie at login (e.g. `teka_role=ADMIN`) would let the middleware do the role gate at the edge. Cleaner architecturally, but new contract across API + 3 web apps. Client-side gate is smaller and equally effective for this surface area.

### Previous initiative — Payment simplification + messaging removal + auth parity (2026-05-17, 4 PRs)

### Initiative — Payment simplification + messaging removal + auth parity (2026-05-17, 4 PRs)

**Audit-first discovery**: brief mentioned three threads — payment simplification, messaging removal, auth persistence. Parallel research agents confirmed: actual payment-method UI surfaces are ONLY 2 (buyer-web + buyer-mobile checkout); messaging surfaces span 5 apps + API + Prisma; reported "users still get logged out" issue applies to seller-web + admin-web, not buyer-web (which was fixed earlier today). Plan: 3 PRs shipped in priority order.

**PR A (#95→#97) — Auth parity for seller-web + admin-web**
- `apps/{seller,admin}-web/src/lib/api-client.ts` — full apiFetch port from buyer-web with hasSessionHint + tryRefresh coalesced refresh-on-401 + loop guard
- `apps/{seller,admin}-web/src/middleware.ts` — accept either teka_access_token OR teka_refresh_token (was access-only)
- Mobile already had the equivalent dio interceptor (verified in audit) — no parity work needed
- Without this, sellers and admins were getting silently logged out 15 min after login. Actual cause of the user's persistent logout reports across non-buyer surfaces.

**PR B (#96, shipped via #99) — Hide Mobile Money UI in checkout**
- `apps/buyer-web/src/app/checkout/page.tsx` — `ENABLE_MOBILE_MONEY = false` constant gates MM PaymentTile + provider sub-card + payer phone field
- `apps/buyer-mobile/lib/features/checkout/presentation/screens/checkout_screen.dart` — `_enableMobileMoney = false` gates MM _PaymentOption + provider tiles + phone TextField
- Backend untouched: PaymentProvider interface + Flexpay + COD + factory + DTO + service branching all preserved. Re-enable is a one-line flip of both constants.
- Historical orders with `paymentMethod: 'MOBILE_MONEY'` still display correctly on seller + admin order pages (display logic was already enum-aware).

**PR C (#98, shipped via #99) — Retire direct buyer↔seller messaging**
- API (`apps/api/src/messaging/messaging.controller.ts`): all 5 endpoints throw 410 GONE with `code: 'MESSAGING_REMOVED'` + French message. MessagingService implementation, Prisma `Conversation` + `Message` models, all DB rows, 3 User relations preserved untouched — historical data stays readable for audit + support.
- buyer-web: deleted `app/messages/`, removed header icons + unread polling, dropped `/messages` from middleware protectedRoutes, replaced PDP "Contacter le vendeur" with `<Link href="/contact">Contacter le support Teka RDC</Link>`
- seller-web: deleted `app/dashboard/messages/`, removed sidebar Messages nav + polling
- buyer-mobile + seller-mobile: deleted `lib/features/messaging/`, dropped `/messages` routes from app_router.dart, removed appbar Messages icons + cards. buyer-mobile PDP shows non-actionable Container("Pour toute question, contactez le support Teka RDC.") with support_agent icon.
- Translation keys preserved with `_DEPRECATED_2026-05-17` marker for audit. New keys: `Support.contactSupport` (web) + `contactSupport` (Flutter buyer-mobile l10n, regenerated via `flutter gen-l10n`).
- Net diff: 33 files, +153 / -3970 lines.

**Ship cycle**: PR #95 → PR #97 → main → deploy ✓. PR #96 + PR #98 → develop in parallel → combined PR #99 → main → deploy ✓. Back-merges main → develop after each release.

**Prod smoke verified**:
- PDP shows "Contacter le support Teka RDC" linking to `/contact` ✓
- Header has zero `/messages` links ✓
- Messaging API endpoints return 401 (auth-gated) — would return 410 GONE for authenticated stale clients
- buyer-web + seller-web + api.teka.cd all return 200

**Still requires real-user verification** (needs authenticated session):
- buyer-web checkout shows only COD tile (verified at code/type-check level)
- buyer-mobile checkout same
- seller.teka.cd login → 16+ min idle → navigate (PR A's actual purpose)

**Explicitly preserved** (per "do not remove anything still needed for audits / re-enable"):
- All backend payment provider abstraction
- All Prisma models, DB rows, User relations
- All `MessagingService` code
- All Messaging.* translation keys with deprecation markers
- Notifications + SMS broadcasts (already isolated — no coupling per audit)

### Previous initiative — Cloudinary media lifecycle + client compression (2026-05-17)

**Scope discovery**: Audit revealed actual upload surface is much smaller than the brief suggested. Only TWO file-upload entry points exist across the whole stack:
1. `seller-web/src/components/product/image-uploader.tsx` → `POST /v1/sellers/products/:id/images`
2. `seller-mobile/lib/features/products/.../product_images_screen.dart` → same endpoint

Buyer-web/buyer-mobile have no avatar upload UI. Admin banner page accepts URL paste only (no `cloudinaryId` stored). `SellerProfile` has no logo field. So the work focused on the product image lifecycle where real uploads happen.

**PR A — Server-side lifecycle (PR #89, released via #90)**:
- `CloudinaryService.deleteImages(ids[])` bulk helper — wraps `delete_resources` in 100-id chunks, logs partial failures but never throws (orphan assets are a cost issue, not correctness; failing the caller's DB delete would be worse).
- New `DELETE /v1/sellers/products/:id/hard` endpoint — `/hard` suffix makes destructive intent obvious at the call site. Owner-scoped. Collects all image cloudinaryIds, deletes the Product row (FK cascade removes ProductImage rows), then bulk-destroys Cloudinary. DB-first ordering so a Cloudinary blip can't roll back the local delete.
- Existing soft-archive flow (`DELETE /v1/sellers/products/:id`) UNCHANGED: per locked decision, archive stays reversible (images kept), only hard-delete purges.
- Safe for order history: `OrderItem` snapshots title + image URL at order time.
- Thumbnail URL template: `f_webp` → `f_auto,q_auto`. Cloudinary now serves AVIF/WebP/JPEG based on requester's Accept header.
- Upload MIME guard: JPEG/PNG/WebP/GIF only on `POST /v1/sellers/products/:id/images`. 5 MB ceiling kept as defence-in-depth.
- e2e: 90/90 pass (5 new auth-guard tests for hard-delete and existing routes).

**PR B — Client compression (PR #91, released via #92)**:
- seller-web: `browser-image-compression` (~6 KB gzipped). `lib/image-compress.ts` wraps the library with `compressImageForUpload(file)` → ≤500 KB WebP (skips GIF + already-small files). Fail-safe: returns original on any error.
- seller-mobile: `flutter_image_compress ^2.3.0`. `lib/core/utils/image_compress.dart` mirrors web helper. Returns `CompressedImage { bytes, filename, mimeType }` so dio MultipartFile gets the right `.webp` extension. `ProductsRepository.uploadImage` switched to `MultipartFile.fromBytes`.
- Cleanup bundled in: stale `app_localizations_en.dart` removed (leftover from May 2026 monolingual refactor — pub get noticed the EN .arb was gone).

**Explicit non-goals (called out in PR bodies)**:
- Avatar upload UI on buyer-web / buyer-mobile (none exists today)
- Seller logo upload (no `SellerProfile.logo` field exists)
- Banner image cleanup (admin pastes URL, no `cloudinaryId` stored — needs schema migration)
- Soft-delete user → cascade-purge products' Cloudinary (would break order history thumbnails)

**Prod smoke (post-deploy)**:
- seller.teka.cd loads cleanly (compression dep didn't break the build)
- `DELETE /v1/sellers/products/:id/hard` returns 401 unauthenticated (route exists, auth-guarded)
- `api.teka.cd/api/v1/browse/products?limit=1` returns 200 with `image: { url, thumbnailUrl }` shape using the new `f_auto,q_auto` template

**Awaiting real-seller verification**:
- seller-web: drop a 3+ MB JPEG → confirm devtools shows ≤500 KB compressed `.webp` payload uploaded
- seller-mobile: pick a fresh camera photo → confirm dio request body weight matches compressed size
- Hard-delete: invoke from a seller account, confirm Cloudinary asset is destroyed (via Cloudinary console)

### Infrastructure fix — COOKIE_DOMAIN missing in prod (2026-05-17)

**Discovery**: After PRs #81/#83/#85 shipped and all code-level smoke passed, the authenticated /checkout flow STILL redirected to /connexion. Inspection from a live logged-in session showed:
- `/v1/auth/me` returned 200 (cookies present on api.teka.cd)
- `document.cookie` on teka.cd was empty — no `teka_session` hint cookie reaching teka.cd
- Manually setting a `.teka.cd`-scoped cookie via JS worked fine (no browser block)

**Root cause**: `.env.production` on the deploy VPS had no `COOKIE_DOMAIN` entry. `apps/api/src/auth/auth.controller.ts::setAuthCookies` reads `COOKIE_DOMAIN` and falls back to `undefined` (cookie defaults to request host). Auth cookies were therefore scoped to `api.teka.cd` only and never reached `teka.cd` middleware. All three PRs' code was correct — they couldn't take effect because the cookies they relied on weren't visible.

**Fix**: Added `COOKIE_DOMAIN=.teka.cd` to `/home/deploy/teka-rdc/.env.production`. Recreated api container via `docker compose up -d api` (picks up new env). Reloaded nginx (`nginx -s reload`) to repoint at the new api container IP. User re-logged in to get fresh `.teka.cd`-scoped cookies (old api.teka.cd-scoped cookies are still in browsers but unused since middleware/server now issues new ones).

**Pre-existing source-of-truth**: CLAUDE.md § 3 already documents `COOKIE_DOMAIN` (empty in dev, `.teka.cd` in prod for cross-subdomain cookies). The local `.env.production` file was missing the line. `.env.production` is gitignored so this drift wasn't catchable via CI.

**Final verification** (after the fix landed, logged-in user `+243814250002`, BUYER role):
- Bug #1 (badge real-time): 7 → 8 in 1.6 seconds after add-to-cart, no reload. ✅
- Bug #2 (cart thumbnails): 8/8 items rendered Cloudinary thumbnails, 0 placeholder SVGs. ✅
- Bug #3 (/checkout no redirect): page loaded with title "Commander | Teka RDC", stepper "Adresse → Paiement → Récapitulatif", authenticated nav ("Mon compte" / "Se déconnecter"). ✅

**Follow-up to consider**: add a startup-time warning in `apps/api/src/main.ts` — if `NODE_ENV=production` AND `COOKIE_DOMAIN` is missing OR is not a leading-dot parent domain of the API URL, log a loud `[BOOT WARNING] COOKIE_DOMAIN is not set — cross-subdomain auth will silently fail.` Optional crash-on-boot for stronger safety net. Not shipped yet; user-discretion follow-up.

### Hotfix #3 — Guest cart thumbnail hydration (2026-05-17, follow-up to #2)

**Symptom**: After PR #84 deployed, prod smoke of `/cart` as a guest still showed placeholder icons instead of product thumbnails — even though PR #83's server-side cart serializer was live and working.

**Root cause**: `apps/buyer-web/src/app/cart/page.tsx::loadGuestCart` typed its API call as `BrowseProduct` (which has `image: { url, thumbnailUrl } | null`) but actually hits `/v1/browse/products/:id` which returns `ProductDetail` (with `images: ProductImage[]` — the full gallery). Reading `p.image` was always `undefined` → `CartItemRow` rendered the placeholder icon for every guest item. The authenticated path (PR #83's serializer) was fine; only the guest hydration was broken.

**Fix** (1 file): `apps/buyer-web/src/app/cart/page.tsx` — type the response as `ProductDetail`, reshape `images[0]` to the `{ url, thumbnailUrl }` object `CartItemRow` consumes. Same transformation PR #83's server-side serializer already does for the authenticated path.

**Ship cycle**:
- PR #85 (fix → develop), 7/7 checks green, merged.
- PR #86 (release develop → main), 10/10 checks green, merged.
- Back-merged main → develop (commit 809a2df).
- Prod deploy run 25985230566 succeeded.
- Prod smoke verified: `/cart` as guest now renders the Cloudinary thumbnail for the seeded product. Screenshot confirms full cart row (image + title + seller + price + "Passer la commande" CTA).

**Pre-existing seed-data quirk surfaced**: sample products use Cloudinary demo placeholder images (`cld-sample-*.jpg`) — fruit bowls, not real product photos. Documented in memory; replace by uploading real assets to teka-rdc Cloudinary cloud and updating `seedSampleProducts()`.

### Hotfix #2 — Cart badge real-time, cart images, checkout redirect (2026-05-17)

**Symptoms** (reported together by user):
1. Cart icon doesn't update count when adding products
2. Product photos missing in cart page
3. Clicking "Passer la commande" redirects authenticated buyers to /connexion

**Root causes** (all server-side contract problems):

1. **Cart badge** — `useCartStore.fetchCart` sets `totalItems: res.data.totalItems`, but the cart API never returned that field. `undefined > 0` is `false`, so the badge stopped rendering after login (guest path computed totals locally, masking the bug pre-login).

2. **Cart images** — Cart API returned raw Prisma shape `product.images = [{ thumbnailUrl }]`. Both frontends expected something else:
   - buyer-web: `product.image?.thumbnailUrl` (singular structured object)
   - buyer-mobile: `product.thumbnailUrl` + `product.sellerName` at the product top level
   Both rendered placeholder icons.

3. **Checkout redirect** — `middleware.ts` gated `/checkout` on `teka_access_token` cookie. That cookie's maxAge is 15 minutes. Any buyer clicking checkout ≥16 minutes after login was bounced to /connexion, even though the 7-day refresh token was still alive and apiFetch's auto-refresh (PR #81) would have silently restored the session on the first request.

**Fix** (2 files):

- `apps/api/src/cart/cart.service.ts` — new private `serializeCart` helper called from every cart-returning method (`getCart`, `addItem`, `updateQuantity`, `removeItem`, `clearCart`, `mergeGuestCart`). Output shape satisfies both frontends in one payload:
  - root: `totalItems` (sum of item.quantity) + `totalCDF` (BigInt centimes, auto-string via main.ts BigInt.prototype.toJSON polyfill)
  - per product: `image: { url, thumbnailUrl } | null` (web BrowseProduct shape) AND top-level `thumbnailUrl` + `sellerName` + `sellerId` (Flutter CartItemProduct.fromJson)
  - Prisma `images` select now grabs both `url` + `thumbnailUrl` (was thumbnailUrl only)
  - `getCartSummary` (used internally by checkout) keeps reading raw Prisma via `findOrCreateCart` to avoid double-serialization
- `apps/buyer-web/src/middleware.ts` — `hasSession` accepts either `teka_access_token` OR `teka_refresh_token`. The refresh token is the real session signal; if present, page renders and `apiFetch` mints a new access token on the first request.

**Ship cycle**:
- PR #83 (fix → develop), 7/7 checks green, merged.
- PR #84 (release develop → main), 10/10 checks green, merged.
- Back-merged main → develop (commit afff312).
- Prod deploy run 25984817507 succeeded.
- Prod smoke verified:
  - Browse endpoint exposes `image: { url, thumbnailUrl }` (confirms canonical contract).
  - `/checkout` with refresh-token-only cookie returns 200 (was 307 → /connexion before fix).
  - `/checkout` with no cookies returns opaqueredirect (negative path intact — no security regression).

**Not changed**: zero client code changes — buyer-web and buyer-mobile contracts were already correct, the API was lying. Existing auth/cart flows, API surface, SEO, JSON-LD, sitemap all untouched.

**Flutter parity**:
- Bugs 1 + 2: no Flutter changes needed — model already reads the fields the new API shape provides.
- Bug 3: Flutter checkout has no middleware-level redirect, so no parity fix. (If the access token expires mid-session, the Flutter dio call fails — but that's a broader concern about adding a dio auto-refresh interceptor, deferred until it becomes an issue.)

### Hotfix #1 — Buyer session loss after navigation (2026-05-17)

**Symptom**: Buyer logs in → adds to cart → clicks logo → opens product → adds to cart → clicks logo → appears logged out.

**Root cause** (two compounding bugs):
1. `apiFetch` had no 401-refresh logic. When the 15-min access token expired mid-session, `/v1/auth/me` returned 401 → `auth-store` set `user=null` → header flipped to "Se connecter" even though the 7-day refresh token was still valid.
2. PR #80's hint-cookie gate in `fetchUser` skipped `/me` entirely when `teka_session` cookie was missing. That perf win locked out every session that predated #80 (valid tokens, no hint cookie, treated as guest).

**Fix** (3 files, +116 / -24):
- `apps/buyer-web/src/lib/api-client.ts` — `apiFetch` auto-refreshes once on 401 via `POST /v1/auth/refresh`. Refresh attempts coalesced via module-level promise so N concurrent 401s share one refresh call (no refresh-token rotation race). Loop-guarded against refresh endpoint itself. Hint-cookie gated so guests don't waste round-trips.
- `apps/api/src/auth/auth.controller.ts` — `GET /v1/auth/me` re-issues the `teka_session` hint cookie on every success (new `refreshSessionHint` private helper). Auto-heals pre-PR#80 sessions on next /me call.
- `apps/buyer-web/src/lib/auth-store.ts` — removed PR #80's hint-cookie gate from `fetchUser`. Always calls /me on AuthProvider mount; auto-refresh handles expired tokens; 1-round-trip guest cost is acceptable.

**Ship cycle**:
- PR #81 (fix → develop), 6/6 checks green, merged.
- PR #82 (release develop → main), all checks green, merged.
- Back-merged main → develop (commit c2b2a5b).
- Prod deploy run 25983908861 succeeded. Prod smoke confirmed: guest `/me` 401, no refresh follow-up (correct gate), `/v1/auth/refresh` returns 400 with no token (alive + validating).

**Not changed**: existing auth architecture, cart logic, API contracts, SEO/SSR, all flows. Only the recovery path on token expiry is new.

**Flutter parity**: Not affected — buyer-mobile uses dio with its own interceptor pipeline and doesn't share the buyer-web access-token expiry path. If a similar symptom shows up on mobile, the equivalent fix is a dio interceptor that retries on 401 via `/v1/auth/refresh`.

### Previous closeout — Buyer audit + OTP fix + stability — CLOSED 2026-05-17
- 7 audit PRs landed (#74 #75 #76 #77 #78 #79 #80). 2 P0 + 4 P1 + 3 P2 fixed. 5 regression e2e tests added. OTP Copy Code user-verified in prod. Final prod smoke clean (zero console messages on guest pageload). Flutter parity verified — no mobile changes needed.

**Detailed audit tracker**: [`tasks/buyer-audit-progress.md`](./tasks/buyer-audit-progress.md) — per-finding log with severity, status, PR# refs.

### Previous initiative — Buyer UI/UX Redesign (Rakuten-inspired) — COMPLETE 2026-05-16
- 5 PRs landed: #69 (tokens) → #70 (homepage) → #71 (conversion path) → #72 (account/auth/search) → #73 (Flutter).
- Tracker: [`tasks/buyer-ui-redesign-progress.md`](./tasks/buyer-ui-redesign-progress.md).

**Detailed redesign tracker**: [`tasks/buyer-ui-redesign-progress.md`](./tasks/buyer-ui-redesign-progress.md) — per-PR status, file lists, verification gates, and the resumption protocol.

**Plan source**: `/Users/Apple/.claude/plans/partitioned-nibbling-spark.md` (5 phased PRs, brand red `#BF0000`).

### Resumption protocol (always do this first when picking up work)
1. Read `tasks/buyer-ui-redesign-progress.md` — find the first non-`[x]` checkbox.
2. Read the plan above for the active PR's full spec.
3. `gh pr list --state all --search 'Rakuten refresh' --json number,state,title,mergedAt`
4. `git log --oneline -15 && git status`
5. Continue from that first non-`[x]` line. Tick boxes as you finish each one.

### Previous initiative — Buyer WhatsApp OTP refactor (2026-05-15)
Tracker: [`tasks/buyer-whatsapp-otp-progress.md`](./tasks/buyer-whatsapp-otp-progress.md). All milestones COMPLETED — shipped via PRs #65/#66/#67/#68. OTP delivery verified end-to-end through Gupshup `/wa/api/v1/template/msg`.

### Previous-previous initiative — Authentication Refactor

**Detailed auth tracker**: [`tasks/auth-refactor-progress.md`](./tasks/auth-refactor-progress.md) — per-milestone status with deferred items called out.

**Summary of this phase (all backend + web work shipped in this branch):**
- Prisma schema: `AuthProvider` enum, `googleId` + `passwordSetAt` on User, `PasswordResetToken`, `SellerMigration`
- SMS provider abstraction (mirrors `PaymentProvider`): Orange DRC (default prod), Africa's Talking (rollback), Mock — selected by `SMS_PROVIDER` env
- Email + password: register / login / forgot / reset (all French-first, no user enumeration, atomic refresh-token revocation on reset)
- Google OAuth: stateless `POST /v1/auth/login/google` using `google-auth-library` with upsert/link logic
- Email OTP fallback for buyers: user-initiated `POST /v1/auth/otp/request-email`
- Seller migration: existing phone-only sellers migrate via `migrate-check` → `migrate-link-email` → `setup-password` (24h JWT)
- Web apps updated: buyer-web (email fallback link), seller-web (full rewrite: login/register/migrate/setup-password/forgot/reset), admin-web (email/phone tab coexist + forgot/reset)
- Mobile apps updated: buyer-mobile (email fallback link + Google-ready repository methods), seller-mobile (full rewrite: email login + register + migrate + setup-password + forgot/reset)

**Deferred (next session)**: Google native SDK wiring in Flutter (`google_sign_in` pubspec + iOS reversed client id + Android `google-services.json`), seller-mobile deep link config for `teka-seller://setup-password`, e2e test coverage for new endpoints (~15 cases), buyer-web email+password tab + Google button.

---

## Previous Phase — City Marketplace & Dynamic Catalog Upgrade (COMPLETED 2026-03-29)

---

## City Marketplace & Dynamic Catalog Upgrade (COMPLETED)

### Phase 1: Database Schema
- [x] Added City and Commune models to Prisma schema
- [x] Added optional cityId FK to Product, Address, SellerProfile
- [x] Pushed schema to cloud PostgreSQL
- [x] Updated shared types (city.ts, product.ts, validators)

### Phase 2: Seed Data
- [x] Seeded 8 cities (Lubumbashi + Kolwezi active, 6 inactive)
- [x] Seeded 8 communes (6 Lubumbashi, 2 Kolwezi)
- [x] Deactivated old 15 main categories
- [x] Created 8 new main categories with 47 subcategories
- [x] Created 72 product attributes with rich option libraries (brands, sizes, models, etc.)
- [x] Updated existing products, addresses, seller profiles with cityId

### Phase 3: API
- [x] Created CitiesModule with public endpoints (GET /v1/cities, GET /v1/cities/:id/communes)
- [x] Added admin city management (CRUD cities + communes)
- [x] Added cityId filter to browse products API
- [x] Added public category attributes endpoint (GET /v1/browse/categories/:id/attributes)
- [x] Updated product creation to derive cityId from seller profile
- [x] Updated address service to use DB-backed city/commune data
- [x] Updated checkout to use city-based seller location for delivery fees
- [x] Updated seller application DTO with optional cityId

### Phase 4: Admin Web
- [x] Created Cities management page with enable/disable toggle and commune CRUD
- [x] Added "Villes" to sidebar navigation
- [x] Added fr + en i18n keys

### Phase 5: Buyer Web
- [x] Created city store (Zustand + localStorage persistence)
- [x] Created city selector modal (shown on first visit)
- [x] Added city indicator to header with change button
- [x] Updated home page, search page, category page to filter by selected city
- [x] Added fr + en i18n keys

### Phase 6: Buyer Mobile (Flutter)
- [x] Created city feature module (model, repository, provider, selection screen)
- [x] Added city selection redirect in router
- [x] Added city display in home screen AppBar
- [x] Updated catalog providers to filter by cityId
- [x] Added fr + en l10n keys

### Phase 7: Seller Forms — Dynamic Attributes
- [x] Seller Web: Created DynamicAttributesForm component (SELECT/MULTISELECT/TEXT/NUMERIC)
- [x] Seller Web: Integrated into product creation form with specifications submission
- [x] Seller Mobile: Created AttributeModel and DynamicAttributeField widget
- [x] Seller Mobile: Integrated into product form screen
- [x] Added fr + en i18n/l10n keys

### Phase 8: Address Forms — Commune Dropdown
- [x] Buyer Web: Added inline address creation form with city/commune dropdowns in checkout
- [x] Buyer Mobile: Added address creation bottom sheet with city/commune dropdowns
- [x] Auto-fill province/town/neighborhood strings from city/commune selections
- [x] Added fr + en i18n/l10n keys

### Phase 9: Verification
- [x] TypeScript: 0 errors (API + shared)
- [x] All 3 web builds: 0 errors (buyer-web, seller-web, admin-web)
- [x] Flutter analyze: 0 errors (buyer-mobile: 20 info, seller-mobile: 15 info — all pre-existing)
- [x] E2E tests: 67/67 pass
- [x] Documentation updated

---

## Post-Phase Refactoring — Redis Removal (COMPLETED)
- [x] Added Otp and OtpRateLimit PostgreSQL tables to replace Redis OTP storage
- [x] Refactored OtpService to use Prisma instead of Redis
- [x] Removed Redis caching from all services (settings, browse, banners, content, promotions, admin-stats, categories, products)
- [x] Removed Redis from auth service (unused import)
- [x] Removed RedisModule/RedisService from all NestJS modules
- [x] Updated health endpoints to check only database (removed Redis health checks)
- [x] Removed Redis from Docker Compose (dev + prod)
- [x] Removed ioredis dependency from package.json
- [x] Removed REDIS_* environment variables from all env files
- [x] Updated test utilities and e2e tests (removed Redis mocks)
- [x] Updated architecture.md and deployment.md documentation
- [x] Verified TypeScript compilation and builds pass

---

## Phase 1 — Foundation (COMPLETED)
- [x] 1.1–1.12 — All scaffolding tasks complete
- Monorepo, Docker, NGINX, NestJS, Prisma, Redis, shared types, Next.js apps, Flutter apps, CI

## Phase 2 — Authentication & Users (COMPLETED)
- [x] SMS OTP auth flow (Africa's Talking)
- [x] JWT access + refresh tokens with rotation
- [x] Auth guards (@Public, @Roles, @CurrentUser)
- [x] User profile CRUD, address management
- [x] Seller registration + admin approval
- [x] Auth on all 5 frontends (3 web + 2 mobile)

## Phase 3 — Product Catalog (COMPLETED)
- [x] Category tree CRUD with per-category attributes
- [x] Product CRUD (seller) with Cloudinary images, multilingual title/description
- [x] Browse API (search, filter, sort, cursor pagination)
- [x] Admin product moderation (approve/reject)
- [x] Seed data: 15 categories, 46 subcategories, 16 attributes, 20 products
- [x] All 5 frontends: catalog browsing, product detail, seller product management, admin moderation

## Phase 4 — Shopping & Orders (COMPLETED)
### Backend (API)
- [x] Prisma schema: Cart, CartItem, DeliveryZone, Order, OrderItem, OrderStatusLog models
- [x] Shared types: cart/order types, constants, validators (Zod)
- [x] Delivery Zones module: estimate fee API + admin CRUD
- [x] Cart module: getCart, addItem, updateQuantity, removeItem, clearCart, mergeGuestCart, getCartSummary
- [x] Checkout module: idempotent checkout with Prisma transaction, per-seller orders, stock decrement, delivery fee calculation
- [x] Orders module (buyer): list, detail, cancel (PENDING only)
- [x] Orders module (seller): confirm, reject, process, ship, out-for-delivery, deliver with state machine
- [x] Orders module (admin): list all with filters, detail, force status, cancel
- [x] Order notification service: French SMS templates (fire-and-forget, logger-based for now)
- [x] Seed data: 12 delivery zones, 3 cart items, 6 orders in various statuses

### Buyer Web
- [x] Cart store (Zustand): guest (localStorage) + authenticated (API) cart management
- [x] Cart page with items, quantity controls, summary
- [x] Checkout flow: address selection → payment method → review → confirm
- [x] Checkout success page
- [x] Order history with status filters and pagination
- [x] Order detail with timeline, cancel for PENDING
- [x] Cart badge in header, "Add to Cart" on product detail
- [x] i18n: Cart, Checkout, CheckoutSuccess, Orders sections (fr + en)

### Seller Web
- [x] Orders list with status filter tabs, action buttons per status
- [x] Order detail with timeline, buyer info, items, reject modal
- [x] Status badge component, sidebar nav item
- [x] i18n: Orders section (fr + en)

### Admin Web
- [x] Orders table with status/seller/buyer/date filters
- [x] Order detail with force status change, cancel
- [x] Delivery zones CRUD table with create/edit modal
- [x] Status badge, sidebar nav items
- [x] i18n: Orders, DeliveryZones sections (fr + en)

### Buyer Mobile (Flutter)
- [x] Cart feature: model, repository, provider, screen, widgets
- [x] Checkout feature: model, repository, provider, stepper screen, success screen
- [x] Orders feature: model, repository, provider, list screen, detail screen, widgets
- [x] Cart badge on home screen, "Add to Cart" wired on product detail
- [x] Router: /cart, /checkout, /checkout/success, /orders, /orders/:id
- [x] l10n: 47 new keys (fr + en)

### Seller Mobile (Flutter)
- [x] Orders feature: model, repository, provider, list screen, detail screen
- [x] Widgets: order card, status badge, action buttons
- [x] Orders nav in home screen with pending count
- [x] Router: /orders, /orders/:id
- [x] l10n: 30 new keys (fr + en)

### Verification Results
- `pnpm --filter api exec tsc --noEmit` — 0 errors
- `pnpm --filter @teka/shared exec tsc --noEmit` — 0 errors
- `pnpm --filter buyer-web build` — 0 errors
- `pnpm --filter seller-web build` — 0 errors
- `pnpm --filter admin-web build` — 0 errors
- `flutter analyze` (buyer-mobile) — 0 errors (12 info hints: deprecated APIs)
- `flutter analyze` (seller-mobile) — 0 errors (14 info hints: use_build_context_synchronously)
- `prisma db seed` — 12 zones, 3 cart items, 6 orders seeded

## Phase 5 — Payments (COMPLETED)
### Backend (API)
- [x] Prisma schema: Transaction, SellerEarning, Payout, CommissionSetting models + 3 enums (TransactionType, TransactionProvider, PayoutStatus)
- [x] Shared types: payment types, constants, Zod validators (initiatePayment, requestPayout, commissionSetting)
- [x] Environment config: FLEXPAY_* vars, PAYMENT_MOCK_MODE
- [x] PaymentProvider interface + FlexpayProvider (real) + MockPaymentProvider (dev)
- [x] Payments module: initiateOrderPayment, handlePaymentCallback (idempotent webhook), COD transactions, transaction listing
- [x] Earnings service: createEarning (idempotent, commission lookup, atomic wallet update), getSellerWallet, listSellerEarnings
- [x] Checkout integration: MM payment initiation, COD transaction creation, paymentPending response
- [x] Orders integration: COD completion on delivery, earnings trigger when DELIVERED + COMPLETED
- [x] Payouts module: seller request (balance validation), admin approve/reject (atomic reversal), payout listing
- [x] Commission module: global rate + category overrides, CRUD settings
- [x] Admin stats service: dashboard KPIs with Redis 5-min cache
- [x] Seed data: 2 commission settings, 4 transactions, 1 earning, 1 payout, wallet balance update

### Buyer Web
- [x] MM checkout: provider radio buttons (M-Pesa/Airtel/Orange) + phone input
- [x] Payment-pending page: polls every 5s for 5min, USSD instructions, success/failure/timeout
- [x] Order detail: payment status badge, pending payment warning banner
- [x] i18n: Checkout, PaymentPending, Orders additions (fr + en)

### Seller Web
- [x] Earnings dashboard: wallet balance cards + earnings/payouts tabs + payout request modal
- [x] Dashboard: wallet balance KPI card
- [x] Orders: paymentMethod + paymentStatus columns
- [x] Order detail: payment info card
- [x] Sidebar: "Revenus" nav item
- [x] i18n: Earnings, Orders additions (fr + en)

### Admin Web
- [x] Transactions page: filter bar (status/type/date/order), table with 7 columns, pagination
- [x] Payouts page: status filter tabs, approve/reject actions with modals
- [x] Commission page: global rate edit + category overrides CRUD
- [x] Dashboard: KPI cards wired to /v1/admin/stats
- [x] Orders: paymentMethod + paymentStatus columns
- [x] Sidebar: Transactions, Virements, Commissions nav items
- [x] i18n: Transactions, Payouts, Commission sections (fr + en)

### Buyer Mobile (Flutter)
- [x] Checkout: MM provider selection (colored tiles) + phone input, payment-pending navigation
- [x] Payment-pending screen: polls every 5s, USSD instructions, success/failure/timeout states
- [x] Order detail: payment status chip (color-coded)
- [x] Router: /checkout/payment-pending route
- [x] l10n: 16 new keys (fr + en)

### Seller Mobile (Flutter)
- [x] Earnings feature: models (SellerWallet, SellerEarning, Payout), repository, provider
- [x] Earnings screen: wallet cards + earnings/payouts tabs
- [x] Request payout screen: operator selection + phone input + balance display
- [x] Widgets: wallet_card, earning_tile, payout_tile
- [x] Order detail: payment info section
- [x] Home screen: earnings card + bottom nav item
- [x] Router: /earnings, /earnings/request-payout routes
- [x] l10n: 31 new keys (fr + en)

### Verification Results
- `pnpm --filter api exec tsc --noEmit` — 0 errors
- `pnpm --filter @teka/shared exec tsc --noEmit` — 0 errors
- `pnpm --filter buyer-web build` — 0 errors
- `pnpm --filter seller-web build` — 0 errors
- `pnpm --filter admin-web build` — 0 errors
- `flutter analyze` (buyer-mobile) — 0 errors (14 info hints: deprecated APIs)
- `flutter analyze` (seller-mobile) — 0 errors (14 info hints: pre-existing async context warnings)
- `prisma db seed` — 2 commission settings, 4 transactions, 1 earning, 1 payout seeded

## Phase 6 — Reviews, Wishlist & Messaging (COMPLETED)
### Backend (API)
- [x] Prisma schema: Review, Wishlist, Conversation, Message models + ReviewStatus enum
- [x] Denormalized avgRating/totalReviews on Product and SellerProfile (updated atomically in transactions)
- [x] Shared types: review + messaging types, constants, Zod validators
- [x] Reviews module: createReview (verify DELIVERED order + product in order, atomic rating recalc), getProductReviews, getProductReviewStats (1-5 distribution), getMyReviewForProduct, canReview, deleteReview (soft-delete + recalc)
- [x] Wishlist module: addToWishlist (upsert), removeFromWishlist (idempotent), getWishlist (paginated), batch check, status check
- [x] Messaging module: getOrCreateConversation, sendMessage (validates participant), getConversations (lastMessage + unreadCount + otherParty), getMessages (cursor-paginated), markAsRead, getUnreadCount
- [x] Admin reviews: listReviews (filterable), hideReview + unhideReview (with rating recalc), deleteReview
- [x] Browse extension: avgRating/totalReviews in product listings, minRating filter, sort by rating
- [x] Seed data: 2 reviews, 3 wishlists, 2 conversations, 6 messages

### Buyer Web
- [x] Product reviews component: star display, rating distribution bar, review list, write review modal
- [x] Wishlist button: heart toggle with optimistic UI
- [x] Wishlist page: paginated grid, remove button, empty state
- [x] Messages page: conversation list with unread badges, 30s polling
- [x] Chat page: message thread, input, 10s polling, optimistic send, mark as read
- [x] Product detail: reviews section + wishlist button + "Contact Seller" button
- [x] Header: wishlist link + messages icon with unread badge
- [x] i18n: Reviews, Wishlist, Messaging sections (fr + en)

### Seller Web
- [x] Reviews dashboard: product selector, stats, review list
- [x] Messages list: conversation list with unread badges
- [x] Chat page: chat interface with 10s polling, optimistic send, date separators
- [x] Dashboard: review stats card + unread messages card
- [x] Sidebar: "Avis" + "Messages" nav items (unread badge on messages)
- [x] i18n: Reviews, Messaging sections (fr + en)

### Admin Web
- [x] Reviews moderation: status tabs, table, hide/unhide/delete actions, star display
- [x] Sidebar: "Avis" nav item
- [x] i18n: Reviews section (fr + en)

### Buyer Mobile (Flutter)
- [x] Reviews: model, repository, provider, star_rating, review_tile, review_stats_bar, review_form_dialog, product_reviews_screen
- [x] Wishlist: model, repository, provider, wishlist_button, wishlist_screen
- [x] Messaging: model, repository, provider, conversation_tile, message_bubble, conversations_screen, chat_screen
- [x] Product detail: reviews section + wishlist button + "Contact Seller"
- [x] Home screen: wishlist + messages navigation icons
- [x] Router: /wishlist, /messages, /messages/:id, /products/:id/reviews
- [x] l10n: 35 new keys (fr + en)

### Seller Mobile (Flutter)
- [x] Reviews (read-only): model, repository, provider, star_rating, review_tile, seller_reviews_screen
- [x] Messaging: model, repository, provider, conversation_tile, message_bubble, conversations_screen, chat_screen
- [x] Home screen: messages + reviews navigation
- [x] Router: /messages, /messages/:id, /reviews
- [x] l10n: 22 new keys (fr + en)

### Verification Results
- `pnpm --filter api exec tsc --noEmit` — 0 errors
- `pnpm --filter @teka/shared exec tsc --noEmit` — 0 errors
- `pnpm --filter buyer-web build` — 0 errors
- `pnpm --filter seller-web build` — 0 errors
- `pnpm --filter admin-web build` — 0 errors
- `flutter analyze` (buyer-mobile) — 0 errors
- `flutter analyze` (seller-mobile) — 0 errors
- `prisma db seed` — compiles (DATABASE_URL not configured — pre-existing blocker)

## Phase 7 — Admin & Platform Operations (COMPLETED)
### Backend (API)
- [x] Prisma schema: Banner, Promotion, ContentPage, SystemSetting, NotificationBroadcast models + 5 enums (BannerStatus, PromotionType, PromotionStatus, ContentPageStatus, NotificationBroadcastStatus)
- [x] Shared types: platform types, constants, Zod validators (banner, promotion, content, setting, broadcast, report, trend schemas)
- [x] Dashboard trends: getDashboardTrends(period) with raw SQL date_trunc aggregation, Redis 10min cache, period selector (7d/30d/90d)
- [x] Banners module: admin CRUD (v1/admin/banners), public active banners (v1/browse/banners, Redis 5min), auto-activate/expire based on dates
- [x] Promotions module: admin CRUD + approve/reject (v1/admin/promotions), seller CRUD with PENDING_APPROVAL (v1/sellers/promotions), public active promotions + flash deals (v1/browse/promotions, v1/browse/flash-deals, Redis 2-5min)
- [x] Content module: admin CRUD (v1/admin/content), public pages by slug (v1/content/:slug, Redis 15min), page list endpoint
- [x] Settings module: admin GET/PUT (v1/admin/settings), public settings (v1/settings/public, Redis 1min), injectable getSetting(key) service
- [x] Broadcasts module: admin CRUD + send (v1/admin/broadcasts), async SMS sending via setImmediate with 100ms delay per SMS
- [x] Reports module: sales/financial/seller-performance reports with CSV export (v1/admin/reports), streaming CSV with UTF-8 BOM
- [x] Seed data: 3 banners (f0000000-), 3 promotions (f1000000-), 5 content pages (f2000000-), 8 system settings (f3000000-), 2 broadcasts (f4000000-)

### Admin Web
- [x] Dashboard trends: Recharts AreaChart/BarChart for Revenue, Orders, Users, GMV with period selector (7d/30d/90d)
- [x] Banners page: status filter tabs, table (thumbnail, title, status badge, dates, sortOrder), create/edit modal, delete
- [x] Promotions page: type/status tabs, table with approve/reject for seller submissions, create modal
- [x] Content page: slug-based CRUD, create/edit form (title fr/en, content fr/en textareas, status toggle), preview modal
- [x] Broadcasts page: create form (title, message with 160-char counter, segment dropdown), send with confirmation, status badges
- [x] Reports page: 3 tabs (Sales/Financial/Seller Performance), date range picker, CSV download
- [x] Settings page: toggle switches (booleans), text inputs (strings/numbers), maintenance mode highlighted card
- [x] Sidebar: 6 new nav items (Bannières, Promotions, Contenu, Diffusions, Rapports, Paramètres)
- [x] i18n: Banners, Promotions, Content, Broadcasts, Reports, Settings sections (fr + en)

### Buyer Web
- [x] Banner carousel: CSS scroll-snap, auto-advance 5s, dot indicators, pause on hover, fetches /v1/browse/banners
- [x] Flash deals section: horizontal scroll, countdown timers, discounted prices with crossed-out original
- [x] Content pages: dynamic /pages/:slug route, fetches /v1/content/:slug, locale-aware rendering, breadcrumb
- [x] Homepage: BannerCarousel replaces static hero (with fallback), FlashDealsSection between hero and categories
- [x] Footer: links to /pages/faq, /pages/terms, /pages/privacy, /pages/help, /pages/about
- [x] i18n: Banners, FlashDeals, ContentPages sections (fr + en)

### Seller Web
- [x] Promotions page: list with status badges, create modal (product selector, type, discount, dates), cancel for PENDING/DRAFT
- [x] Sidebar: "Promotions" nav item
- [x] i18n: Promotions section (fr + en)

### Buyer Mobile (Flutter)
- [x] Banner carousel: model, repository, provider, PageView widget (auto-advance, dot indicators, WidgetsBindingObserver)
- [x] Flash deals: model, repository, provider, flash_deal_card (countdown timer, discounted price), flash_deals_section (horizontal ListView)
- [x] Content pages: model, repository, content_page_screen (locale-aware rendering)
- [x] Home screen: BannerCarousel at top + FlashDealsSection after banners
- [x] Router: /pages/:slug route
- [x] l10n: 15 new keys (fr + en)

### Seller Mobile (Flutter)
- [x] Promotions: model (canCancel, discountCDFDisplay), repository, provider (StateNotifier), promotions_list_screen (FAB, pull-to-refresh, infinite scroll), create_promotion_screen (SegmentedButton, product dropdown, date pickers), promotion_card, promotion_status_badge
- [x] Home screen: promotions card between reviews and messages
- [x] Router: /promotions, /promotions/create routes
- [x] l10n: 25 new keys (fr + en)

### Verification Results
- `pnpm --filter api exec tsc --noEmit` — 0 errors
- `pnpm --filter @teka/shared exec tsc --noEmit` — 0 errors
- `pnpm --filter buyer-web build` — 0 errors (warnings only: unused vars in cart/search pages)
- `pnpm --filter seller-web build` — 0 errors (warnings only: pre-existing img/unused var warnings)
- `pnpm --filter admin-web build` — 0 errors (warnings only: pre-existing img warnings)
- `flutter analyze` (buyer-mobile) — 0 errors (17 info hints: pre-existing deprecated APIs)
- `flutter analyze` (seller-mobile) — 0 errors (15 info hints: pre-existing async context + unused import warnings)
- `prisma db seed` — compiles (DATABASE_URL not configured — pre-existing blocker)

## Phase 8 — Optimization & Production Readiness (COMPLETED)

### 8.1 API Performance — Redis Caching
- [x] Redis caching for browse service: categories (1hr TTL), product detail (5min TTL)
- [x] Cache invalidation on category create/update/delete and product update/archive
- [x] Graceful degradation (try-catch around cache ops)
- [x] RedisModule imported in browse, categories, products modules

### 8.2 Database Optimization — Composite Indexes
- [x] 11 composite indexes added to schema.prisma:
  - Order: `[buyerId, createdAt]`, `[sellerId, status]`, `[sellerId, createdAt]`, `[status, createdAt]`
  - Product: `[categoryId, status, createdAt]`, `[status, priceCDF]`, `[status, avgRating]`
  - Message: `[conversationId, createdAt]`
  - Review: `[productId, status]`
  - Transaction: `[orderId, status]`

### 8.3 Health Checks
- [x] Enhanced health controller: DB (`SELECT 1`) + Redis (`ping()`) checks
- [x] 3 endpoints: `/health` (status+checks), `/health/ready` (503 if deps down), `/health/live` (always 200)
- [x] `@Public()` + `@SkipThrottle()` decorators on health endpoints
- [x] Response includes uptime, memory usage, check durations

### 8.4 API Rate Limiting
- [x] Installed `@nestjs/throttler` — 100 requests/60s per IP
- [x] ThrottlerGuard as APP_GUARD (defense-in-depth alongside NGINX)
- [x] `@SkipThrottle()` on payment webhooks and health checks

### 8.5 SEO (buyer-web)
- [x] Server-side `generateMetadata()` on 5 key pages: homepage, product detail, category, search, content
- [x] Client components moved to `components/pages/` (home-page, product-detail-page, category-page, search-page, content-page-client)
- [x] Server-side API utility (`lib/server-api.ts`) for metadata fetching
- [x] JSON-LD structured data: Product schema on product detail pages
- [x] `robots.ts`: allows crawling, blocks /checkout /cart /orders /messages /login /register /profile /wishlist
- [x] `sitemap.ts`: static pages + dynamic products/categories from API (revalidated hourly)
- [x] `metadataBase: new URL('https://teka.cd')` + title template `%s | Teka RDC`
- [x] SEO i18n keys (fr + en)

### 8.6 Error Boundaries
- [x] `global-error.tsx` for buyer-web, seller-web, admin-web (inline styles, outside layout)
- [x] `error.tsx` for all 3 apps (Tailwind, i18n, retry + home link)
- [x] `not-found.tsx` for buyer-web (custom 404 with search suggestion)
- [x] Enhanced `http-exception.filter.ts` with structured logging (method, URL, userId) + Sentry placeholder
- [x] Errors i18n section added to all 6 locale files

### 8.7 PWA (buyer-web)
- [x] `manifest.ts`: PWA manifest (name "Teka RDC", theme_color #BF0000, standalone display)
- [x] `sw.js`: Vanilla service worker — cache-first for static assets, network-first for API, offline navigation fallback
- [x] `offline.html`: Minimal French offline page with retry button
- [x] Service worker registration in locale layout

### 8.8 Font Optimization
- [x] `next/font/google` Inter with `display: 'swap'` + CSS variable `--font-inter`
- [x] Applied `inter.variable` to `<html>` element
- [x] Updated `globals.css` font-family to use CSS variable fallback chain

### 8.9 Docker Production
- [x] `docker-compose.prod.yml`: Redis (password auth, 256MB), API (512MB, health check), 3 web apps (256MB), NGINX (SSL ports 80/443), json-file logging with rotation, frontend/backend networks
- [x] `nginx/nginx.prod.conf`: HTTPS with Let's Encrypt, HTTP→HTTPS redirect, HSTS (2yr), security headers (CSP, X-Frame-Options, etc.), 1yr static cache, gzip level 6
- [x] `.env.production.example`: All vars with production defaults, cloud DB/Redis placeholders
- [x] Root `.dockerignore`: excludes node_modules, .git, mobile apps, docs, .env.*
- [x] API Dockerfile: `HEALTHCHECK` instruction + `USER node` for non-root execution
- [x] `output: 'standalone'` added to buyer-web `next.config.ts`

### 8.10 Documentation
- [x] `docs/deployment.md` (462 lines): Prerequisites, step-by-step guide, env vars, SSL setup, monitoring, backup, updates/rollback, scaling
- [x] `docs/architecture.md` (425 lines): ASCII diagram, service architecture, data flows (auth, checkout, webhooks), DB schema, caching, security model
- [x] `docs/api-reference.md` (739 lines): All API endpoints by module (verified against 37 controllers), auth requirements, examples, error codes

### 8.11 End-to-End Tests
- [x] `test/test-utils.ts`: Comprehensive mocks (25+ Prisma model delegates, RedisService), `createTestApp()` helper, `resetMocks()` helper
- [x] `test/app.e2e-spec.ts`: 6 health check tests (ok, degraded DB, degraded Redis, live, ready, ready 503)
- [x] `test/auth.e2e-spec.ts`: 17 tests (OTP request/verify, register, login, /me, logout, refresh)
- [x] `test/browse.e2e-spec.ts`: 17 tests (categories cached/uncached, products search/filter/sort, banners)
- [x] `test/checkout.e2e-spec.ts`: 11 tests (cart auth, checkout auth, order auth)
- [x] `test/payments.e2e-spec.ts`: 6 tests (payment initiation, webhook, transactions)
- [x] Total: 57 test cases across 5 test files
- [x] `.env.test` + updated `jest-e2e.json` with moduleNameMapper + 30s timeout

### Verification Results
- `pnpm --filter api exec tsc --noEmit` — 0 errors
- `pnpm --filter @teka/shared exec tsc --noEmit` — 0 errors
- `pnpm --filter buyer-web build` — 0 errors (warnings only: unused eslint directive, unused var in product-reviews)
- `pnpm --filter seller-web build` — 0 errors (warnings only: pre-existing img/unused var warnings)
- `pnpm --filter admin-web build` — 0 errors (warnings only: pre-existing unused var warnings)
- `flutter analyze` (buyer-mobile) — 0 errors (17 info hints: pre-existing deprecated APIs)
- `flutter analyze` (seller-mobile) — 0 errors (15 info hints: pre-existing async context + unused import warnings)

---

## Monolingual Refactor — French Only (2026-04-25)

**Constraint:** Preserve API contracts. DB JSONB columns keep `{ fr, en }` shape; API responses unchanged. Only UI / URL surface goes FR-only.

### Web (3 apps)
- [x] **M1** — `routing.ts` to single locale (`locales: ['fr']`, `localePrefix: 'never'`, no detection)
- [x] **M2** — Deleted `messages/en.json` everywhere; only `fr.json` remains
- [x] **M3** — Deleted language-switcher component; stripped imports from header (buyer-web), seller-web home + dashboard layout, admin-web home
- [x] **M4** — Sitemap (`apps/buyer-web/src/app/sitemap.ts`): single-locale URLs
- [x] **M5** — Dropped `alternates.languages` from all metadata blocks (home, product, category, categorie/[slug], static [slug])
- [x] **M6** — `next.config.ts` redirects: simplified — `/pages/<canonical>` → `/<fr>` + cross-language `/<en-slug>` → `/<fr-slug>` + wildcard `/en/:path*` → `/:path*`
- [x] **M7** — `static-pages.ts`: collapsed `Record<Locale, string>` to plain `string`; dropped helpers' locale parameter
- [x] **M8** — Stripped `locale === 'en'` conditional rendering from product page metadata + all hreflang blocks

### Mobile (2 apps)
- [x] **M9** — Deleted `app_en.arb` from buyer-mobile + seller-mobile (generated `app_localizations_en.dart` left in place — harmless until next `flutter gen-l10n` run regenerates)
- [x] **M10** — Locale provider: state fixed at `Locale('fr')`, `setLocale` is a no-op, persistence dropped
- [x] **M11** — `LocaleNotifier.supportedLocales` → `[Locale('fr')]`

### Backend / DB
- **No changes.** API contract preserved per spec.

### Verification
- [x] **V1** — Type-check + production build pass for all 3 web apps
- [x] **V2** — `flutter analyze --no-fatal-infos` passes for both mobile apps (only baseline info-level deprecations)
- [x] **V3** — API e2e suite: 71/71 still pass
- [ ] **V4** — Smoke after deploy: `https://teka.cd/` renders FR; no language switcher visible; `https://teka.cd/en/<anything>` 308→`/<anything>`

### Notes
- `[locale]` route segment kept on disk (no file moves needed). With `localePrefix: 'never'`, the segment is effectively constant `'fr'` and URLs have no prefix.
- `useLocale()` calls in components stay — always return `'fr'`. Cleaner removal can come in a follow-up.
- DB JSONB shape (`{ fr, en }`) untouched. EN strings still live there but no UI surface exposes them.

---

## Profile Management Initiative (2026-05-20 → 2026-05-21)

Two-phase user-facing surface for self-service account management on all 5 frontends. Both phases shipped via real `develop → main` merges (no squash) and live-validated in production.

### Phase 7a — Notification Preferences (PRs #138, #139)

- [x] **7a-A1** — Prisma: `User.notificationPrefs JSONB?` (nullable; default behavior is opt-in)
- [x] **7a-A2** — DTO + service: `NotificationPrefsDto` (smsOrderUpdates, smsBroadcasts), `NotificationPrefsService.resolve / update / shouldSendOrderUpdates / shouldSendBroadcasts`
- [x] **7a-A3** — Controller: `GET /v1/users/notification-prefs`, `PATCH /v1/users/notification-prefs`
- [x] **7a-A4** — Enforcement: 6 SMS sites in `order-notification.service.ts` + the broadcast loop in `broadcasts.service.ts` now gate on `shouldSend*(userId)`
- [x] **7a-A5** — Manual SQL migration applied to prod: `apps/api/prisma/migrations/manual/2026-05-20_user_notification_prefs.sql`
- [x] **7a-W1..W3** — "Notifications" card on `/profil` (buyer-web), `/dashboard/profile` (seller-web, admin-web) with 2 switches + auto-save + optimistic toggle
- [x] **7a-M1..M2** — Same card on `profile_screen.dart` (buyer-mobile, seller-mobile) with l10n keys regenerated via `flutter gen-l10n`

### Phase 7b — Session Management (PRs #140, #141)

- [x] **7b-A1** — `JwtStrategy.validate` returns `jti` so `@CurrentUser('jti')` works
- [x] **7b-A2** — `generateTokens` + `generateTokensForUser` accept optional `{ userAgent, ipAddress }`; persisted to `RefreshToken.deviceInfo` (cap 500 chars) + `ipAddress`
- [x] **7b-A3** — Plumbed `device` through all 5 callers: loginWithEmail, registerWithEmailForRole, refreshTokens, buyerOtpService.verifyOtp, buyerClaimService.verifyClaim
- [x] **7b-A4** — Controllers add `@Req() req: Request` + `extractDevice()` helper (User-Agent + `req.ip`)
- [x] **7b-A5** — `SessionsService` + 3 endpoints under `/v1/users`:
  - `GET /sessions` (active rows only, current flagged)
  - `DELETE /sessions/:id` (refuses self; 404 cross-account)
  - `DELETE /sessions` (revoke all except current)
- [x] **7b-W1..W3** — "Appareils connectés" card on all 3 web profile pages (list + per-row Déconnecter + bulk "Déconnecter les autres appareils")
- [x] **7b-M1..M2** — `_SessionsCard` widget on both mobile profile screens (mirrors web parity)
- [x] **7b-V1** — 90/90 e2e green; type-check + flutter analyze clean on all 5 frontends
- [x] **7b-V2** — Production smoke validated: list / per-row revoke / bulk revoke all behave as designed

### Follow-up — Real Client IP (PRs #142, #143)

Phase 7b smoke surfaced `RefreshToken.ipAddress` was rendering as `::ffff:172.18.0.6` (nginx's internal docker IP). Express's `req.ip` returns the socket address unless the app opts into `trust proxy`.

- [x] **F1** — `apps/api/src/main.ts`: `app.getHttpAdapter().getInstance().set('trust proxy', 1)` to trust the single nginx hop
- [x] **F2** — No backfill: existing rows roll off naturally on the 7d refresh-token TTL
- [x] **F3** — Verified post-deploy: fresh login on seller.teka.cd showed real public IP `197.250.153.149` (was previously `::ffff:172.18.0.6`)

### Test coverage

- API e2e: 90/90 green (was 71 pre-initiative, +19 incremental coverage across the initiative)
- Web type-check: clean on all 3 apps
- Flutter analyze: 0 errors on both mobile apps (pre-existing info-level deprecation warnings only)

---

## ALL 8 PHASES COMPLETE

The Teka RDC e-commerce marketplace is feature-complete across all 5 frontends (API, 3 web apps, 2 mobile apps) and production-ready with:
- Full authentication (email + password for sellers/admins; WhatsApp OTP via Gupshup for buyers since 2026-05-15)
- Product catalog with categories, search, filters
- Shopping cart, checkout, order lifecycle
- Mobile Money payments + COD
- Reviews, wishlists, buyer-seller messaging
- Admin dashboard with full platform management
- SEO, PWA, error boundaries, health checks
- Docker production configs, SSL, documentation
- 90 e2e test cases covering critical paths
- Self-service profile management: avatar / personal info / password / notification preferences / active session list (2026-05-20)
